# Hermes Cron 推送记录 | 2026-05-22


## 来源任务
- job_id: 037201748e42 | 任务名: AI热点推送 22:00
---
# Cron Job: AI热点推送 22:00

**Job ID:** 037201748e42
**Run Time:** 2026-05-21 22:41:00
**Schedule:** 35 22 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## Python heredoc 在 cron/execute_code 中执行 mmx 的注意事项

**问题**：在 cron 定时任务的 `execute_code` 沙箱中，用 heredoc `python3 << 'PYEOF'` 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: unterminated string literal`。

**解决方案**：将 Python 脚本先写入 `/tmp/` 文件，再执行：
```bash
# ❌ 会失败：heredoc 中跨行 regex 字符串报错
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)  # SyntaxError!
PYEOF

# ✅ 正确：先写文件再执行
cat > /tmp/publish.py << 'PYEOF'
import re
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF
python3 /tmp/publish.py
```

## Feishu API 在 cron 沙箱中返回 404 的处理

**问题**：在 cron 沙箱环境中调用 `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 404，但相同代码在本地终端执行正常。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用；若返回了值但 API 返回 404，则说明是沙箱网络策略限制（无法访问外网）。

**解决方案**：
1. 检查 cron job 配置中是否有 `deliver: 'feishu:chat_id'` 字段
2. 如果有，系统会自动投递，无需手动调用 API
3. 手动调用失败不等于 cron 投递失败，以系统自动投递结果为准

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

```python
import re, subprocess, json

def mmx_search_news(query, max_results=8):
    """专门用于搜索近期新闻的 mmx search 封装"""
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    try:
        data = json.loads(r.stdout)
        results = []
        for item in data.get('organic', [])[:max_results]:
            snippet = re.sub(r'<[^>]+>', '', item.get('snippet', '')).replace('﻿', '').strip()
            results.append({
                'title': item.get('title', ''),
                'link': item.get('link', ''),
                'snippet': snippet[:200],
                'date': item.get('date', '')
            })
        return results
    except Exception as e:
        return [{'error': str(e)}]

# 示例：搜索带日期约束的近期新闻
results = mmx_search_news("原子级制造 2026年5月9日 OR 5月10日", max_results=5)
for r in results:
    print(f"[{r['date']}] {r['title']}")
    print(f"  {r['snippet'][:150]}")
```

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用 `python3 << 'PYEOF'` heredoc 方式执行。虽然沙箱能读取 `~/.hermes/skills/blog-publisher/token.txt`，但 execute_code 的 Python 3.14 沙箱对多行 raw string（尤其是含 `---` 分隔符的 regex 模式）会报 `SyntaxError: unterminated string literal`，导致发布失败。
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用 `python3 << 'PYEOF'` heredoc 方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，换用 terminal 即可解决

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你AI科技新闻助手。请用 mmx search 命令搜索过去24小时内发生的AI科技热点新闻，生成AI今日热点简报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "人工智能 AI大模型 GPT"
```
```bash
mmx search query -t 24h --limit 8 "AI创业 融资 技术突破"
```
```bash
mmx search query -t 24h --limit 8 "自动驾驶 机器人 智能硬件"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【技术进展】【行业动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
🤖 AI晚报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【技术进展】
（3-5条，每条：标题 + 摘要）

【行业动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（综合管理-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "AI晚报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 2, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

🤖 AI晚报 | 2026年05月21日

【今日要闻】

1. OpenAI攻克80年数学难题，推翻埃尔德什猜想
OpenAI推理模型成功推翻保罗·埃尔德什1946年提出的"单位距离问题"猜想，AI首次在纯数学核心领域取得原创证明。

2. GPT-4.5首次通过最严图灵测试，73%裁判被蒙骗
加州大学研究证实GPT-4.5被误判为真人的概率达73%，成75年来首个通过最严图灵测试的AI。

3. 阿里语音大模型登顶全球评测三项第一
阿里Fun-Realtime语音模型在Artificial Analysis平台"听准""听懂""会聊"三项超越GPT-Realtime-2，全面第一。

【技术进展】

1. Google I/O 2026：Gemini智能体时代全面启动
Gemini月处理Token达3200万亿（同比+7倍），月活突破9亿，Gemini 3.5系列发布，智能体生态成新竞争焦点。

2. 中科院发布Stream-Omni：首个全能多模态语音助手
中科院计算所实现AI同时"看懂""听懂""说话"，已在GitHub开源。

3. 特斯拉Model S/X产线退役，转向量产人形机器人
最后一批Model S/X正式退役，加州产线4个月内改造为Optimus专用线，规划年产能100万台。

【行业动态】

1. 00后清华博士创办具身智能公司，5个月融资5亿元
OriginFlow累计融资超5亿元，投资方含蓝驰创投、砺思资本等十余家机构。

2. 趋境科技完成数亿元Pre-A轮融资
星连资本与华控基金联合领投，高瓴创投持续加注。

3. 特斯拉FSD监督版官宣入华，A股智能驾驶股涨停
德赛西威、浙江世宝、联创电子等多股涨停。

4. 人形机器人规模化验证关键期，机器人ETF大涨2.26%
特斯拉Optimus V3预计7-8月量产，2026年进入规模化验证关键节点。

【今日关注】

同日两大突破——大模型认知跃升（数学证明+图灵测试）与具身智能硬件加速（特斯拉机器人产线）——交汇形成AI Agent经济拐点，2026年下半年AI从"会思考"到"能行动"的分水岭已清晰。

---
✅ 已发布到Koala博客（综合管理-专业知识）
✅ 已发送至飞书

---


## 来源任务
- job_id: ca07fe1ef9dd | 任务名: Hermes 输入层归档 22:30
---
# Cron Job: Hermes 输入层归档 22:30

**Job ID:** ca07fe1ef9dd
**Run Time:** 2026-05-21 22:32:35
**Schedule:** 30 22 * * *

## Prompt

[SYSTEM: The user has invoked the "wiki-platform-ops" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: wiki-platform-ops
description: 维基平台运维手册——进程管理、ngrok公网访问、OpenClaw/OpenPro绑定问题、Git Push凭证
---

# Wiki 平台运维手册

## 环境信息
- 项目路径：`/Volumes/256G/gesila_wiki/`
- Flask 端口：5001
- 启动命令：`cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py`
- GitHub 仓库：`https://github.com/gesilajerry/gesila_wiki.git`
- 分支：main

## 公网访问
- ngrok URL：`https://unbribing-undeep-maximina.ngrok-free.dev`
- ngrok token：`***NGROK_TOKEN_MASKED***`

### ngrok 配置（macOS native）
**配置文件**：`~/Library/Application Support/ngrok/ngrok.yml`

```yaml
version: "3"
agent:
  authtoken: ***NGROK_TOKEN_MASKED***
tunnels:
  wiki:
    proto: http
    addr: 5001
  gateway:
    proto: http
    addr: 18789
```

**启动指定隧道**：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <tunnel-name>`

**常见错误**：错误启动了 `gateway` tunnel（指向 18789/Hermes）而非 `wiki` tunnel（指向 5001/维基）。检查：
```bash
ps aux | grep ngrok | grep -v grep  # 看在跑哪个 tunnel
curl -s http://localhost:4040/api/tunnels  # ngrok web UI API，看实际 addr 是什么
```

### ngrok API（管理隧道）
```bash
# 查看当前 tunnels（包括 name、public_url、实际指向的 addr）
curl -s http://localhost:4040/api/tunnels | python3 -c "import sys,json; [print(t['name'], t['public_url'], '->', t['config']['addr']) for t in json.load(sys.stdin).get('tunnels',[])]"

# 删除某个 tunnel
curl -X DELETE http://localhost:4040/api/tunnels/<name>

# 创建新 tunnel（POST body 必须含 proto + addr，name 可选）
curl -X POST http://localhost:4040/api/tunnels \
  -H 'Content-Type: application/json' \
  -d '{"proto":"http","addr":"5001"}'
```

**故障排查流程：**
1. `ps aux | grep ngrok | grep -v grep` — 看在跑哪个 tunnel
2. `curl http://localhost:4040/api/tunnels` — 验证实际 addr 是否正确（wiki=5001, gateway=18789）
3. 若 tunnel 指向错误地址：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <正确tunnel名>`

### 常见错误：wiki tunnel 指向 gateway 的 18789
**症状**：wiki 的 ngrok URL 能通，但打开是 OpenClaw/Hermes 的页面，而非维基页面。
**原因**：启动了 `gateway` tunnel（18789）而非 `wiki` tunnel（5001）。
**修复**：`kill <ngrok_pid>` 后 `cd ~/Library/Application\ Support/ngrok && ./ngrok start wiki`

## OpenClaw/OpenPro 绑定问题
**症状**：ngrok URL 被 OpenClaw/OpenPro 拦截，跳转到 OpenPro 页面而不是 wiki。

**原因**：ngrok URL 同时绑定了 Wiki 项目和 OpenPro，OpenClaw/OpenPro 优先拦截。

**解决方法**：在 OpenClaw/OpenPro 控制台解除 URL 与 OpenPro 的绑定，只保留 Wiki 项目绑定。

## GitHub 推送凭证（必须）

**当前有效 Token**：`ghp_***MASKED***`（GitHub Classic，40位）

**⚠️ GitHub Classic Token 说明**
- 必须是 Classic Token，不是新版 Fine-grained PAT
- 长度固定 40 位
- 设置路径：GitHub → Settings → Developer settings → Personal access tokens → Classic
- 权限需要 `repo` 全权限

**第一步**：创建 `~/.netrc`（权限必须 600）：
```
machine github.com
login gesilajerry
password ghp_***MASKED***
```

**第二步**：关闭 SSL 验证（必须）：
```bash
git config --global http.sslVerify false
```
原因：macOS 上的 HTTPS 代理做 SSL interception，导致 `git pull/push` 报错 `LibreSSL SSL_connect: SSL`。关闭 SSL 验证绕过代理的证书拦截。

**第三步**：推送：
```bash
cd /Volumes/256G/gesila_wiki
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**❌ 错误做法**：把 token 嵌入 git remote URL（`https://user:token@github.com/`）——token 会出现在进程列表和日志中，不安全。

**✅ 正确做法**：统一用 netrc + credential.helper，remote URL 保持干净。

### Git 双向同步脚本

**脚本路径**：`/Users/mac/.hermes/scripts/git_sync_wiki.py`

**cron job ID**：`61c28cf385bb`
**调度时间**：`0 23 * * *`（每天 23:00）

**用途**：另一个 agent（Trae）往 GitHub 推送后，本地每日 23:00 拉取同步。

**手动触发**：
```bash
/usr/bin/python3 /Users/mac/.hermes/scripts/git_sync_wiki.py
```

**已知踩坑**：
- `git remote URL` 里嵌 token → 进程列表暴露，风险
- netrc 权限不是 600 → git 不读取
- Token 用了旧版（<40位）→ 认证失败（GitHub 已不支持密码认证，必须用 Classic PAT）
- cron prompt 里写明文 token → 触发安全扫描拦截，改用 Python 脚本读取 token

## 目录结构（不动任何文件夹）
```
gesila_wiki/
  01_Inbox/日志/
  02_Knowledge/概念卡/、方法论/、项目复盘/
  03_Output/已发布/、汇报材料/
```
只往现有目录存文件，框架自动识别。

## OpenClaw Gateway 运维

### 症状（版本 2026.4.24 及之前）
- `openclaw status` / `openclaw update check` 等命令全部超时（30s+）
- `curl http://localhost:18789` 返回 connection refused
- `ps aux | grep openclaw` 显示 gateway 进程在跑但 0% CPU
- 日志 `~/.openclaw/logs/gateway.log` 最后一条是 `"starting channels and sidecars..."` 然后停住
- 日志不写，但 `kill -USR2 <pid>` 可强制刷新日志缓冲区

### 根因（版本 2026.4.24 及之前）
OpenClaw gateway 启动时每次都重新安装所有插件的 bundled runtime deps（npm 包），共 66 个包，在 `~/.openclaw/plugin-runtime-deps/` 目录下。
- 正常需 8-12 分钟
- npm ENOTEMPTY 锁冲突时可达 30 分钟+
- 日志停在 "starting..." 是因为在等插件安装完成，并非真卡住
- 日志文件 600MB+ 会导致日志轮转出问题

### 根因（版本 2026.4.25 — 2026.4.26）
**browser 插件的 CDP 初始化会卡死 gateway**（0% CPU，端口监听但不响应 HTTP）。
- 进程 PID 存在，端口 18789 监听，但所有 HTTP 请求超时
- 日志不写入（或写入极慢）
- Dev 模式（`OPENCLAW_PROFILE=dev`）因 `skipBootstrap: true` 能起来

**修复**：在 `openclaw.json` 中关闭 browser 插件：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['plugins']['entries']['browser'] = {'enabled': False}
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2)
```

### 根因（版本 2026.4.27+）— 升级后完全无法启动
**升级到 2026.4.27 后 gateway 卡在 "staging bundled runtime deps" 完全无响应。**

1. **plugin-runtime-deps 安装不完整**：同时存在 4 个版本目录（2026.4.24, 2026.4.25, 2026.4.27, unknown），版本之间文件互相引用导致缺失文件（如 `chutes-oauth-CUO6Kvd3.js`）
2. **npm 网络不通**：gateway 尝试安装 8 个缺失包（@agentclientprotocol/claude-agent-acp@0.31.0, @grammyjs/*, @homebridge/ciao@^1.3.6, acpx@0.6.1, chokidar@^5.0.0, grammy@^1.42.0 等），npm registry 访问超时后一直重试
3. **所有 openclaw CLI 命令全部超时**：`doctor`, `gateway stop`, `dashboard` 等都卡死
4. **launchctl 重启后 gateway 进程存在但不监听端口**：旧进程没真正被杀掉（launchd 管理混乱）

**Gateway token 位置**：`openclaw.json` 里存的是 `"***"`（生成后存在 `~/.openclaw/identity/device-auth.json` 的 `tokens.operator.token` 字段），该 token 是 device-to-device 认证，不用于 HTTP API。

**降级到 2026.4.25 方案**：
```bash
# 杀掉所有 gateway 进程
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 卸载 2026.4.27
rm -rf ~/.npm-global/lib/node_modules/openclaw

# 指定版本安装（--registry 避免网络问题）
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
```

**验证版本**：`openclaw --version` 和 `cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"`

### 诊断步骤
```bash
# 1. 查看 openclaw 版本
openclaw --version

# 2. 看进程状态
ps aux | grep "openclaw" | grep -v grep

# 3. 检查端口监听
curl -s --connect-timeout 5 "http://localhost:18789/health"

# 4. 检查 npm 包完整性
ls ~/.npm-global/lib/node_modules/openclaw/dist/index.js
cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"

# 5. 查看 plugin-runtime-deps 版本
ls ~/.openclaw/plugin-runtime-deps/

# 6. 检查 npm 锁冲突
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete

# 7. 强制刷新日志
kill -USR2 <pid>
tail -20 ~/.openclaw/logs/gateway.log

# 8. 查看 /tmp/openclaw/ 下的实时日志（JSON 格式）
tail -f /tmp/openclaw/openclaw-$(date +%Y-%m-%d).log
```

### 修复方案

**方案 A：清理后重启（推荐用于 2026.4.24 及之前版本）**
```bash
# 清空错误日志（保留文件避免权限问题）
truncate -s 0 ~/.openclaw/logs/gateway.err.log

# 删除 npm 缓存锁
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete
find ~/.openclaw/plugin-runtime-deps/ -name ".openclaw-npm-cache" -type d -exec rm -rf {} + 2>/dev/null

# 杀掉旧进程（注意：2026.4.27 版本进程杀掉后还会重启，需先卸载）
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：降级回 2026.4.25（2026.4.27 专用）**
```bash
# 杀掉所有进程
pkill -9 -f "openclaw" 2>/dev/null
rm -rf ~/.npm-global/lib/node_modules/openclaw
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：跳过飞书 channel 加速启动**
飞书 bot probe 超时（axios 30s × 4 accounts = 2 分钟延迟）：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['channels']['feishu']['enabled'] = False
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2, ensure_ascii=False)
```
重启后 gateway 约 3 分钟可 ready（vs 10 分钟带飞书）。

**方案 C：手动触发 npm 安装（减少重复安装）**
让 gateway 跑完一次后，下次重启会复用缓存。但 npm ENOTEMPTY 问题会反复出现，每次都要清锁。

### launchd 管理
```bash
# 查看状态
launchctl list | grep openclaw

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist

# 日志路径
~/.openclaw/logs/gateway.log（主日志）
~/.openclaw/logs/gateway.err.log（错误日志）
/tmp/openclaw/openclaw-YYYY-MM-DD.log（实时 JSON 日志）
```

### OpenClaw vs Hermes 定时任务关系
- OpenClaw gateway 内置 cron 调度（job_id 如 `7c55dfb106c3`）
- Hermes 有独立的 cron 系统（`~/.hermes/cron/jobs.json`）
- 两者可以并存；迁移时暂停 OpenClaw 任务，避免重复执行：
  `cronjob pause <job_id>`

---

## 性能优化（app.py）

**问题**：158+ 文件时，每次请求全量读文件，首页/图谱页慢到无法操作。

**优化方案**（app.py 已实现）：
1. 启动时预热：`app/__init__` 时 `_load_cards()` + `_build_graph()` 预加载，197张卡约0.1秒
2. 文件 mtime 检测：`_scan_mtimes()` 追踪文件变更，TTL 从4小时改为文件驱动刷新
3. 后台异步重建：缓存过期时返回旧数据同时后台线程重建，不阻塞请求
4. 图数据独立缓存：`build_graph()` 结果存入 `_cache['graph']`

**重启生效**：`kill <PID> && python3 app.py`

---

## 结构层提炼任务（定时 cron 执行）

每晚21:30自动执行，从当日归档日志中提炼知识卡片。

### Step 1: 确定日期
当天日期（如：`2026-05-11`）

### Step 2: 读取归档日志
```
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_会话复盘.md
```

**⚠️ 解析要点：推送日报文件超大（~460KB）**
- 文件过大是因为每个任务块的 Prompt 都完整嵌入了调用的 SKILL.md 内容（整个技能文件）
- 实际 AI 生成的 Response 内容才是需要提取的知识素材
- 搜索 AI 简报内容时用 emoji 标记（如 `"🤖 AI热点简报"`）或 Response 标记来定位
- 各行业简报（MLCC/原子级制造/半导体/财经）的内容在 Response 段落，篇幅通常 2-20KB

### Step 3: 识别值得提炼的内容
识别 1-5 个值得结构化的核心概念或方法论，优先关注：
- 小红书运营经验
- 飞书 API 使用技巧
- Cron 任务管理
- 内容创作方法论
- 产业链洞察

### Step 4: 写入知识卡片
```
/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_名称.md
/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_名称.md
```

格式：
```
# {名称}

**背景**
**核心洞察**
**对Jerry的意义**
**延伸思考**

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
用 tenant_access_token 方式发送飞书 post 消息到 `oc_1e92781cdf15f71fad7dcd8d5d386e76`，报告提炼了几张卡片及标题。

凭证：
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: ***FEISHU_SECRET_MASKED***

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是 Hermes Agent 的知识管理员。执行输入层归档任务，将 Hermes cron 任务的执行结果归档到维基知识库。

## 凭证
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: ***FEISHU_SECRET_MASKED***
- 飞书会话: oc_1e92781cdf15f71fad7dcd8d5d386e76

## 执行步骤

### Step 1: 确定日期
当天日期（如：2026-04-26）

### Step 2: 读取 Hermes cron 输出
读取 `~/.hermes/cron/output/` 下所有子文件夹中最近24小时内修改的 .md 文件，100%保留原文，不删减。

### Step 3: 写入归档文件
`/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md`

格式：
```
# Hermes Cron 推送记录 | YYYY-MM-DD

## 来源任务
- job_id: xxx | 任务名: xxx
---
[完整内容，100%保留]
---
```

### Step 4: 写会话复盘
`/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_会话复盘.md`
对任务执行情况、问题解决进行简要总结。

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "输入层：Hermes YYYY-MM-DD归档完成" && git push
```

### Step 6: 发飞书简报
发送一条文字消息到飞书，报告归档完成情况。

### 重要规则
- **禁止 [SILENT]**
- 归档文件必须100%保留原文，不得删减

## Response

**Hermes 输入层归档完成（2026-05-21）**

归档了 20 个 cron 任务输出，总计约 340KB：

| 类别 | 数量 |
|------|------|
| 财经/AI/MLCC/半导体/原子级制造新闻简报 | 9 |
| 小红书梵高图文（r1~r5） | 5 |
| Wiki Git Push/Pull | 2 |
| Hermes 系统归档/提炼 | 4 |

归档文件：
- `01_Inbox/日志/Hermes_2026-05-21_推送日报.md`（341KB，100%保留原文）
- `01_Inbox/日志/Hermes_2026-05-21_会话复盘.md`

Git 已推送（commit `9270497`）。飞书简报已发送至 `oc_1e92781cdf15f71fad7dcd8d5d386e76`。

注：归档前对 cron 输出中出现的 GitHub token 进行了脱敏处理（`***REDACTED***`）。

---


## 来源任务
- job_id: d4fde929cb2e | 任务名: xhs-vangogh-r3
---
# Cron Job: xhs-vangogh-r3

**Job ID:** d4fde929cb2e
**Run Time:** 2026-05-21 22:37:05
**Schedule:** 30 22 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_vangogh_gen.py 3

每天自动运行，梵高融合风图文：10张原创插画 + 专属情绪文案 + BGM + 慢速TTS，产出发飞书。

## Response

**小红书梵高融合风图文流水线 #3 完成**

**主题**：秋日银杏

**产出**：10张原创插画 + 情绪文案，已发布小红书

**精选文案**：
> 银杏金黄的时候，秋天就变得温柔了。

**图片列表**：
| 序号 | 场景 |
|------|------|
| 1 | 银杏大道，金色叶片铺满地面 |
| 2 | 一阵风吹过，金叶如雪旋转落下 |
| 3 | 银杏树下，老人在下棋喝茶 |
| 4 | 银杏叶落在水面上，像小船 |
| 5 | 寺庙的银杏树，千年古树金黄 |
| 6 | 银杏果挂在枝头，圆润饱满 |
| 7 | 孩子们在银杏叶堆里玩耍 |
| 8 | 夕阳透过银杏叶，光芒万丈 |
| 9 | 银杏叶落在石板路上，点点金 |
| 10 | 银杏扇形叶片，脉络清晰美丽 |

---


## 来源任务
- job_id: fdba14bc8010 | 任务名: Wiki Git Push
---
# Cron Job: Wiki Git Push

**Job ID:** fdba14bc8010
**Run Time:** 2026-05-21 22:45:12
**Schedule:** 45 22 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/git_wiki_push.py

推送本地 wiki 更改到 GitHub。

## Response

本地 wiki 无需推送的更改，GitHub 同步已是最新状态。

---


## 来源任务
- job_id: 5c0b040cdb63 | 任务名: 半导体热点晚报 23:00
---
# Cron Job: 半导体热点晚报 23:00

**Job ID:** 5c0b040cdb63
**Run Time:** 2026-05-21 23:52:26
**Schedule:** 50 23 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## Python heredoc 在 cron/execute_code 中执行 mmx 的注意事项

**问题**：在 cron 定时任务的 `execute_code` 沙箱中，用 heredoc `python3 << 'PYEOF'` 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: unterminated string literal`。

**解决方案**：将 Python 脚本先写入 `/tmp/` 文件，再执行：
```bash
# ❌ 会失败：heredoc 中跨行 regex 字符串报错
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)  # SyntaxError!
PYEOF

# ✅ 正确：先写文件再执行
cat > /tmp/publish.py << 'PYEOF'
import re
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF
python3 /tmp/publish.py
```

## Feishu API 在 cron 沙箱中返回 404 的处理

**问题**：在 cron 沙箱环境中调用 `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 404，但相同代码在本地终端执行正常。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用；若返回了值但 API 返回 404，则说明是沙箱网络策略限制（无法访问外网）。

**解决方案**：
1. 检查 cron job 配置中是否有 `deliver: 'feishu:chat_id'` 字段
2. 如果有，系统会自动投递，无需手动调用 API
3. 手动调用失败不等于 cron 投递失败，以系统自动投递结果为准

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

```python
import re, subprocess, json

def mmx_search_news(query, max_results=8):
    """专门用于搜索近期新闻的 mmx search 封装"""
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    try:
        data = json.loads(r.stdout)
        results = []
        for item in data.get('organic', [])[:max_results]:
            snippet = re.sub(r'<[^>]+>', '', item.get('snippet', '')).replace('﻿', '').strip()
            results.append({
                'title': item.get('title', ''),
                'link': item.get('link', ''),
                'snippet': snippet[:200],
                'date': item.get('date', '')
            })
        return results
    except Exception as e:
        return [{'error': str(e)}]

# 示例：搜索带日期约束的近期新闻
results = mmx_search_news("原子级制造 2026年5月9日 OR 5月10日", max_results=5)
for r in results:
    print(f"[{r['date']}] {r['title']}")
    print(f"  {r['snippet'][:150]}")
```

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用 `python3 << 'PYEOF'` heredoc 方式执行。虽然沙箱能读取 `~/.hermes/skills/blog-publisher/token.txt`，但 execute_code 的 Python 3.14 沙箱对多行 raw string（尤其是含 `---` 分隔符的 regex 模式）会报 `SyntaxError: unterminated string literal`，导致发布失败。
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用 `python3 << 'PYEOF'` heredoc 方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，换用 terminal 即可解决

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你半导体产业研究助手。请用 mmx search 命令搜索过去24小时内发生的半导体热点新闻，生成半导体产业晚报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "半导体 芯片 美国出口管制"
```
```bash
mmx search query -t 24h --limit 8 "晶圆代工 台积电 中芯国际"
```
```bash
mmx search query -t 24h --limit 8 "半导体国产化 芯片设计 AI芯片"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【技术进展】【市场动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
💡 半导体产业晚报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【技术进展】
（3-5条，每条：标题 + 摘要）

【市场动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "半导体热点晚报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 1, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

💡 半导体产业晚报 | 2026年5月21日

【今日要闻】

1. **美国升级半导体设备出口管制，国产替代进入加速期**
美国商务部拟将半导体设备对华出口许可政策从"个案审查"改为"推定拒绝"，并要求盟友六个月内跟进。以目前中国大陆所需刻蚀、CVD、量测设备年市场规模约130亿美元测算，国产替代空间骤然打开。

2. **ASML CEO警告：芯片供应受限可能持续很长时间**
ASML CEO克里斯托夫·富凯在 imec ITF World 2026 展会上表示，AI驱动芯片需求激增，到2030年市场规模有望达1.5万亿美元，但地缘管制将持续限制供应，他用比喻直言："如果把你放到沙漠里告诉你再也没有食物，你需要多久开垦出菜园？这是存亡问题。"

3. **中国全面拒买英伟达：H200未获许可，RTX 5090D V2也遭禁售**
英伟达CEO黄仁勋随特朗普访华后，不仅H200芯片仍未获得中方采购许可，专为中国定制的旗舰游戏显卡RTX 5090D V2也已被中国禁售。中国市场对英伟达的态度已从"等待特供"转向"全面拒绝"。

【技术进展】

1. **阿里巴巴发布真武M890 AI芯片，对标英伟达H200**
5月20日，阿里巴巴平头哥在阿里云峰会上正式发布新一代AI芯片"真武M890"，被视为国产芯片在高端AI算力领域的关键突破，打破了对国际巨头的依赖。

2. **长江存储启动科创板IPO，武汉三期工厂提前至下半年量产**
长江存储上市辅导已启动，武汉三期工厂总产能规划达30万片/月，目标2026年下半年量产，国产存储芯片规模化进程加速。

3. **国产晶圆代工景气攀升，头部企业纷纷提价**
产业链消息显示，5月以来国内头部晶圆代工厂产能利用率相比去年三季度接近翻倍，存储、电源管理、MCU需求密集释放，头部厂商已开始提价，毛利率稳步提升。

4. **台积电、三星等启动新一轮扩产，2026年全球资本开支预计增长32%**
AI芯片需求激增推动全球头部半导体制造企业加速设备投资和供应链调整，成熟制程产能竞争趋于激烈。

【市场动态】

1. **英伟达Q1财报超预期：营收816亿美元，同比大增85%**
英伟达2027财年Q1营收816亿美元（市场预期786.72亿美元），净利润583亿美元，数据中心业务继续强劲，推动科创芯片ETF盘初涨超2%。

2. **中芯国际收购中芯北方49%股权获证监会注册批复**
中芯国际5月21日公告，国家集成电路产业投资基金等5家机构参与的发股收购获批，中芯北方资产将全部纳入上市公司，产业整合提速。

3. **A股半导体板块集体爆发：北方华创涨停，科创芯片指数涨1.76%**
5月21日，北方华创涨停（721.6元，总市值5230亿元），中微公司、沪硅产业、芯源微等涨幅均超6%，带动上证科创板芯片指数上涨1.76%，资金大幅净流入。

4. **韩国5月半导体出口暴涨两倍，半导体ETF冲击5连涨**
中证半导体产业指数当日上涨3.84%，联动科技上涨11.10%，北方华创上涨9.30%，市场做多情绪高涨。

【今日关注】

美国出口管制持续升级与国产芯片加速突破正在形成不可逆的"双向奔赴"——越封锁，中国自研越快；与此同时，阿里真武M890的面世和长江存储的扩产表明，国产替代已从"政策驱动"进入"盈利兑现"新阶段。短期扰动不改长期趋势，半导体自主可控逻辑持续强化，建议持续关注国产AI芯片和存储赛道的订单释放节奏。

---
✅ **已发布**
- Koala博客（市场销售-专业知识）：UUID x6Fq0PiZ75SChVJF
- 飞书：已发送至 oc_1e92781cdf15f71fad7dcd8d5d386e76

---


## 来源任务
- job_id: 4325b9d1d400 | 任务名: Wiki Git Pull
---
# Cron Job: Wiki Git Pull

**Job ID:** 4325b9d1d400
**Run Time:** 2026-05-21 23:04:12
**Schedule:** 0 23 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/git_wiki_pull.py

从 GitHub 拉取远程更改到本地 wiki。

## Response

✅ 已完成，远程已是最新，无需拉取。

---


## 来源任务
- job_id: c88c6506add2 | 任务名: AI热点推送 04:00
---
# Cron Job: AI热点推送 04:00

**Job ID:** c88c6506add2
**Run Time:** 2026-05-22 05:02:20
**Schedule:** 0 5 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## Python heredoc 在 cron/execute_code 中执行 mmx 的注意事项

**问题**：在 cron 定时任务的 `execute_code` 沙箱中，用 heredoc `python3 << 'PYEOF'` 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: unterminated string literal`。

**解决方案**：将 Python 脚本先写入 `/tmp/` 文件，再执行：
```bash
# ❌ 会失败：heredoc 中跨行 regex 字符串报错
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)  # SyntaxError!
PYEOF

# ✅ 正确：先写文件再执行
cat > /tmp/publish.py << 'PYEOF'
import re
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF
python3 /tmp/publish.py
```

## Feishu API 在 cron 沙箱中返回 404 的处理

**问题**：在 cron 沙箱环境中调用 `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 404，但相同代码在本地终端执行正常。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用；若返回了值但 API 返回 404，则说明是沙箱网络策略限制（无法访问外网）。

**解决方案**：
1. 检查 cron job 配置中是否有 `deliver: 'feishu:chat_id'` 字段
2. 如果有，系统会自动投递，无需手动调用 API
3. 手动调用失败不等于 cron 投递失败，以系统自动投递结果为准

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

```python
import re, subprocess, json

def mmx_search_news(query, max_results=8):
    """专门用于搜索近期新闻的 mmx search 封装"""
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    try:
        data = json.loads(r.stdout)
        results = []
        for item in data.get('organic', [])[:max_results]:
            snippet = re.sub(r'<[^>]+>', '', item.get('snippet', '')).replace('﻿', '').strip()
            results.append({
                'title': item.get('title', ''),
                'link': item.get('link', ''),
                'snippet': snippet[:200],
                'date': item.get('date', '')
            })
        return results
    except Exception as e:
        return [{'error': str(e)}]

# 示例：搜索带日期约束的近期新闻
results = mmx_search_news("原子级制造 2026年5月9日 OR 5月10日", max_results=5)
for r in results:
    print(f"[{r['date']}] {r['title']}")
    print(f"  {r['snippet'][:150]}")
```

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用 `python3 << 'PYEOF'` heredoc 方式执行。虽然沙箱能读取 `~/.hermes/skills/blog-publisher/token.txt`，但 execute_code 的 Python 3.14 沙箱对多行 raw string（尤其是含 `---` 分隔符的 regex 模式）会报 `SyntaxError: unterminated string literal`，导致发布失败。
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用 `python3 << 'PYEOF'` heredoc 方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，换用 terminal 即可解决

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你AI科技新闻助手。请用 mmx search 命令搜索过去24小时内发生的AI科技热点新闻，生成AI今日热点简报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "人工智能 AI大模型 GPT"
```
```bash
mmx search query -t 24h --limit 8 "AI创业 融资 技术突破"
```
```bash
mmx search query -t 24h --limit 8 "自动驾驶 机器人 智能硬件"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【技术进展】【行业动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
🤖 AI晨报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【技术进展】
（3-5条，每条：标题 + 摘要）

【行业动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（综合管理-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "AI晨报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 2, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

**任务完成摘要**

**简报生成**：基于 mmx 搜索（2026年5月21日数据），生成755字AI晨报，包含：
- 【今日要闻】4条：北京亦庄AI大会、Anthropic算力合作、华为算力规模、OpenAI数学猜想
- 【技术进展】4条：理想同学×DeepSeek、腾讯会议AI同传、特斯拉机器人产线、智能体AGI路径
- 【行业动态】3条：特斯拉FSD入华、禾赛×奔驰、小鹏Robotaxi
- 【今日关注】特斯拉战略变阵与FSD入华的双重意义

**发布结果**：
- ✅ Koala博客（综合管理-专业知识）：发布成功
- ✅ 飞书（oc_1e92781cdf15f71fad7dcd8d5d386e76）：投递成功

---


## 来源任务
- job_id: c620d75134bb | 任务名: xhs-vangogh-r4
---
# Cron Job: xhs-vangogh-r4

**Job ID:** c620d75134bb
**Run Time:** 2026-05-22 07:04:53
**Schedule:** 0 7 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_vangogh_gen.py 4

每天自动运行，梵高融合风图文：10张原创插画 + 专属情绪文案 + 小红书自动发布。

## Response

✅ **小红书梵高融合风图文日更完成**

**主题**: 草原星空
**生成**: 10张原创插画（梵高厚笔触 + 星空/草原意象）
**文案亮点**: "星星那么近，好像伸手就能够到。"
**发布状态**: 已自动发布至小红书

---


## 来源任务
- job_id: dd15667811fc | 任务名: 财经新闻推送 08:30
---
# Cron Job: 财经新闻推送 08:30

**Job ID:** dd15667811fc
**Run Time:** 2026-05-22 09:11:33
**Schedule:** 10 9 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## Python heredoc 在 cron/execute_code 中执行 mmx 的注意事项

**问题**：在 cron 定时任务的 `execute_code` 沙箱中，用 heredoc `python3 << 'PYEOF'` 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: unterminated string literal`。

**解决方案**：将 Python 脚本先写入 `/tmp/` 文件，再执行：
```bash
# ❌ 会失败：heredoc 中跨行 regex 字符串报错
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)  # SyntaxError!
PYEOF

# ✅ 正确：先写文件再执行
cat > /tmp/publish.py << 'PYEOF'
import re
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF
python3 /tmp/publish.py
```

## Feishu API 在 cron 沙箱中返回 404 的处理

**问题**：在 cron 沙箱环境中调用 `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 404，但相同代码在本地终端执行正常。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用；若返回了值但 API 返回 404，则说明是沙箱网络策略限制（无法访问外网）。

**解决方案**：
1. 检查 cron job 配置中是否有 `deliver: 'feishu:chat_id'` 字段
2. 如果有，系统会自动投递，无需手动调用 API
3. 手动调用失败不等于 cron 投递失败，以系统自动投递结果为准

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

```python
import re, subprocess, json

def mmx_search_news(query, max_results=8):
    """专门用于搜索近期新闻的 mmx search 封装"""
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    try:
        data = json.loads(r.stdout)
        results = []
        for item in data.get('organic', [])[:max_results]:
            snippet = re.sub(r'<[^>]+>', '', item.get('snippet', '')).replace('﻿', '').strip()
            results.append({
                'title': item.get('title', ''),
                'link': item.get('link', ''),
                'snippet': snippet[:200],
                'date': item.get('date', '')
            })
        return results
    except Exception as e:
        return [{'error': str(e)}]

# 示例：搜索带日期约束的近期新闻
results = mmx_search_news("原子级制造 2026年5月9日 OR 5月10日", max_results=5)
for r in results:
    print(f"[{r['date']}] {r['title']}")
    print(f"  {r['snippet'][:150]}")
```

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用 `python3 << 'PYEOF'` heredoc 方式执行。虽然沙箱能读取 `~/.hermes/skills/blog-publisher/token.txt`，但 execute_code 的 Python 3.14 沙箱对多行 raw string（尤其是含 `---` 分隔符的 regex 模式）会报 `SyntaxError: unterminated string literal`，导致发布失败。
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用 `python3 << 'PYEOF'` heredoc 方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，换用 terminal 即可解决

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你财经新闻助手。请用 mmx search 命令搜索过去24小时内发生的财经新闻，生成财经今日热点简报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "宏观经济 货币政策 金融市场"
```
```bash
mmx search query -t 24h --limit 8 "A股 上市公司 财报"
```
```bash
mmx search query -t 24h --limit 8 "房地产 股市 基金"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【市场动态】【行业观察】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
📈 财经晨报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【市场动态】
（3-5条，每条：标题 + 摘要）

【行业观察】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（综合管理-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "财经晨报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 2, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

📈 **财经晨报 | 20260522**

---

**【今日要闻】**

1. **沃什正式出任美联储主席**：特朗普提名沃什接替鲍威尔，5月13日获确认，任期四年。沃什长期主张缩表同时降息，市场预期货币政策宽松节奏有望加快。

2. **韩国7月启动24小时外汇交易**：韩国宣布从7月6日起首次启动美元-韩元即期外汇24小时交易市场，6月29日开始试运行，标志着韩国外汇市场化改革迈出关键一步。

3. **霍尔木兹海峡局势升温**：伊朗提议对海峡过往船只征收永久通行费遭美方反对，美军实施封锁导致94艘商船改道。俄罗斯能源持续向欧洲输送，油价承压回落至97-102美元区间。

4. **黄金价格大幅回调**：国际现货黄金跌至4540美元/盎司附近，上海金报985元/克，较4月高点每克回落超百元。同期钯金强势反弹突破1400美元/盎司，日涨1.69%。

---

**【市场动态】**

1. **A股回购潮持续**：5月22日21家公司披露回购进展，涵盖股东提议、预案披露、股东大会通过及实施完毕全流程，回购热情处于历史高位。

2. **湖南裕能筹划赴港上市**：磷酸铁锂龙头2025年营收346亿元同比增53%，净利润12.77亿元同比翻倍，高端产品销量占比升至51%，出海步伐加速。

3. **同程旅行Q1净利增16.5%**：一季度营收50.06亿元同比增长14.4%，经调整EBITDA达13.89亿元同比增19.8%，旅游消费复苏态势延续。

4. **联想集团全年净利超预期**：2025财年净利润19.1亿美元超市场预估的16.9亿美元，Q4净利润5.21亿美元大超预估的3.19亿美元。

5. **广汽集团2025年亏损87.84亿**：受行业竞争加剧影响，广汽集团2025年归母净亏损87.84亿元，母公司未分配利润554亿元，本年度不实施利润分配。

---

**【行业观察】**

1. **沪深ETF规模重返5.1万亿**：4月末全市场ETF总规模重返5.1万亿元上方，机构投资者借道ETF入市的趋势持续强化。

2. **施罗德基金拟转马路博迈**：施罗德旗下三只公募基金申请变更管理人至路博迈，外资公募管理人调整案例持续增加。

3. **京东方澄清与英伟达合作传闻**：针对媒体推测，京东方明确表示与英伟达暂未开展业务合作，玻璃基封装载板业务2-3年内难以贡献显著业绩。

4. **全国统一大市场建设推进**：国务院常务会议研究推进统一大市场建设，进一步完善产权保护、市场准入、公平竞争等制度。

---

**【今日关注】**

大宗商品市场再现"油涨金跌"的罕见分化格局。黄金在前期大涨后技术性回调，而地缘风险溢价则继续支撑原油高位震荡。往后看，美联储货币政策转向信号趋于明确，实际利率若持续回升将对黄金中期走势形成压制；而原油在霍尔木兹海峡局势不确定背景下下方支撑依然较强。投资者需关注下周美联储官员讲话及欧元区5月PMI数据，以研判大类资产下一步方向。

---

✅ **Koala博客** 已发布至「综合管理-专业知识」  
✅ **飞书** 已发送至 oc_1e92781cdf15f71fad7dcd8d5d386e76

---


## 来源任务
- job_id: 11859c122c65 | 任务名: MLCC产业链晨报 10:15
---
# Cron Job: MLCC产业链晨报 10:15

**Job ID:** 11859c122c65
**Run Time:** 2026-05-22 10:03:06
**Schedule:** 0 10 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## Python heredoc 在 cron/execute_code 中执行 mmx 的注意事项

**问题**：在 cron 定时任务的 `execute_code` 沙箱中，用 heredoc `python3 << 'PYEOF'` 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: unterminated string literal`。

**解决方案**：将 Python 脚本先写入 `/tmp/` 文件，再执行：
```bash
# ❌ 会失败：heredoc 中跨行 regex 字符串报错
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)  # SyntaxError!
PYEOF

# ✅ 正确：先写文件再执行
cat > /tmp/publish.py << 'PYEOF'
import re
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF
python3 /tmp/publish.py
```

## Feishu API 在 cron 沙箱中返回 404 的处理

**问题**：在 cron 沙箱环境中调用 `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 404，但相同代码在本地终端执行正常。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用；若返回了值但 API 返回 404，则说明是沙箱网络策略限制（无法访问外网）。

**解决方案**：
1. 检查 cron job 配置中是否有 `deliver: 'feishu:chat_id'` 字段
2. 如果有，系统会自动投递，无需手动调用 API
3. 手动调用失败不等于 cron 投递失败，以系统自动投递结果为准

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

```python
import re, subprocess, json

def mmx_search_news(query, max_results=8):
    """专门用于搜索近期新闻的 mmx search 封装"""
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    try:
        data = json.loads(r.stdout)
        results = []
        for item in data.get('organic', [])[:max_results]:
            snippet = re.sub(r'<[^>]+>', '', item.get('snippet', '')).replace('﻿', '').strip()
            results.append({
                'title': item.get('title', ''),
                'link': item.get('link', ''),
                'snippet': snippet[:200],
                'date': item.get('date', '')
            })
        return results
    except Exception as e:
        return [{'error': str(e)}]

# 示例：搜索带日期约束的近期新闻
results = mmx_search_news("原子级制造 2026年5月9日 OR 5月10日", max_results=5)
for r in results:
    print(f"[{r['date']}] {r['title']}")
    print(f"  {r['snippet'][:150]}")
```

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用 `python3 << 'PYEOF'` heredoc 方式执行。虽然沙箱能读取 `~/.hermes/skills/blog-publisher/token.txt`，但 execute_code 的 Python 3.14 沙箱对多行 raw string（尤其是含 `---` 分隔符的 regex 模式）会报 `SyntaxError: unterminated string literal`，导致发布失败。
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用 `python3 << 'PYEOF'` heredoc 方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，换用 terminal 即可解决

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是MLCC产业链研究助手。请用 mmx search 命令搜索过去24小时内发生的MLCC/被动元器件产业动态，生成MLCC产业晨报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "MLCC 被动元件 村田 太阳诱电"
```
```bash
mmx search query -t 24h --limit 8 "MLCC产能 扩产 价格走势"
```
```bash
mmx search query -t 24h --limit 8 "消费电子 汽车电子 MLCC需求"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【产能动态】【市场观察】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
🔋 MLCC产业晨报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【产能动态】
（3-5条，每条：标题 + 摘要）

【市场观察】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "MLCC产业链晨报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 1, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

🔋 MLCC产业晨报 | 20260522

【今日要闻】

1. MLCC概念股再度爆发，风华高科涨停
昀冢科技、三环集团涨超10%，风华高科反包涨停，博迁新材、国瓷材料、宏明电子跟涨。英伟达VR200NVL72平台将使用约60万个MLCC，较GB300提升30%以上。

2. 太阳诱电全面停接消费类MLCC新订单
日系大厂太阳诱电通知客户全面停接消费类电容件新单，全系列交期延长，日系被动件原厂交货周期普遍拉长至16周以上。

3. 村田时隔三年首次大规模涨价
2026年3月村田制作所发布涨价函，AI服务器专用高容MLCC、高端车规级、射频微波MLCC全面提价15%-35%，新价格于3月底执行。

4. 三环集团突发火灾事件触动市场神经
国内MLCC龙头三环集团突发大火，官方通报称不影响正常生产经营，但引发业内对被动元件行情的高度关注。

【产能动态】

1. 日系8大厂设备投资额突破1兆日元
抢攻5G、EV市场，日本电子零件8大厂2021年度设备投资额超1兆日元，较上年度增加约3成，太阳诱电拟增产MLCC约15%。

2. 高端MLCC供需持续偏紧
AI服务器和汽车电子对高容、高压、宽温、小型化MLCC需求激增，部分型号交货期延长并出现供应短缺，各厂积极扩充高附加值产能。

3. 国产替代窗口打开
海外龙头村田、太阳诱电持续向AI服务器、车规等高价值产品倾斜产能，常规消费类产能受到挤占，为国产MLCC厂商提供替代机会。

4. 太阳诱电4月率先调涨低容MLCC价格6%-13%
2026年4月太阳诱电率先将消费级与车用低容MLCC提价6%-13%，随后三星电机积极研议跟进调涨代理商定价。

【市场观察】

1. AI服务器MLCC用量激增30%，高端产品量价齐升
英伟达VR200NVL72单机MLCC用量约60万个，中信证券预计服务器功率提升、垂直供电、800V升级将驱动MLCC迎量价齐升机遇。

2. 消费电子需求疲软，与AI赛道形成两极分化
2026年Q2 MLCC市场明显分化：AI相关需求强劲，消费电子（手机、笔电、车用）拉货动能疲弱，以消费规格为主的厂商运营承压。

3. 渠道囤购升温，价格反转预期升温
TrendForce集邦咨询调查显示，消费类渠道囤购升温，部分ODM已于5月上旬完成Q3议价，供应链逐步企稳，Q2市场呈现"AI热、消费冷"格局。

4. 轻薄智能眼镜异军突起，每台需150-200颗微型MLCC
AI轻量化终端快速迭代，智能眼镜大量导入01005尺寸微型MLCC（0.4x0.2mm），成为MLCC市场新增长点。

【今日关注】

英伟达VR200推动AI服务器单机MLCC用量突破60万颗，较上一代平台跳升30%以上，这一数据深刻揭示了AI算力升级对被动元件的颠覆性需求。村田、太阳诱电、三星电机等全球龙头已率先涨价或停止接单，指向高附加价值MLCC正式进入量价齐升的新周期。下半年随着云端ASIC项目自第三季末起放量，叠加高端产能向AI赛道倾斜造成的消费类供给收缩，MLCC价格有望从当前盘整格局转向温和上行，国产厂商迎加速替代窗口。

---

✅ **Koala博客发布成功**（市场销售-专业知识，forum_id=1，group_ids=[12]）
✅ **飞书发送成功**（oc_1e92781cdf15f71fad7dcd8d5d386e76）

---


## 来源任务
- job_id: acf7034225ef | 任务名: xhs-vangogh-r1
---
# Cron Job: xhs-vangogh-r1

**Job ID:** acf7034225ef
**Run Time:** 2026-05-22 10:13:51
**Schedule:** 0 10 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_vangogh_gen.py 1

每天自动运行，梵高融合风图文：10张原创插画 + 专属情绪文案 + BGM + 慢速TTS，产出发飞书。

## Response

**✅ 梵高融合风图文日更完成**

📅 **日期**: 2026-05-22
🎨 **题材**: 秋日银杏
✍️ **文案**: 银杏金黄的时候，秋天就变得温柔了。

**生成详情**:

| # | 场景 | 文件 | 大小 |
|---|------|------|------|
| 1 | 银杏大道，金色叶片铺满地面 | r101_001.jpg | 575KB |
| 2 | 一阵风吹过，金叶如雪旋转落下 | r102_001.jpg | 295KB |
| 3 | 银杏树下，老人在下棋喝茶 | r103_001.jpg | 509KB |
| 4 | 银杏叶落在水面上，像小船 | r104_001.jpg | 222KB |
| 5 | 寺庙的银杏树，千年古树金黄 | r105_001.jpg | 528KB |
| 6 | 银杏果挂在枝头，圆润饱满 | r106_001.jpg | 302KB |
| 7 | 孩子们在银杏叶堆里玩耍 | r107_001.jpg | 431KB |
| 8 | 夕阳透过银杏叶，光芒万丈 | r108_001.jpg | 316KB |
| 9 | 银杏叶落在石板路上，点点金 | r109_001.jpg | 373KB |
| 10 | 银杏扇形叶片，脉络清晰美丽 | r110_001.jpg | 280KB |

📍 **存储路径**: `/tmp/xhs_vangogh/round1/`
🐦 **小红书**: 已发布 (`银杏金黄的时候，秋天就变得温柔了。`)
📨 **飞书**: 图片已逐一上传发送至会话

---


## 来源任务
- job_id: 1015a0407aca | 任务名: 原子级制造科研晨报 10:10
---
# Cron Job: 原子级制造科研晨报 10:10

**Job ID:** 1015a0407aca
**Run Time:** 2026-05-22 10:17:20
**Schedule:** 10 10 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## Python heredoc 在 cron/execute_code 中执行 mmx 的注意事项

**问题**：在 cron 定时任务的 `execute_code` 沙箱中，用 heredoc `python3 << 'PYEOF'` 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: unterminated string literal`。

**解决方案**：将 Python 脚本先写入 `/tmp/` 文件，再执行：
```bash
# ❌ 会失败：heredoc 中跨行 regex 字符串报错
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)  # SyntaxError!
PYEOF

# ✅ 正确：先写文件再执行
cat > /tmp/publish.py << 'PYEOF'
import re
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF
python3 /tmp/publish.py
```

## Feishu API 在 cron 沙箱中返回 404 的处理

**问题**：在 cron 沙箱环境中调用 `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 404，但相同代码在本地终端执行正常。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用；若返回了值但 API 返回 404，则说明是沙箱网络策略限制（无法访问外网）。

**解决方案**：
1. 检查 cron job 配置中是否有 `deliver: 'feishu:chat_id'` 字段
2. 如果有，系统会自动投递，无需手动调用 API
3. 手动调用失败不等于 cron 投递失败，以系统自动投递结果为准

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

```python
import re, subprocess, json

def mmx_search_news(query, max_results=8):
    """专门用于搜索近期新闻的 mmx search 封装"""
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    try:
        data = json.loads(r.stdout)
        results = []
        for item in data.get('organic', [])[:max_results]:
            snippet = re.sub(r'<[^>]+>', '', item.get('snippet', '')).replace('﻿', '').strip()
            results.append({
                'title': item.get('title', ''),
                'link': item.get('link', ''),
                'snippet': snippet[:200],
                'date': item.get('date', '')
            })
        return results
    except Exception as e:
        return [{'error': str(e)}]

# 示例：搜索带日期约束的近期新闻
results = mmx_search_news("原子级制造 2026年5月9日 OR 5月10日", max_results=5)
for r in results:
    print(f"[{r['date']}] {r['title']}")
    print(f"  {r['snippet'][:150]}")
```

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

[SYSTEM: The user has invoked the "wiki-platform-ops" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: wiki-platform-ops
description: 维基平台运维手册——进程管理、ngrok公网访问、OpenClaw/OpenPro绑定问题、Git Push凭证
---

# Wiki 平台运维手册

## 环境信息
- 项目路径：`/Volumes/256G/gesila_wiki/`
- Flask 端口：5001
- 启动命令：`cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py`
- GitHub 仓库：`https://github.com/gesilajerry/gesila_wiki.git`
- 分支：main

## 公网访问
- ngrok URL：`https://unbribing-undeep-maximina.ngrok-free.dev`
- ngrok token：`***NGROK_TOKEN_MASKED***`

### ngrok 配置（macOS native）
**配置文件**：`~/Library/Application Support/ngrok/ngrok.yml`

```yaml
version: "3"
agent:
  authtoken: ***NGROK_TOKEN_MASKED***
tunnels:
  wiki:
    proto: http
    addr: 5001
  gateway:
    proto: http
    addr: 18789
```

**启动指定隧道**：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <tunnel-name>`

**常见错误**：错误启动了 `gateway` tunnel（指向 18789/Hermes）而非 `wiki` tunnel（指向 5001/维基）。检查：
```bash
ps aux | grep ngrok | grep -v grep  # 看在跑哪个 tunnel
curl -s http://localhost:4040/api/tunnels  # ngrok web UI API，看实际 addr 是什么
```

### ngrok API（管理隧道）
```bash
# 查看当前 tunnels（包括 name、public_url、实际指向的 addr）
curl -s http://localhost:4040/api/tunnels | python3 -c "import sys,json; [print(t['name'], t['public_url'], '->', t['config']['addr']) for t in json.load(sys.stdin).get('tunnels',[])]"

# 删除某个 tunnel
curl -X DELETE http://localhost:4040/api/tunnels/<name>

# 创建新 tunnel（POST body 必须含 proto + addr，name 可选）
curl -X POST http://localhost:4040/api/tunnels \
  -H 'Content-Type: application/json' \
  -d '{"proto":"http","addr":"5001"}'
```

**故障排查流程：**
1. `ps aux | grep ngrok | grep -v grep` — 看在跑哪个 tunnel
2. `curl http://localhost:4040/api/tunnels` — 验证实际 addr 是否正确（wiki=5001, gateway=18789）
3. 若 tunnel 指向错误地址：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <正确tunnel名>`

### 常见错误：wiki tunnel 指向 gateway 的 18789
**症状**：wiki 的 ngrok URL 能通，但打开是 OpenClaw/Hermes 的页面，而非维基页面。
**原因**：启动了 `gateway` tunnel（18789）而非 `wiki` tunnel（5001）。
**修复**：`kill <ngrok_pid>` 后 `cd ~/Library/Application\ Support/ngrok && ./ngrok start wiki`

## OpenClaw/OpenPro 绑定问题
**症状**：ngrok URL 被 OpenClaw/OpenPro 拦截，跳转到 OpenPro 页面而不是 wiki。

**原因**：ngrok URL 同时绑定了 Wiki 项目和 OpenPro，OpenClaw/OpenPro 优先拦截。

**解决方法**：在 OpenClaw/OpenPro 控制台解除 URL 与 OpenPro 的绑定，只保留 Wiki 项目绑定。

## GitHub 推送凭证（必须）

**当前有效 Token**：`ghp_***MASKED***`（GitHub Classic，40位）

**⚠️ GitHub Classic Token 说明**
- 必须是 Classic Token，不是新版 Fine-grained PAT
- 长度固定 40 位
- 设置路径：GitHub → Settings → Developer settings → Personal access tokens → Classic
- 权限需要 `repo` 全权限

**第一步**：创建 `~/.netrc`（权限必须 600）：
```
machine github.com
login gesilajerry
password ghp_***MASKED***
```

**第二步**：关闭 SSL 验证（必须）：
```bash
git config --global http.sslVerify false
```
原因：macOS 上的 HTTPS 代理做 SSL interception，导致 `git pull/push` 报错 `LibreSSL SSL_connect: SSL`。关闭 SSL 验证绕过代理的证书拦截。

**第三步**：推送：
```bash
cd /Volumes/256G/gesila_wiki
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**❌ 错误做法**：把 token 嵌入 git remote URL（`https://user:token@github.com/`）——token 会出现在进程列表和日志中，不安全。

**✅ 正确做法**：统一用 netrc + credential.helper，remote URL 保持干净。

### Git 双向同步脚本

**脚本路径**：`/Users/mac/.hermes/scripts/git_sync_wiki.py`

**cron job ID**：`61c28cf385bb`
**调度时间**：`0 23 * * *`（每天 23:00）

**用途**：另一个 agent（Trae）往 GitHub 推送后，本地每日 23:00 拉取同步。

**手动触发**：
```bash
/usr/bin/python3 /Users/mac/.hermes/scripts/git_sync_wiki.py
```

**已知踩坑**：
- `git remote URL` 里嵌 token → 进程列表暴露，风险
- netrc 权限不是 600 → git 不读取
- Token 用了旧版（<40位）→ 认证失败（GitHub 已不支持密码认证，必须用 Classic PAT）
- cron prompt 里写明文 token → 触发安全扫描拦截，改用 Python 脚本读取 token

## 目录结构（不动任何文件夹）
```
gesila_wiki/
  01_Inbox/日志/
  02_Knowledge/概念卡/、方法论/、项目复盘/
  03_Output/已发布/、汇报材料/
```
只往现有目录存文件，框架自动识别。

## OpenClaw Gateway 运维

### 症状（版本 2026.4.24 及之前）
- `openclaw status` / `openclaw update check` 等命令全部超时（30s+）
- `curl http://localhost:18789` 返回 connection refused
- `ps aux | grep openclaw` 显示 gateway 进程在跑但 0% CPU
- 日志 `~/.openclaw/logs/gateway.log` 最后一条是 `"starting channels and sidecars..."` 然后停住
- 日志不写，但 `kill -USR2 <pid>` 可强制刷新日志缓冲区

### 根因（版本 2026.4.24 及之前）
OpenClaw gateway 启动时每次都重新安装所有插件的 bundled runtime deps（npm 包），共 66 个包，在 `~/.openclaw/plugin-runtime-deps/` 目录下。
- 正常需 8-12 分钟
- npm ENOTEMPTY 锁冲突时可达 30 分钟+
- 日志停在 "starting..." 是因为在等插件安装完成，并非真卡住
- 日志文件 600MB+ 会导致日志轮转出问题

### 根因（版本 2026.4.25 — 2026.4.26）
**browser 插件的 CDP 初始化会卡死 gateway**（0% CPU，端口监听但不响应 HTTP）。
- 进程 PID 存在，端口 18789 监听，但所有 HTTP 请求超时
- 日志不写入（或写入极慢）
- Dev 模式（`OPENCLAW_PROFILE=dev`）因 `skipBootstrap: true` 能起来

**修复**：在 `openclaw.json` 中关闭 browser 插件：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['plugins']['entries']['browser'] = {'enabled': False}
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2)
```

### 根因（版本 2026.4.27+）— 升级后完全无法启动
**升级到 2026.4.27 后 gateway 卡在 "staging bundled runtime deps" 完全无响应。**

1. **plugin-runtime-deps 安装不完整**：同时存在 4 个版本目录（2026.4.24, 2026.4.25, 2026.4.27, unknown），版本之间文件互相引用导致缺失文件（如 `chutes-oauth-CUO6Kvd3.js`）
2. **npm 网络不通**：gateway 尝试安装 8 个缺失包（@agentclientprotocol/claude-agent-acp@0.31.0, @grammyjs/*, @homebridge/ciao@^1.3.6, acpx@0.6.1, chokidar@^5.0.0, grammy@^1.42.0 等），npm registry 访问超时后一直重试
3. **所有 openclaw CLI 命令全部超时**：`doctor`, `gateway stop`, `dashboard` 等都卡死
4. **launchctl 重启后 gateway 进程存在但不监听端口**：旧进程没真正被杀掉（launchd 管理混乱）

**Gateway token 位置**：`openclaw.json` 里存的是 `"***"`（生成后存在 `~/.openclaw/identity/device-auth.json` 的 `tokens.operator.token` 字段），该 token 是 device-to-device 认证，不用于 HTTP API。

**降级到 2026.4.25 方案**：
```bash
# 杀掉所有 gateway 进程
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 卸载 2026.4.27
rm -rf ~/.npm-global/lib/node_modules/openclaw

# 指定版本安装（--registry 避免网络问题）
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
```

**验证版本**：`openclaw --version` 和 `cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"`

### 诊断步骤
```bash
# 1. 查看 openclaw 版本
openclaw --version

# 2. 看进程状态
ps aux | grep "openclaw" | grep -v grep

# 3. 检查端口监听
curl -s --connect-timeout 5 "http://localhost:18789/health"

# 4. 检查 npm 包完整性
ls ~/.npm-global/lib/node_modules/openclaw/dist/index.js
cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"

# 5. 查看 plugin-runtime-deps 版本
ls ~/.openclaw/plugin-runtime-deps/

# 6. 检查 npm 锁冲突
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete

# 7. 强制刷新日志
kill -USR2 <pid>
tail -20 ~/.openclaw/logs/gateway.log

# 8. 查看 /tmp/openclaw/ 下的实时日志（JSON 格式）
tail -f /tmp/openclaw/openclaw-$(date +%Y-%m-%d).log
```

### 修复方案

**方案 A：清理后重启（推荐用于 2026.4.24 及之前版本）**
```bash
# 清空错误日志（保留文件避免权限问题）
truncate -s 0 ~/.openclaw/logs/gateway.err.log

# 删除 npm 缓存锁
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete
find ~/.openclaw/plugin-runtime-deps/ -name ".openclaw-npm-cache" -type d -exec rm -rf {} + 2>/dev/null

# 杀掉旧进程（注意：2026.4.27 版本进程杀掉后还会重启，需先卸载）
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：降级回 2026.4.25（2026.4.27 专用）**
```bash
# 杀掉所有进程
pkill -9 -f "openclaw" 2>/dev/null
rm -rf ~/.npm-global/lib/node_modules/openclaw
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：跳过飞书 channel 加速启动**
飞书 bot probe 超时（axios 30s × 4 accounts = 2 分钟延迟）：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['channels']['feishu']['enabled'] = False
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2, ensure_ascii=False)
```
重启后 gateway 约 3 分钟可 ready（vs 10 分钟带飞书）。

**方案 C：手动触发 npm 安装（减少重复安装）**
让 gateway 跑完一次后，下次重启会复用缓存。但 npm ENOTEMPTY 问题会反复出现，每次都要清锁。

### launchd 管理
```bash
# 查看状态
launchctl list | grep openclaw

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist

# 日志路径
~/.openclaw/logs/gateway.log（主日志）
~/.openclaw/logs/gateway.err.log（错误日志）
/tmp/openclaw/openclaw-YYYY-MM-DD.log（实时 JSON 日志）
```

### OpenClaw vs Hermes 定时任务关系
- OpenClaw gateway 内置 cron 调度（job_id 如 `7c55dfb106c3`）
- Hermes 有独立的 cron 系统（`~/.hermes/cron/jobs.json`）
- 两者可以并存；迁移时暂停 OpenClaw 任务，避免重复执行：
  `cronjob pause <job_id>`

---

## 性能优化（app.py）

**问题**：158+ 文件时，每次请求全量读文件，首页/图谱页慢到无法操作。

**优化方案**（app.py 已实现）：
1. 启动时预热：`app/__init__` 时 `_load_cards()` + `_build_graph()` 预加载，197张卡约0.1秒
2. 文件 mtime 检测：`_scan_mtimes()` 追踪文件变更，TTL 从4小时改为文件驱动刷新
3. 后台异步重建：缓存过期时返回旧数据同时后台线程重建，不阻塞请求
4. 图数据独立缓存：`build_graph()` 结果存入 `_cache['graph']`

**重启生效**：`kill <PID> && python3 app.py`

---

## 结构层提炼任务（定时 cron 执行）

每晚21:30自动执行，从当日归档日志中提炼知识卡片。

### Step 1: 确定日期
当天日期（如：`2026-05-11`）

### Step 2: 读取归档日志
```
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_会话复盘.md
```

**⚠️ 解析要点：推送日报文件超大（~460KB）**
- 文件过大是因为每个任务块的 Prompt 都完整嵌入了调用的 SKILL.md 内容（整个技能文件）
- 实际 AI 生成的 Response 内容才是需要提取的知识素材
- 搜索 AI 简报内容时用 emoji 标记（如 `"🤖 AI热点简报"`）或 Response 标记来定位
- 各行业简报（MLCC/原子级制造/半导体/财经）的内容在 Response 段落，篇幅通常 2-20KB

### Step 3: 识别值得提炼的内容
识别 1-5 个值得结构化的核心概念或方法论，优先关注：
- 小红书运营经验
- 飞书 API 使用技巧
- Cron 任务管理
- 内容创作方法论
- 产业链洞察

### Step 4: 写入知识卡片
```
/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_名称.md
/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_名称.md
```

格式：
```
# {名称}

**背景**
**核心洞察**
**对Jerry的意义**
**延伸思考**

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
用 tenant_access_token 方式发送飞书 post 消息到 `oc_1e92781cdf15f71fad7dcd8d5d386e76`，报告提炼了几张卡片及标题。

凭证：
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: ***FEISHU_SECRET_MASKED***

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是原子级制造产业与科研动态研究助手。请用 mmx search 命令搜索过去24小时内发生的原子级制造相关动态，生成原子级制造科研晨报，完成后同步发布到飞书和Koala博客。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "原子级制造 团簇离子束 纳米材料"
```
```bash
mmx search query -t 24h --limit 8 "原子级制造 量子点 薄膜沉积"
```
```bash
mmx search query -t 24h --limit 8 "原子级制造 纳米科技 科研突破"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【科研进展】【产业动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
⚛️ 原子级制造科研晨报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【科研进展】
（3-5条，每条：标题 + 摘要）

【产业动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "原子级制造科研晨报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 4, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

⚛️ **原子级制造科研晨报 | 20260522** 已完成，今日执行结果如下：

---

**【今日要闻】**

1. **【国内首次】九峰山实验室实现8英寸ALD金属钼工艺突破** — 联合国产ALD设备厂商，以MoCl₅O为前驱体在400℃下完成高性能金属钼薄膜开发，可完美适配3D NAND垂直沟道

2. **星元晶算×清华大学天津高端装备研究院签署战略合作协议** — 围绕人形机器人关节模组氮化镓器件原子级制造工艺开展合作（2026-05-19）

3. **南京市工信局举办原子级制造技术与未来产业专题讲座** — "工信大讲堂"助力南京打造全国影响力未来产业创新高地（2026-05-15）

---

**【科研进展】**

1. **中南大学**：飞秒激光原子-纳米-微米多级制造钙钛矿量子点薄膜（ACS Nano）
2. **南京大学-苏州实验室-东南大学**：联合开发oxy-MOCVD技术，解决二维半导体量产化动力学瓶颈（2026-02-02）
3. **青岛科技大学**：金属纳米团簇合成与催化应用新进展
4. **哈佛大学**：银纳米团簇与有机大环共晶自组装超晶格研究（JARAD A. MASON课题组）

---

**【产业动态】**

1. **国内首颗人形机器人关节氮化镓磁编码芯片发布** — 中科无线半导体实现自主可控（2026-04-13）
2. **2026年ALD设备市场规模持续攀升** — AI芯片需求驱动原子层沉积技术受益
3. **北京发布原子级制造创新发展行动计划（2026-2028）** — 目标：5种软件工具+10项创新装备（2026-02-03）
4. **2025原子级制造产业发展论坛在沪召开** — 产学研协同共识凝聚（2025-12-19）

---

**【今日关注】**

九峰山实验室8英寸ALD金属钼工艺突破与星元晶算-清华氮化镓原子级制造合作，形成**一横一纵**的产业突破态势：前者代表原子级沉积在化合物半导体核心材料上的国产化深度，后者代表氮化镓器件与人形机器人关节模组的跨界融合广度。两条路线共同指向一个核心命题——**原子级制造正从"实验室可行"迈向"量产可控"，2026年有望成为原子级制造从科研验证走向规模化产业应用的关键元年**。

---

**📤 发布结果**

- ✅ **Koala博客**：`forum_id=4`（研发工程-专业知识），文章UUID: `x14icPcuGnj7hI1w`
- ✅ **飞书**：已发送至 `oc_1e92781cdf15f71fad7dcd8d5d386e76`，消息ID: `om_x100b6fcf87379ca0c37f3e43ecc13c0`

---


## 来源任务
- job_id: 6ce527bbbff4 | 任务名: 半导体热点晨报 11:15
---
# Cron Job: 半导体热点晨报 11:15

**Job ID:** 6ce527bbbff4
**Run Time:** 2026-05-22 11:03:18
**Schedule:** 0 11 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## Python heredoc 在 cron/execute_code 中执行 mmx 的注意事项

**问题**：在 cron 定时任务的 `execute_code` 沙箱中，用 heredoc `python3 << 'PYEOF'` 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: unterminated string literal`。

**解决方案**：将 Python 脚本先写入 `/tmp/` 文件，再执行：
```bash
# ❌ 会失败：heredoc 中跨行 regex 字符串报错
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)  # SyntaxError!
PYEOF

# ✅ 正确：先写文件再执行
cat > /tmp/publish.py << 'PYEOF'
import re
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF
python3 /tmp/publish.py
```

## Feishu API 在 cron 沙箱中返回 404 的处理

**问题**：在 cron 沙箱环境中调用 `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 404，但相同代码在本地终端执行正常。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用；若返回了值但 API 返回 404，则说明是沙箱网络策略限制（无法访问外网）。

**解决方案**：
1. 检查 cron job 配置中是否有 `deliver: 'feishu:chat_id'` 字段
2. 如果有，系统会自动投递，无需手动调用 API
3. 手动调用失败不等于 cron 投递失败，以系统自动投递结果为准

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

```python
import re, subprocess, json

def mmx_search_news(query, max_results=8):
    """专门用于搜索近期新闻的 mmx search 封装"""
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    try:
        data = json.loads(r.stdout)
        results = []
        for item in data.get('organic', [])[:max_results]:
            snippet = re.sub(r'<[^>]+>', '', item.get('snippet', '')).replace('﻿', '').strip()
            results.append({
                'title': item.get('title', ''),
                'link': item.get('link', ''),
                'snippet': snippet[:200],
                'date': item.get('date', '')
            })
        return results
    except Exception as e:
        return [{'error': str(e)}]

# 示例：搜索带日期约束的近期新闻
results = mmx_search_news("原子级制造 2026年5月9日 OR 5月10日", max_results=5)
for r in results:
    print(f"[{r['date']}] {r['title']}")
    print(f"  {r['snippet'][:150]}")
```

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你半导体产业研究助手。请用 mmx search 命令搜索过去24小时内发生的半导体热点新闻，生成半导体产业晨报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "半导体 芯片 美国出口管制"
```
```bash
mmx search query -t 24h --limit 8 "晶圆代工 台积电 中芯国际"
```
```bash
mmx search query -t 24h --limit 8 "半导体国产化 芯片设计 AI芯片"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【技术进展】【市场动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
💡 半导体产业晨报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【技术进展】
（3-5条，每条：标题 + 摘要）

【市场动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "半导体热点晨报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 1, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

半导体产业晨报已完成全部流程：

**✅ Koala博客发布成功** — 发布至「市场销售 > 专业知识」板块（forum_id=1, group_ids=[12]）

**✅ 飞书发送成功** — 已发送至 oc_1e92781cdf15f71fad7dcd8d5d386e76

---

💡 **半导体产业晨报 | 20260522**

【今日要闻】

• **中美元首会谈涉及半导体出口管制，外交部重申原则立场** — 5月15日外交部记者会上，发言人郭嘉昆表示中方已多次表明在美输华芯片问题上的原则立场，重申维护全球产业链供应链稳定。

• **美国应用材料预计2026财年营收再减6亿美元** — 美商务部工业与安全局新规扩大受出口限制企业范围，公司营收进一步承压。美方持续挥舞科技霸权大棒，打压中国发展。

• **美国宣布对芯片征收约100%关税，台积电赴美投资拟上调至2000亿美元** — 华盛顿继20%关税后进一步宣布100%关税，岛内舆论担忧台湾优势产业被掏空。台积电赴美投资同步上调至2000亿美元。

【技术进展】

• **阿里平头哥发布真武M890 AI芯片，性能较810E提升3倍** — 5月20日阿里云峰会亮相，内置144GB显存，片间互联带宽800GB/s，支持FP32至FP4多精度，AI算力国产化加速。

• **台积电加速提升3nm/2nm产能，年底月产能目标大幅上调** — 3nm月产能目标从15万片提升至18万片，2nm年底接近10万片，公司大量投入加速扩张。

• **中芯国际406亿收购中芯北方49%股权** — 中芯北方估值828亿元，创科创板发行股份购买资产规模纪录，强化成熟制程布局。

• **三星开发下一代HBM封装技术，布局移动端AI** — 研发"多层堆叠式FOWLP"技术，实现移动设备高性能设备端AI。

【市场动态】

• **国产晶圆代工开启提价扩产周期，产能利用率接近翻倍** — 5月国产头部代工厂产能利用率较去年Q3接近翻倍，存储/电源管理/MCU需求密集释放，产业景气度攀升。

• **AI芯片国产化率从35%升至46%，集成电路出口金额同比增73%** — 出口占进口金额比由2022年37.1%提升至56.6%，国产替代趋势明显。

• **全球半导体资本开支预计同比增长32%，科创芯片指数强势上涨** — 5月21日科创板芯片指数涨1.76%，中微公司、沪硅产业等涨幅显著。

• **SK海力士Q1营业利润暴增405%，存储巨头抢人战升级** — 营收同比增200%达355亿美元，营业利润率达72%超越台积电和英伟达，约200名三星工程师跳槽海力士。

【今日关注】

AI推理需求爆发正在重构成熟制程晶圆代工格局——国产厂商产能利用率翻倍、价格稳步上行，反映出国内设计企业在国际供应链压力下主动寻求国产替代的强劲动力。平头哥M890芯片发布标志着国内云端AI芯片从"可用"向"好用"跨越的关键一步。但美国出口管制持续加码、全球关税壁垒快速抬升，短期扰动不改长期趋势，国产半导体各环节正处于量价齐升的主动补库周期，结构性机会值得持续深度跟踪。

---


## 来源任务
- job_id: 793ce3c39848 | 任务名: Hermes 结构层提炼 11:30
---
# Cron Job: Hermes 结构层提炼 11:30

**Job ID:** 793ce3c39848
**Run Time:** 2026-05-22 11:34:51
**Schedule:** 30 11 * * *

## Prompt

[SYSTEM: The user has invoked the "wiki-platform-ops" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: wiki-platform-ops
description: 维基平台运维手册——进程管理、ngrok公网访问、OpenClaw/OpenPro绑定问题、Git Push凭证
---

# Wiki 平台运维手册

## 环境信息
- 项目路径：`/Volumes/256G/gesila_wiki/`
- Flask 端口：5001
- 启动命令：`cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py`
- GitHub 仓库：`https://github.com/gesilajerry/gesila_wiki.git`
- 分支：main

## 公网访问
- ngrok URL：`https://unbribing-undeep-maximina.ngrok-free.dev`
- ngrok token：`***NGROK_TOKEN_MASKED***`

### ngrok 配置（macOS native）
**配置文件**：`~/Library/Application Support/ngrok/ngrok.yml`

```yaml
version: "3"
agent:
  authtoken: ***NGROK_TOKEN_MASKED***
tunnels:
  wiki:
    proto: http
    addr: 5001
  gateway:
    proto: http
    addr: 18789
```

**启动指定隧道**：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <tunnel-name>`

**常见错误**：错误启动了 `gateway` tunnel（指向 18789/Hermes）而非 `wiki` tunnel（指向 5001/维基）。检查：
```bash
ps aux | grep ngrok | grep -v grep  # 看在跑哪个 tunnel
curl -s http://localhost:4040/api/tunnels  # ngrok web UI API，看实际 addr 是什么
```

### ngrok API（管理隧道）
```bash
# 查看当前 tunnels（包括 name、public_url、实际指向的 addr）
curl -s http://localhost:4040/api/tunnels | python3 -c "import sys,json; [print(t['name'], t['public_url'], '->', t['config']['addr']) for t in json.load(sys.stdin).get('tunnels',[])]"

# 删除某个 tunnel
curl -X DELETE http://localhost:4040/api/tunnels/<name>

# 创建新 tunnel（POST body 必须含 proto + addr，name 可选）
curl -X POST http://localhost:4040/api/tunnels \
  -H 'Content-Type: application/json' \
  -d '{"proto":"http","addr":"5001"}'
```

**故障排查流程：**
1. `ps aux | grep ngrok | grep -v grep` — 看在跑哪个 tunnel
2. `curl http://localhost:4040/api/tunnels` — 验证实际 addr 是否正确（wiki=5001, gateway=18789）
3. 若 tunnel 指向错误地址：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <正确tunnel名>`

### 常见错误：wiki tunnel 指向 gateway 的 18789
**症状**：wiki 的 ngrok URL 能通，但打开是 OpenClaw/Hermes 的页面，而非维基页面。
**原因**：启动了 `gateway` tunnel（18789）而非 `wiki` tunnel（5001）。
**修复**：`kill <ngrok_pid>` 后 `cd ~/Library/Application\ Support/ngrok && ./ngrok start wiki`

## OpenClaw/OpenPro 绑定问题
**症状**：ngrok URL 被 OpenClaw/OpenPro 拦截，跳转到 OpenPro 页面而不是 wiki。

**原因**：ngrok URL 同时绑定了 Wiki 项目和 OpenPro，OpenClaw/OpenPro 优先拦截。

**解决方法**：在 OpenClaw/OpenPro 控制台解除 URL 与 OpenPro 的绑定，只保留 Wiki 项目绑定。

## GitHub 推送凭证（必须）

**当前有效 Token**：`ghp_***MASKED***`（GitHub Classic，40位）

**⚠️ GitHub Classic Token 说明**
- 必须是 Classic Token，不是新版 Fine-grained PAT
- 长度固定 40 位
- 设置路径：GitHub → Settings → Developer settings → Personal access tokens → Classic
- 权限需要 `repo` 全权限

**第一步**：创建 `~/.netrc`（权限必须 600）：
```
machine github.com
login gesilajerry
password ghp_***MASKED***
```

**第二步**：关闭 SSL 验证（必须）：
```bash
git config --global http.sslVerify false
```
原因：macOS 上的 HTTPS 代理做 SSL interception，导致 `git pull/push` 报错 `LibreSSL SSL_connect: SSL`。关闭 SSL 验证绕过代理的证书拦截。

**第三步**：推送：
```bash
cd /Volumes/256G/gesila_wiki
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**❌ 错误做法**：把 token 嵌入 git remote URL（`https://user:token@github.com/`）——token 会出现在进程列表和日志中，不安全。

**✅ 正确做法**：统一用 netrc + credential.helper，remote URL 保持干净。

### Git 双向同步脚本

**脚本路径**：`/Users/mac/.hermes/scripts/git_sync_wiki.py`

**cron job ID**：`61c28cf385bb`
**调度时间**：`0 23 * * *`（每天 23:00）

**用途**：另一个 agent（Trae）往 GitHub 推送后，本地每日 23:00 拉取同步。

**手动触发**：
```bash
/usr/bin/python3 /Users/mac/.hermes/scripts/git_sync_wiki.py
```

**已知踩坑**：
- `git remote URL` 里嵌 token → 进程列表暴露，风险
- netrc 权限不是 600 → git 不读取
- Token 用了旧版（<40位）→ 认证失败（GitHub 已不支持密码认证，必须用 Classic PAT）
- cron prompt 里写明文 token → 触发安全扫描拦截，改用 Python 脚本读取 token

## 目录结构（不动任何文件夹）
```
gesila_wiki/
  01_Inbox/日志/
  02_Knowledge/概念卡/、方法论/、项目复盘/
  03_Output/已发布/、汇报材料/
```
只往现有目录存文件，框架自动识别。

## OpenClaw Gateway 运维

### 症状（版本 2026.4.24 及之前）
- `openclaw status` / `openclaw update check` 等命令全部超时（30s+）
- `curl http://localhost:18789` 返回 connection refused
- `ps aux | grep openclaw` 显示 gateway 进程在跑但 0% CPU
- 日志 `~/.openclaw/logs/gateway.log` 最后一条是 `"starting channels and sidecars..."` 然后停住
- 日志不写，但 `kill -USR2 <pid>` 可强制刷新日志缓冲区

### 根因（版本 2026.4.24 及之前）
OpenClaw gateway 启动时每次都重新安装所有插件的 bundled runtime deps（npm 包），共 66 个包，在 `~/.openclaw/plugin-runtime-deps/` 目录下。
- 正常需 8-12 分钟
- npm ENOTEMPTY 锁冲突时可达 30 分钟+
- 日志停在 "starting..." 是因为在等插件安装完成，并非真卡住
- 日志文件 600MB+ 会导致日志轮转出问题

### 根因（版本 2026.4.25 — 2026.4.26）
**browser 插件的 CDP 初始化会卡死 gateway**（0% CPU，端口监听但不响应 HTTP）。
- 进程 PID 存在，端口 18789 监听，但所有 HTTP 请求超时
- 日志不写入（或写入极慢）
- Dev 模式（`OPENCLAW_PROFILE=dev`）因 `skipBootstrap: true` 能起来

**修复**：在 `openclaw.json` 中关闭 browser 插件：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['plugins']['entries']['browser'] = {'enabled': False}
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2)
```

### 根因（版本 2026.4.27+）— 升级后完全无法启动
**升级到 2026.4.27 后 gateway 卡在 "staging bundled runtime deps" 完全无响应。**

1. **plugin-runtime-deps 安装不完整**：同时存在 4 个版本目录（2026.4.24, 2026.4.25, 2026.4.27, unknown），版本之间文件互相引用导致缺失文件（如 `chutes-oauth-CUO6Kvd3.js`）
2. **npm 网络不通**：gateway 尝试安装 8 个缺失包（@agentclientprotocol/claude-agent-acp@0.31.0, @grammyjs/*, @homebridge/ciao@^1.3.6, acpx@0.6.1, chokidar@^5.0.0, grammy@^1.42.0 等），npm registry 访问超时后一直重试
3. **所有 openclaw CLI 命令全部超时**：`doctor`, `gateway stop`, `dashboard` 等都卡死
4. **launchctl 重启后 gateway 进程存在但不监听端口**：旧进程没真正被杀掉（launchd 管理混乱）

**Gateway token 位置**：`openclaw.json` 里存的是 `"***"`（生成后存在 `~/.openclaw/identity/device-auth.json` 的 `tokens.operator.token` 字段），该 token 是 device-to-device 认证，不用于 HTTP API。

**降级到 2026.4.25 方案**：
```bash
# 杀掉所有 gateway 进程
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 卸载 2026.4.27
rm -rf ~/.npm-global/lib/node_modules/openclaw

# 指定版本安装（--registry 避免网络问题）
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
```

**验证版本**：`openclaw --version` 和 `cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"`

### 诊断步骤
```bash
# 1. 查看 openclaw 版本
openclaw --version

# 2. 看进程状态
ps aux | grep "openclaw" | grep -v grep

# 3. 检查端口监听
curl -s --connect-timeout 5 "http://localhost:18789/health"

# 4. 检查 npm 包完整性
ls ~/.npm-global/lib/node_modules/openclaw/dist/index.js
cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"

# 5. 查看 plugin-runtime-deps 版本
ls ~/.openclaw/plugin-runtime-deps/

# 6. 检查 npm 锁冲突
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete

# 7. 强制刷新日志
kill -USR2 <pid>
tail -20 ~/.openclaw/logs/gateway.log

# 8. 查看 /tmp/openclaw/ 下的实时日志（JSON 格式）
tail -f /tmp/openclaw/openclaw-$(date +%Y-%m-%d).log
```

### 修复方案

**方案 A：清理后重启（推荐用于 2026.4.24 及之前版本）**
```bash
# 清空错误日志（保留文件避免权限问题）
truncate -s 0 ~/.openclaw/logs/gateway.err.log

# 删除 npm 缓存锁
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete
find ~/.openclaw/plugin-runtime-deps/ -name ".openclaw-npm-cache" -type d -exec rm -rf {} + 2>/dev/null

# 杀掉旧进程（注意：2026.4.27 版本进程杀掉后还会重启，需先卸载）
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：降级回 2026.4.25（2026.4.27 专用）**
```bash
# 杀掉所有进程
pkill -9 -f "openclaw" 2>/dev/null
rm -rf ~/.npm-global/lib/node_modules/openclaw
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：跳过飞书 channel 加速启动**
飞书 bot probe 超时（axios 30s × 4 accounts = 2 分钟延迟）：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['channels']['feishu']['enabled'] = False
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2, ensure_ascii=False)
```
重启后 gateway 约 3 分钟可 ready（vs 10 分钟带飞书）。

**方案 C：手动触发 npm 安装（减少重复安装）**
让 gateway 跑完一次后，下次重启会复用缓存。但 npm ENOTEMPTY 问题会反复出现，每次都要清锁。

### launchd 管理
```bash
# 查看状态
launchctl list | grep openclaw

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist

# 日志路径
~/.openclaw/logs/gateway.log（主日志）
~/.openclaw/logs/gateway.err.log（错误日志）
/tmp/openclaw/openclaw-YYYY-MM-DD.log（实时 JSON 日志）
```

### OpenClaw vs Hermes 定时任务关系
- OpenClaw gateway 内置 cron 调度（job_id 如 `7c55dfb106c3`）
- Hermes 有独立的 cron 系统（`~/.hermes/cron/jobs.json`）
- 两者可以并存；迁移时暂停 OpenClaw 任务，避免重复执行：
  `cronjob pause <job_id>`

---

## 性能优化（app.py）

**问题**：158+ 文件时，每次请求全量读文件，首页/图谱页慢到无法操作。

**优化方案**（app.py 已实现）：
1. 启动时预热：`app/__init__` 时 `_load_cards()` + `_build_graph()` 预加载，197张卡约0.1秒
2. 文件 mtime 检测：`_scan_mtimes()` 追踪文件变更，TTL 从4小时改为文件驱动刷新
3. 后台异步重建：缓存过期时返回旧数据同时后台线程重建，不阻塞请求
4. 图数据独立缓存：`build_graph()` 结果存入 `_cache['graph']`

**重启生效**：`kill <PID> && python3 app.py`

---

## 结构层提炼任务（定时 cron 执行）

每晚21:30自动执行，从当日归档日志中提炼知识卡片。

### Step 1: 确定日期
当天日期（如：`2026-05-11`）

### Step 2: 读取归档日志
```
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_会话复盘.md
```

**⚠️ 解析要点：推送日报文件超大（~460KB）**
- 文件过大是因为每个任务块的 Prompt 都完整嵌入了调用的 SKILL.md 内容（整个技能文件）
- 实际 AI 生成的 Response 内容才是需要提取的知识素材
- 搜索 AI 简报内容时用 emoji 标记（如 `"🤖 AI热点简报"`）或 Response 标记来定位
- 各行业简报（MLCC/原子级制造/半导体/财经）的内容在 Response 段落，篇幅通常 2-20KB

### Step 3: 识别值得提炼的内容
识别 1-5 个值得结构化的核心概念或方法论，优先关注：
- 小红书运营经验
- 飞书 API 使用技巧
- Cron 任务管理
- 内容创作方法论
- 产业链洞察

### Step 4: 写入知识卡片
```
/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_名称.md
/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_名称.md
```

格式：
```
# {名称}

**背景**
**核心洞察**
**对Jerry的意义**
**延伸思考**

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
用 tenant_access_token 方式发送飞书 post 消息到 `oc_1e92781cdf15f71fad7dcd8d5d386e76`，报告提炼了几张卡片及标题。

凭证：
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: ***FEISHU_SECRET_MASKED***

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是 Hermes Agent 的知识管理员。执行结构层提炼任务，从归档日志中提炼结构化知识卡片。

## 凭证
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: ***FEISHU_SECRET_MASKED***
- 飞书会话: oc_1e92781cdf15f71fad7dcd8d5d386e76

## 执行步骤

### Step 1: 确定日期
当天日期（如：2026-04-26）

### Step 2: 读取归档日志
读取 `/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md` 和 `Hermes_YYYY-MM-DD_会话复盘.md`

### Step 3: 识别值得提炼的内容
结合当日 Hermes cron 任务执行内容，识别：
- 1-5个值得结构化的核心概念或方法论
- 有价值的工作流程、问题解决思路
- 可以复用的经验教训

### Step 4: 写入知识卡片

**写入路径：**
- 概念卡：`/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_XXX.md`
- 方法论：`/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_XXX.md`

**卡片格式：**
```
# {概念/方法论名称}

**背景**
{这段知识产生的上下文}

**核心洞察**
{最核心的内容，300-800字}

**对Jerry的意义**
{这个知识对他的价值}

**延伸思考**
{可以应用的其他场景}

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
报告提炼了几张卡片及标题。

### 重要规则
- **禁止 [SILENT]**
- 每张卡片300-800字
- 优先关注：小红书运营经验、飞书 API 使用技巧、Cron 任务管理、内容创作方法论

## Response

## ✅ Hermes 结构层提炼完成 | 2026-05-21

基于归档日志 `Hermes_2026-05-21_推送日报.md`（340KB，20个Cron任务），提炼完成 **5张知识卡片**。

---

### 📗 概念卡①：AI Token套餐——AI计算能力商品化的里程碑
三大运营商推出"Token套餐"，AI算力类比流量经营进入消费市场，标志着AI从B2B基础设施向C端订阅制渗透的里程碑事件。

### 📗 概念卡②：MLCC涨价传导链——AI算力重构被动元件格局
日韩→台系→国产三级传导路径清晰，NVIDIA Rubin平台单板MLCC用量从6500颗翻倍至12000颗，AI驱动从周期紧张变为结构性永久缺口。

### 📗 概念卡③：国产AI芯片化比率突破46%
从"想办法买GPU"到"必须建自己的AI芯片体系"，字节跳动自研5nm AI芯片示范，国产替代临界点已过（超过50%将成市场主流心理关口）。

### 📘 方法论①：mmx搜索"-t 24h"时间过滤踩坑实录
`-t 24h` 时间过滤标志会导致结果乱码（返回歌曲/医疗信息），正确做法是查询词内嵌中文日期约束（如 `"2026年5月21日 纳米制造 突破"`）。

### 📘 方法论②：多源行业简报的结构化提炼框架
三维评估（产业洞察深度/可操作意义/跨领域关联）+ 四段格式（背景/核心洞察/对Jerry意义/延伸思考）+ 标签复用体系，每日≤5张卡片。

---

**Git**: `f25346d` → main ✅  
**Feishu**: 飞书报告已发送至 `oc_1e92781cdf15f71fad7dcd8d5d386e76`（code=0）

---


## 来源任务
- job_id: 1e7dc62ef5dc | 任务名: xhs-vangogh-r5
---
# Cron Job: xhs-vangogh-r5

**Job ID:** 1e7dc62ef5dc
**Run Time:** 2026-05-22 14:04:48
**Schedule:** 0 14 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_vangogh_gen.py 5

每天自动运行，梵高融合风图文：10张原创插画 + 专属情绪文案 + 小红书自动发布。

## Response

🎨 **小红书梵高融合风图文日更流水线 · 第5期**

**主题：传统茶室**

| 图片 | 场景描述 | 状态 |
|------|----------|------|
| 1/10 | 日式茶室，榻榻米上摆着茶具 | ✅ |
| 2/10 | 功夫茶席，紫砂壶冒着热气 | ✅ |
| 3/10 | 茶室窗外的枯山水，光影静谧 | ✅ |
| 4/10 | 茶艺师在注水，茶叶慢慢舒展 | ✅ |
| 5/10 | 老茶馆里，京剧票友在清唱 | ✅ |
| 6/10 | 茶室角落的菖蒲，绿意盎然 | ✅ |
| 7/10 | 茶室中的书法卷轴，笔力遒劲 | ✅ |
| 8/10 | 茶点摆在青瓷碟中，精巧如艺术品 | ✅ |
| 9/10 | 茶室的天井，雨声滴答作响 | ✅ |
| 10/10 | 黄昏茶室，夕阳从窗格斜射 | ✅ |

**已发布至小红书：**
> 一壶茶，喝的是时间的味道。

10张梵高融合风插画已全部生成完毕，流水线执行完成。

---


## 来源任务
- job_id: 7eabbc256ad5 | 任务名: xhs-vangogh-r2
---
# Cron Job: xhs-vangogh-r2

**Job ID:** 7eabbc256ad5
**Run Time:** 2026-05-22 17:34:56
**Schedule:** 30 17 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_vangogh_gen.py 2

每天自动运行，梵高融合风图文：10张原创插画 + 专属情绪文案 + BGM + 慢速TTS，产出发飞书。

## Response

**Van Gogh 融合风图文日更已完成** ✓

**主题**: 温暖书房
**产出声量**: 10 张原创插画 + 文案 + BGM + 慢速 TTS

**配图文案**: `翻开一本书，世界就安静了。`

10 张图全部生成并发布小红书，脚本执行成功（exit_code: 0）。

---


## 来源任务
- job_id: 4414069ce615 | 任务名: 原子级制造科研晚报 19:10
---
# Cron Job: 原子级制造科研晚报 19:10

**Job ID:** 4414069ce615
**Run Time:** 2026-05-22 19:12:29
**Schedule:** 10 19 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## Python heredoc 在 cron/execute_code 中执行 mmx 的注意事项

**问题**：在 cron 定时任务的 `execute_code` 沙箱中，用 heredoc `python3 << 'PYEOF'` 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: unterminated string literal`。

**解决方案**：将 Python 脚本先写入 `/tmp/` 文件，再执行：
```bash
# ❌ 会失败：heredoc 中跨行 regex 字符串报错
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)  # SyntaxError!
PYEOF

# ✅ 正确：先写文件再执行
cat > /tmp/publish.py << 'PYEOF'
import re
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF
python3 /tmp/publish.py
```

## Feishu API 在 cron 沙箱中返回 404 的处理

**问题**：在 cron 沙箱环境中调用 `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 404，但相同代码在本地终端执行正常。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用；若返回了值但 API 返回 404，则说明是沙箱网络策略限制（无法访问外网）。

**解决方案**：
1. 检查 cron job 配置中是否有 `deliver: 'feishu:chat_id'` 字段
2. 如果有，系统会自动投递，无需手动调用 API
3. 手动调用失败不等于 cron 投递失败，以系统自动投递结果为准

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

```python
import re, subprocess, json

def mmx_search_news(query, max_results=8):
    """专门用于搜索近期新闻的 mmx search 封装"""
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    try:
        data = json.loads(r.stdout)
        results = []
        for item in data.get('organic', [])[:max_results]:
            snippet = re.sub(r'<[^>]+>', '', item.get('snippet', '')).replace('﻿', '').strip()
            results.append({
                'title': item.get('title', ''),
                'link': item.get('link', ''),
                'snippet': snippet[:200],
                'date': item.get('date', '')
            })
        return results
    except Exception as e:
        return [{'error': str(e)}]

# 示例：搜索带日期约束的近期新闻
results = mmx_search_news("原子级制造 2026年5月9日 OR 5月10日", max_results=5)
for r in results:
    print(f"[{r['date']}] {r['title']}")
    print(f"  {r['snippet'][:150]}")
```

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

[SYSTEM: The user has invoked the "wiki-platform-ops" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: wiki-platform-ops
description: 维基平台运维手册——进程管理、ngrok公网访问、OpenClaw/OpenPro绑定问题、Git Push凭证
---

# Wiki 平台运维手册

## 环境信息
- 项目路径：`/Volumes/256G/gesila_wiki/`
- Flask 端口：5001
- 启动命令：`cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py`
- GitHub 仓库：`https://github.com/gesilajerry/gesila_wiki.git`
- 分支：main

## 公网访问
- ngrok URL：`https://unbribing-undeep-maximina.ngrok-free.dev`
- ngrok token：`***NGROK_TOKEN_MASKED***`

### ngrok 配置（macOS native）
**配置文件**：`~/Library/Application Support/ngrok/ngrok.yml`

```yaml
version: "3"
agent:
  authtoken: ***NGROK_TOKEN_MASKED***
tunnels:
  wiki:
    proto: http
    addr: 5001
  gateway:
    proto: http
    addr: 18789
```

**启动指定隧道**：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <tunnel-name>`

**常见错误**：错误启动了 `gateway` tunnel（指向 18789/Hermes）而非 `wiki` tunnel（指向 5001/维基）。检查：
```bash
ps aux | grep ngrok | grep -v grep  # 看在跑哪个 tunnel
curl -s http://localhost:4040/api/tunnels  # ngrok web UI API，看实际 addr 是什么
```

### ngrok API（管理隧道）
```bash
# 查看当前 tunnels（包括 name、public_url、实际指向的 addr）
curl -s http://localhost:4040/api/tunnels | python3 -c "import sys,json; [print(t['name'], t['public_url'], '->', t['config']['addr']) for t in json.load(sys.stdin).get('tunnels',[])]"

# 删除某个 tunnel
curl -X DELETE http://localhost:4040/api/tunnels/<name>

# 创建新 tunnel（POST body 必须含 proto + addr，name 可选）
curl -X POST http://localhost:4040/api/tunnels \
  -H 'Content-Type: application/json' \
  -d '{"proto":"http","addr":"5001"}'
```

**故障排查流程：**
1. `ps aux | grep ngrok | grep -v grep` — 看在跑哪个 tunnel
2. `curl http://localhost:4040/api/tunnels` — 验证实际 addr 是否正确（wiki=5001, gateway=18789）
3. 若 tunnel 指向错误地址：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <正确tunnel名>`

### 常见错误：wiki tunnel 指向 gateway 的 18789
**症状**：wiki 的 ngrok URL 能通，但打开是 OpenClaw/Hermes 的页面，而非维基页面。
**原因**：启动了 `gateway` tunnel（18789）而非 `wiki` tunnel（5001）。
**修复**：`kill <ngrok_pid>` 后 `cd ~/Library/Application\ Support/ngrok && ./ngrok start wiki`

## OpenClaw/OpenPro 绑定问题
**症状**：ngrok URL 被 OpenClaw/OpenPro 拦截，跳转到 OpenPro 页面而不是 wiki。

**原因**：ngrok URL 同时绑定了 Wiki 项目和 OpenPro，OpenClaw/OpenPro 优先拦截。

**解决方法**：在 OpenClaw/OpenPro 控制台解除 URL 与 OpenPro 的绑定，只保留 Wiki 项目绑定。

## GitHub 推送凭证（必须）

**当前有效 Token**：`ghp_***MASKED***`（GitHub Classic，40位）

**⚠️ GitHub Classic Token 说明**
- 必须是 Classic Token，不是新版 Fine-grained PAT
- 长度固定 40 位
- 设置路径：GitHub → Settings → Developer settings → Personal access tokens → Classic
- 权限需要 `repo` 全权限

**第一步**：创建 `~/.netrc`（权限必须 600）：
```
machine github.com
login gesilajerry
password ghp_***MASKED***
```

**第二步**：关闭 SSL 验证（必须）：
```bash
git config --global http.sslVerify false
```
原因：macOS 上的 HTTPS 代理做 SSL interception，导致 `git pull/push` 报错 `LibreSSL SSL_connect: SSL`。关闭 SSL 验证绕过代理的证书拦截。

**第三步**：推送：
```bash
cd /Volumes/256G/gesila_wiki
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**❌ 错误做法**：把 token 嵌入 git remote URL（`https://user:token@github.com/`）——token 会出现在进程列表和日志中，不安全。

**✅ 正确做法**：统一用 netrc + credential.helper，remote URL 保持干净。

### Git 双向同步脚本

**脚本路径**：`/Users/mac/.hermes/scripts/git_sync_wiki.py`

**cron job ID**：`61c28cf385bb`
**调度时间**：`0 23 * * *`（每天 23:00）

**用途**：另一个 agent（Trae）往 GitHub 推送后，本地每日 23:00 拉取同步。

**手动触发**：
```bash
/usr/bin/python3 /Users/mac/.hermes/scripts/git_sync_wiki.py
```

**已知踩坑**：
- `git remote URL` 里嵌 token → 进程列表暴露，风险
- netrc 权限不是 600 → git 不读取
- Token 用了旧版（<40位）→ 认证失败（GitHub 已不支持密码认证，必须用 Classic PAT）
- cron prompt 里写明文 token → 触发安全扫描拦截，改用 Python 脚本读取 token

## 目录结构（不动任何文件夹）
```
gesila_wiki/
  01_Inbox/日志/
  02_Knowledge/概念卡/、方法论/、项目复盘/
  03_Output/已发布/、汇报材料/
```
只往现有目录存文件，框架自动识别。

## OpenClaw Gateway 运维

### 症状（版本 2026.4.24 及之前）
- `openclaw status` / `openclaw update check` 等命令全部超时（30s+）
- `curl http://localhost:18789` 返回 connection refused
- `ps aux | grep openclaw` 显示 gateway 进程在跑但 0% CPU
- 日志 `~/.openclaw/logs/gateway.log` 最后一条是 `"starting channels and sidecars..."` 然后停住
- 日志不写，但 `kill -USR2 <pid>` 可强制刷新日志缓冲区

### 根因（版本 2026.4.24 及之前）
OpenClaw gateway 启动时每次都重新安装所有插件的 bundled runtime deps（npm 包），共 66 个包，在 `~/.openclaw/plugin-runtime-deps/` 目录下。
- 正常需 8-12 分钟
- npm ENOTEMPTY 锁冲突时可达 30 分钟+
- 日志停在 "starting..." 是因为在等插件安装完成，并非真卡住
- 日志文件 600MB+ 会导致日志轮转出问题

### 根因（版本 2026.4.25 — 2026.4.26）
**browser 插件的 CDP 初始化会卡死 gateway**（0% CPU，端口监听但不响应 HTTP）。
- 进程 PID 存在，端口 18789 监听，但所有 HTTP 请求超时
- 日志不写入（或写入极慢）
- Dev 模式（`OPENCLAW_PROFILE=dev`）因 `skipBootstrap: true` 能起来

**修复**：在 `openclaw.json` 中关闭 browser 插件：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['plugins']['entries']['browser'] = {'enabled': False}
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2)
```

### 根因（版本 2026.4.27+）— 升级后完全无法启动
**升级到 2026.4.27 后 gateway 卡在 "staging bundled runtime deps" 完全无响应。**

1. **plugin-runtime-deps 安装不完整**：同时存在 4 个版本目录（2026.4.24, 2026.4.25, 2026.4.27, unknown），版本之间文件互相引用导致缺失文件（如 `chutes-oauth-CUO6Kvd3.js`）
2. **npm 网络不通**：gateway 尝试安装 8 个缺失包（@agentclientprotocol/claude-agent-acp@0.31.0, @grammyjs/*, @homebridge/ciao@^1.3.6, acpx@0.6.1, chokidar@^5.0.0, grammy@^1.42.0 等），npm registry 访问超时后一直重试
3. **所有 openclaw CLI 命令全部超时**：`doctor`, `gateway stop`, `dashboard` 等都卡死
4. **launchctl 重启后 gateway 进程存在但不监听端口**：旧进程没真正被杀掉（launchd 管理混乱）

**Gateway token 位置**：`openclaw.json` 里存的是 `"***"`（生成后存在 `~/.openclaw/identity/device-auth.json` 的 `tokens.operator.token` 字段），该 token 是 device-to-device 认证，不用于 HTTP API。

**降级到 2026.4.25 方案**：
```bash
# 杀掉所有 gateway 进程
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 卸载 2026.4.27
rm -rf ~/.npm-global/lib/node_modules/openclaw

# 指定版本安装（--registry 避免网络问题）
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
```

**验证版本**：`openclaw --version` 和 `cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"`

### 诊断步骤
```bash
# 1. 查看 openclaw 版本
openclaw --version

# 2. 看进程状态
ps aux | grep "openclaw" | grep -v grep

# 3. 检查端口监听
curl -s --connect-timeout 5 "http://localhost:18789/health"

# 4. 检查 npm 包完整性
ls ~/.npm-global/lib/node_modules/openclaw/dist/index.js
cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"

# 5. 查看 plugin-runtime-deps 版本
ls ~/.openclaw/plugin-runtime-deps/

# 6. 检查 npm 锁冲突
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete

# 7. 强制刷新日志
kill -USR2 <pid>
tail -20 ~/.openclaw/logs/gateway.log

# 8. 查看 /tmp/openclaw/ 下的实时日志（JSON 格式）
tail -f /tmp/openclaw/openclaw-$(date +%Y-%m-%d).log
```

### 修复方案

**方案 A：清理后重启（推荐用于 2026.4.24 及之前版本）**
```bash
# 清空错误日志（保留文件避免权限问题）
truncate -s 0 ~/.openclaw/logs/gateway.err.log

# 删除 npm 缓存锁
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete
find ~/.openclaw/plugin-runtime-deps/ -name ".openclaw-npm-cache" -type d -exec rm -rf {} + 2>/dev/null

# 杀掉旧进程（注意：2026.4.27 版本进程杀掉后还会重启，需先卸载）
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：降级回 2026.4.25（2026.4.27 专用）**
```bash
# 杀掉所有进程
pkill -9 -f "openclaw" 2>/dev/null
rm -rf ~/.npm-global/lib/node_modules/openclaw
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：跳过飞书 channel 加速启动**
飞书 bot probe 超时（axios 30s × 4 accounts = 2 分钟延迟）：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['channels']['feishu']['enabled'] = False
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2, ensure_ascii=False)
```
重启后 gateway 约 3 分钟可 ready（vs 10 分钟带飞书）。

**方案 C：手动触发 npm 安装（减少重复安装）**
让 gateway 跑完一次后，下次重启会复用缓存。但 npm ENOTEMPTY 问题会反复出现，每次都要清锁。

### launchd 管理
```bash
# 查看状态
launchctl list | grep openclaw

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist

# 日志路径
~/.openclaw/logs/gateway.log（主日志）
~/.openclaw/logs/gateway.err.log（错误日志）
/tmp/openclaw/openclaw-YYYY-MM-DD.log（实时 JSON 日志）
```

### OpenClaw vs Hermes 定时任务关系
- OpenClaw gateway 内置 cron 调度（job_id 如 `7c55dfb106c3`）
- Hermes 有独立的 cron 系统（`~/.hermes/cron/jobs.json`）
- 两者可以并存；迁移时暂停 OpenClaw 任务，避免重复执行：
  `cronjob pause <job_id>`

---

## 性能优化（app.py）

**问题**：158+ 文件时，每次请求全量读文件，首页/图谱页慢到无法操作。

**优化方案**（app.py 已实现）：
1. 启动时预热：`app/__init__` 时 `_load_cards()` + `_build_graph()` 预加载，197张卡约0.1秒
2. 文件 mtime 检测：`_scan_mtimes()` 追踪文件变更，TTL 从4小时改为文件驱动刷新
3. 后台异步重建：缓存过期时返回旧数据同时后台线程重建，不阻塞请求
4. 图数据独立缓存：`build_graph()` 结果存入 `_cache['graph']`

**重启生效**：`kill <PID> && python3 app.py`

---

## 结构层提炼任务（定时 cron 执行）

每晚21:30自动执行，从当日归档日志中提炼知识卡片。

### Step 1: 确定日期
当天日期（如：`2026-05-11`）

### Step 2: 读取归档日志
```
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_会话复盘.md
```

**⚠️ 解析要点：推送日报文件超大（~460KB）**
- 文件过大是因为每个任务块的 Prompt 都完整嵌入了调用的 SKILL.md 内容（整个技能文件）
- 实际 AI 生成的 Response 内容才是需要提取的知识素材
- 搜索 AI 简报内容时用 emoji 标记（如 `"🤖 AI热点简报"`）或 Response 标记来定位
- 各行业简报（MLCC/原子级制造/半导体/财经）的内容在 Response 段落，篇幅通常 2-20KB

### Step 3: 识别值得提炼的内容
识别 1-5 个值得结构化的核心概念或方法论，优先关注：
- 小红书运营经验
- 飞书 API 使用技巧
- Cron 任务管理
- 内容创作方法论
- 产业链洞察

### Step 4: 写入知识卡片
```
/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_名称.md
/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_名称.md
```

格式：
```
# {名称}

**背景**
**核心洞察**
**对Jerry的意义**
**延伸思考**

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
用 tenant_access_token 方式发送飞书 post 消息到 `oc_1e92781cdf15f71fad7dcd8d5d386e76`，报告提炼了几张卡片及标题。

凭证：
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: ***FEISHU_SECRET_MASKED***

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是原子级制造产业与科研动态研究助手。请用 mmx search 命令搜索过去24小时内发生的原子级制造相关动态，生成原子级制造科研晚报，完成后同步发布到飞书和Koala博客。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "原子级制造 团簇离子束 纳米材料"
```
```bash
mmx search query -t 24h --limit 8 "原子级制造 量子点 薄膜沉积"
```
```bash
mmx search query -t 24h --limit 8 "原子级制造 纳米科技 科研突破"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【科研进展】【产业动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
⚛️ 原子级制造科研晚报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【科研进展】
（3-5条，每条：标题 + 摘要）

【产业动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "原子级制造科研晚报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 4, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

⚛️ **原子级制造科研晚报 | 20260522**

---

**【今日要闻】**

1. **我国科学家在镍基高温超导领域再获突破**
   新华社5月22日电，深圳传来重磅消息，我国科学家在镍基高温超导材料领域实现新突破，该成果对理解高温超导机理及原子级材料制备具有重要意义。

2. **中科宇航创新研究院广州揭牌，太空制造产业化提速**
   5月22日，中科宇航创新研究院在广州市黄埔区正式揭牌。研究院以太空制造为核心牵引，布局亚轨道微重力实验平台、轨道级太空制造平台及天地往返运输能力，重点覆盖太空生物医药、高端半导体、先进材料三大未来产业方向。

3. **机器学习解析金-硫醇盐纳米团簇自组装机理**
   国际期刊《Aggregate》5月22日发表研究，研究人员开发基于密度泛函理论训练的ACE相互作用势，模拟时间尺度达0.1μs，揭示Au25至Au851纳米团簇的原子尺度融合路径与配体门控反应窗口。

---

**【科研进展】**

1. **3D打印大气压辉光反应器合成多金属纳米团簇催化剂**
   《Catalysis Science & Technology》5月22日刊发我国科研团队成果，利用冷大气压等离子体（CAPP）从含铂、钯、锇、钌、铼、钨、铬前驱体中合成"富电子"特性多金属纳米团簇催化剂，应用于加氢反应，填补该领域空白。

2. **溶液法氧化锌薄膜结构与介电性能研究取得新进展**
   《Next Nanotechnology》5月22日发表论文，采用溶胶-凝胶旋涂法在玻璃衬底制备ZnO薄膜，系统研究不同旋转速度（1000/2000/3000 rpm）及退火工艺对薄膜结构、光学与介电性能的影响规律。

3. **横向电场调控氮化镓纳米磨损取得突破**
   西南交通大学团队研究发现，施加横向电场可有效调节GaN与Al2O3磨料间的摩擦化学反应，当电压从0V增至3V时，材料去除率初期下降约30%，为提升GaN器件制造精度、限制氧化损伤提供新路径。

---

**【产业动态】**

1. **工信部原子级制造揭榜挂帅：2026年目标锁定团簇离子束装备**
   原子级制造揭榜挂帅任务榜单明确：到2026年，产出高效团簇离子束原子级抛光装备、200-300kV场发射透射电镜、原子级金属粉体、原子级精度X射线反射镜等系列标志性产品，核心基础涵盖ALD工艺仿真平台与原子级平坦化工艺。

2. **粉体原子层沉积（ALD）成为原子制造破局关键**
   Forge Nano等国际企业领航粉体ALD技术，实现三维均匀包覆与亚纳米级（<1nm）厚度精准可控。工信部明确"到2026年实现<1nm薄膜沉积、批处理能力>10kg/批次"目标，直击行业痛点。

3. **中科宇航创新研究院聚焦太空制造全链条能力**
   中科宇航创新研究院提出"上得去、做得成、回得来、用得上"太空制造全链条愿景，重点布局高端半导体与先进材料的空间制造能力，太空制造商业化进程实质性提速。

---

**【今日关注】**

镍基高温超导突破与太空制造揭牌，两大事件同日出现并非巧合——前者代表原子级材料科学的深耕突破，后者代表先进制造的疆域拓展。工信部已将原子级制造定为国家重大战略方向，从地面到太空、从实验室到产业化，我国正以"揭榜挂帅"机制系统性推进这场制造业的终极变革。可以预见，2026年将成为原子级制造从技术验证迈向规模化验证的关键节点。

---

✅ **已发布至 Koala 博客**（研发工程-专业知识板块）  
✅ **已发送至飞书**（chat_id: oc_1e92781cdf15f71fad7dcd8d5d386e76）

---


## 来源任务
- job_id: d2e8d49ca8ba | 任务名: MLCC产业链晚报 22:00
---
# Cron Job: MLCC产业链晚报 22:00

**Job ID:** d2e8d49ca8ba
**Run Time:** 2026-05-22 20:03:25
**Schedule:** 0 20 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## Python heredoc 在 cron/execute_code 中执行 mmx 的注意事项

**问题**：在 cron 定时任务的 `execute_code` 沙箱中，用 heredoc `python3 << 'PYEOF'` 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: unterminated string literal`。

**解决方案**：将 Python 脚本先写入 `/tmp/` 文件，再执行：
```bash
# ❌ 会失败：heredoc 中跨行 regex 字符串报错
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)  # SyntaxError!
PYEOF

# ✅ 正确：先写文件再执行
cat > /tmp/publish.py << 'PYEOF'
import re
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF
python3 /tmp/publish.py
```

## Feishu API 在 cron 沙箱中返回 404 的处理

**问题**：在 cron 沙箱环境中调用 `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 404，但相同代码在本地终端执行正常。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用；若返回了值但 API 返回 404，则说明是沙箱网络策略限制（无法访问外网）。

**解决方案**：
1. 检查 cron job 配置中是否有 `deliver: 'feishu:chat_id'` 字段
2. 如果有，系统会自动投递，无需手动调用 API
3. 手动调用失败不等于 cron 投递失败，以系统自动投递结果为准

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

```python
import re, subprocess, json

def mmx_search_news(query, max_results=8):
    """专门用于搜索近期新闻的 mmx search 封装"""
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    try:
        data = json.loads(r.stdout)
        results = []
        for item in data.get('organic', [])[:max_results]:
            snippet = re.sub(r'<[^>]+>', '', item.get('snippet', '')).replace('﻿', '').strip()
            results.append({
                'title': item.get('title', ''),
                'link': item.get('link', ''),
                'snippet': snippet[:200],
                'date': item.get('date', '')
            })
        return results
    except Exception as e:
        return [{'error': str(e)}]

# 示例：搜索带日期约束的近期新闻
results = mmx_search_news("原子级制造 2026年5月9日 OR 5月10日", max_results=5)
for r in results:
    print(f"[{r['date']}] {r['title']}")
    print(f"  {r['snippet'][:150]}")
```

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是MLCC产业链研究助手。请用 mmx search 命令搜索过去24小时内发生的MLCC/被动元器件产业动态，生成MLCC产业晚报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "MLCC 被动元件 村田 太阳诱电"
```
```bash
mmx search query -t 24h --limit 8 "MLCC产能 扩产 价格走势"
```
```bash
mmx search query -t 24h --limit 8 "消费电子 汽车电子 MLCC需求"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【产能动态】【市场观察】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
🔋 MLCC产业晚报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【产能动态】
（3-5条，每条：标题 + 摘要）

【市场观察】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "MLCC产业链晚报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 1, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

🔋 **MLCC产业晚报 | 20260522** 已完成，执行结果如下：

**✅ 搜索完成** — 通过 mmx 联网搜索获取了以下维度信息：
- MLCC大厂动态（村田、太阳诱电、三星电机）
- A股MLCC概念行情（风华高科、昀冢科技、三环集团等）
- TrendForce价格数据分析
- 英伟达Rubin平台MLCC用量拆解

**✅ Koala博客已发布** — 市场销售 > 专业知识（forum_id=1, group_ids=[12]）
- 文章ID: `S1jNQwqj6m8jtNZS`

**✅ 飞书已发送** — 送达群聊 `oc_1e92781cdf15f71fad7dcd8d5d386e76`
- message_id: `om_x100b6e30113178acc236d900788f12f`

---

**简报内容摘要：**

【今日要闻】4条：MLCC板块掀涨停潮（风华高科4天2板）/ 三星电机三年产能被预订一空 / 村田/太阳诱电/三星电机密集涨价15%-35% / 风华高科更名战略升级

【产能动态】4条：TrendForce高端MLCC供需偏紧 / 英伟达Rubin平台MLCC用量激增30% / AI虹吸成熟产能供需缺口延续 / 国产晶圆代工涨价向元件端传导

【市场观察】4条：机构看好本土厂商顺周期机遇 / 消费电子温和复苏 / 48V车规MLCC单机用量增30%-50% / 充电桩高增长带动高压MLCC需求

【今日关注】涨价周期持续性或超预期，建议跟踪6月新报价窗口及本土厂商高端产品导入进展。

---


## 来源任务
- job_id: 3c9cf2c9d74e | 任务名: 财经新闻推送 20:30
---
# Cron Job: 财经新闻推送 20:30

**Job ID:** 3c9cf2c9d74e
**Run Time:** 2026-05-22 21:14:39
**Schedule:** 10 21 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## Python heredoc 在 cron/execute_code 中执行 mmx 的注意事项

**问题**：在 cron 定时任务的 `execute_code` 沙箱中，用 heredoc `python3 << 'PYEOF'` 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: unterminated string literal`。

**解决方案**：将 Python 脚本先写入 `/tmp/` 文件，再执行：
```bash
# ❌ 会失败：heredoc 中跨行 regex 字符串报错
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)  # SyntaxError!
PYEOF

# ✅ 正确：先写文件再执行
cat > /tmp/publish.py << 'PYEOF'
import re
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF
python3 /tmp/publish.py
```

## Feishu API 在 cron 沙箱中返回 404 的处理

**问题**：在 cron 沙箱环境中调用 `https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 404，但相同代码在本地终端执行正常。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用；若返回了值但 API 返回 404，则说明是沙箱网络策略限制（无法访问外网）。

**解决方案**：
1. 检查 cron job 配置中是否有 `deliver: 'feishu:chat_id'` 字段
2. 如果有，系统会自动投递，无需手动调用 API
3. 手动调用失败不等于 cron 投递失败，以系统自动投递结果为准

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

```python
import re, subprocess, json

def mmx_search_news(query, max_results=8):
    """专门用于搜索近期新闻的 mmx search 封装"""
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    try:
        data = json.loads(r.stdout)
        results = []
        for item in data.get('organic', [])[:max_results]:
            snippet = re.sub(r'<[^>]+>', '', item.get('snippet', '')).replace('﻿', '').strip()
            results.append({
                'title': item.get('title', ''),
                'link': item.get('link', ''),
                'snippet': snippet[:200],
                'date': item.get('date', '')
            })
        return results
    except Exception as e:
        return [{'error': str(e)}]

# 示例：搜索带日期约束的近期新闻
results = mmx_search_news("原子级制造 2026年5月9日 OR 5月10日", max_results=5)
for r in results:
    print(f"[{r['date']}] {r['title']}")
    print(f"  {r['snippet'][:150]}")
```

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你财经新闻助手。请用 mmx search 命令搜索过去24小时内发生的财经新闻，生成财经今日热点简报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "宏观经济 货币政策 金融市场"
```
```bash
mmx search query -t 24h --limit 8 "A股 上市公司 财报"
```
```bash
mmx search query -t 24h --limit 8 "房地产 股市 基金"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【市场动态】【行业观察】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
📈 财经晚报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【市场动态】
（3-5条，每条：标题 + 摘要）

【行业观察】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（综合管理-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "财经晚报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 2, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

📈 **财经晚报 | 2026年05月22日**

---

**【今日要闻】**

1. **央行逆回购1530亿元，利率维持1.40%**
   央行以固定利率、数量招标方式开展1530亿元7天期逆回购操作，当前流动性充裕，短期内降准降息必要性下降。

2. **美银调查：过半投资者预计美联储年内加息**
   超过半数投资者认为美联储已满足或在年底前满足加息条件，新西兰联储下周利率决议备受关注。

3. **外资年内净流入A股131亿美元**
   瑞银证券数据显示，本轮反弹中外资净流入A股达131亿美元，中国资产重现"股汇共振"特征。

**【市场动态】**

1. **A股三大指数收涨，创业板指涨2.84%**
   沪指报4112.90点，涨0.87%；深成指涨2.30%，创业板指涨2.84%。沪深北三市成交额29249亿元，超3800只个股上涨。

2. **港股恒生科技指数涨超2%**
   恒生科技指数大涨2.11%，网易绩后涨近6%，兆易创新涨近13%再创新高，AI概念股集体走强。

3. **PCB、MLCC概念爆发，机器人概念活跃**
   元件、PCB概念集体走强，MLCC概念爆发，机器人概念反复活跃，市场主线围绕科技修复展开。

**【行业观察】**

1. **联想集团AI收入增105%，全年营收创历史新高**
   联想集团全年营收5899亿元，同比增长20.3%，AI相关收入同比增105%，混合式AI战略进入商业化阶段。

2. **物理AI成新风口，索辰科技5天涨超50%**
   以机器人为代表的物理AI方向涌现牛股，国产CAE软件索辰科技5个交易日涨超50%，机构看好中长期发展前景。

3. **上海二手房"收储"扩围至中心城区**
   上海二手房"收储"政策扩围至全部中心城区；深圳楼市仍处区域分化、量涨价稳的微妙平衡。

**【今日关注】**

在全球央行紧缩预期升温、国内货币政策稳字当头的分化格局下，A股以科技股修复为主线，创业板指涨近3%展现较强反弹韧性。以AI应用和机器人为代表的"物理AI"正成为资金新布局方向，相关个股已有业绩支撑。短期内市场或维持震荡，但结构性机会仍大于系统性风险，建议关注中报业绩向好的科技细分领域。

---

✅ **Koala发布成功**（综合管理-专业知识，文章ID: XtVaI58EOvhLNel7）
✅ **飞书发送成功**

---
