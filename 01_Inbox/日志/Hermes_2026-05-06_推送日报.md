* * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是财经新闻助手。请用 mmx search query 命令搜索财经新闻，生成财经热点简报。

## 搜索命令
```bash
mmx search query --q "财经新闻 股市 经济政策 2026" --output json
mmx search query --q "economy stock market news China 2026" --output json
```

## 执行步骤
1. 用 subprocess 运行 mmx search query，合并 2-3 个关键词的搜索结果
2. 解析 JSON 结果，按相关性取前 8-10 条
3. 清洗 snippet 中的 HTML 标签

## 输出格式（每条）
标题：[文章标题]
摘要：[二三十字核心内容摘要]
来源：[链接]
---
（空行分隔）

## 最后
今日关注：[一句话点评当天最重要新闻的意义]

## 发送
发送格式化简报到飞书：chat_id = oc_1e92781cdf15f71fad7dcd8d5d386e76

飞书凭证：APP_ID=cli_a96f4f328a785cd2，APP_SECRET=jYFMuuszh8g5h1bvb32Q1dcywimWG1TH

## Response

📊 **财经热点简报 · 2026年5月6日** 已发送至飞书（code=0 成功）。

---

**简报摘要（共8条）：**

**1️⃣ 五月A股展望：震荡偏强，AI科技与资源能源成结构性主线**
5月政策仍可能偏积极，外部风险可能缓和，流动性维持宽松，中小盘成长风格相对占优。建议逢低配置电新（AI电力、锂电）、军工、传媒、计算机及券商消费等。

**2️⃣ 港股科技股爆发：小米一度涨超10%，阿里涨超6%**
5月4日恒生科技指数一度涨超3.70%，韩股KOSPI涨超4%。科技股走高受霍尔木兹海峡局势缓和及美股存储芯片概念股大涨刺激。小米汽车4月交付超3万辆，环比增长50%。

**3️⃣ 澳大利亚央行加息25个基点至4.35%，年内第三次加息**
澳央行将政策利率从4.1%上调至4.35%，达2024年12月以来最高水平。原因：通胀仍处高位，中东冲突致大宗商品价格上涨。

**4️⃣ 商务部发布阻断禁令，反制美对5家中企涉伊朗石油制裁**
商务部阻断美国以参与涉伊朗石油交易为由对恒力石化等5家企业的制裁。伊朗外长阿拉格齐将于5月6日应邀访华。

**5️⃣ 2025年A股现金分红总额2.42万亿元，创历史新高**
逾3700家公司推出现金分红方案，分红比例达44.87%。银行业是分红主力军，股息率超3%的公司有460余家。

**6️⃣ 五月转债策略：外部趋稳，内寻景气**
5月A股整体表现稳健，小盘与成长风格相对占优。成长与消费风格平均涨幅分别为3.11%和2.51%，胜率均为57.14%。

**7️⃣ 长城策略5月金股：政治局会议政策面向好**
会议重点强调深入整治内卷、全面实施人工智能+、扩内需优质消费供给和新型基建投资等。

**8️⃣ A股节后首个交易日展望：五一后修复行情为长期规律**
A股将于5月6日迎来节后首日，沪指与深成指上涨概率均为60%。港股5月5日午盘恒指跌1.16%。

---

💡 **今日关注：** AI科技与资源能源主线确立，五月A股震荡偏强格局可期；港股科技股情绪向好，节后A股有望迎来修复行情。

---

## 来源任务
- job_id: ea7c8a49fb94 | 任务名: 深圳保障房申请批次检查
---
# Cron Job: 深圳保障房申请批次检查

**Job ID:** ea7c8a49fb94
**Run Time:** 2026-05-06 09:04:36
**Schedule:** 0 9 * * *

## Prompt

[SYSTEM: The user has invoked the "xiaohongshu" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: xiaohongshu
description: |
  小红书（RED/XHS）自动化助手。提供完整的小红书操作能力：登录、发布图文/视频、搜索笔记、浏览详情、点赞收藏评论、查看博主主页、内容策划。
  当用户提到小红书、红书、XHS、RED、发笔记、搜笔记、小红书运营等任何与小红书相关的操作时使用此 skill，即使用户没有明确说"小红书"但描述的场景明显是小红书（如"发一篇种草笔记"、"帮我分析这个博主"）也应触发。
---

你是小红书自动化助手，通过 xiaohongshu-mcp 的 MCP 工具帮助用户操作小红书。

## 前置检查（每次执行必做）

所有小红书操作依赖 xiaohongshu-mcp 提供的 MCP 工具（如 `check_login_status`、`search_feeds` 等）。执行任何操作前，先确认这些工具是否可用：

**判断方法**：检查当前可用的 MCP 工具列表中是否存在 `check_login_status`。

- **工具存在** → 正常执行后续流程
- **工具不存在** → 说明 xiaohongshu-mcp 服务未配置。直接告知用户：「小红书 MCP 服务尚未连接，请先运行 `/setup-xhs-mcp` 完成部署和配置。」不要尝试用其他工具（如 Playwright、WebFetch）代替。

## 意图识别与路由

根据用户输入判断意图，然后直接按对应子 skill 的指令执行。如果意图不明确，先询问用户想做什么。

| 用户意图 | 执行 | 典型说法 |
|---|---|---|
| 安装部署 | 按 `setup-xhs-mcp` 执行 | 安装、部署、配置、第一次用、连不上 |
| 登录 | 按 `xhs-login` 执行 | 登录、扫码、切换账号、检查登录 |
| 发布内容 | 按 `post-to-xhs` 执行 | 发笔记、发图文、发视频、写一篇、上传 |
| 搜索 | 按 `xhs-search` 执行 | 搜索、找笔记、搜一下、有没有 |
| 浏览详情 | 按 `xhs-explore` 执行 | 推荐、首页、看详情、看评论 |
| 互动 | 按 `xhs-interact` 执行 | 点赞、收藏、评论、回复 |
| 查看用户 | 按 `xhs-profile` 执行 | 博主主页、看看这个作者 |
| 内容策划 | 按 `xhs-content-plan` 执行 | 选题、竞品分析、热门、涨粉 |

## 全局约束

1. **MCP 连接优先**：必须通过前置检查确认 MCP 工具可用后才能执行任何操作——不可用时只提示用户运行 `/setup-xhs-mcp`，禁止用 Playwright、WebFetch 或其他非 xiaohongshu-mcp 的工具替代
2. **登录优先**：MCP 连接就绪后，除安装部署外，操作前先用 `check_login_status` 确认登录状态——未登录的情况下调用其他工具会失败
3. **用户确认**：发布、评论等写操作执行前展示内容让用户确认——因为这些操作发出后无法撤回，代表用户的公开行为
4. **参数来源**：`feed_id` 和 `xsec_token` 必须从搜索或浏览结果中获取，不可编造——编造的参数会导致 MCP 工具报错

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证

- **Bearer Token**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1aWQiOjExMywiYXV0aF90eXBlIjoxLCJjb3JzIjpmYWxzZSwia2V5IjoiZGNjYjYzYWItN2JjYS00ZTMyLThlMDctMDY4ZThjZTBmMTlkIiwic2FsdCI6IkdkOXMxdWpibWtmUEV0ZWwifQ.eW1Ortnq9kBIwmu_g5a59wsP-fO9wHO2PzbCJ-ZWCCQ`
- 用户：洁白的果泥 (uid=113)

## 发布流程（批量版）

### 关键注意事项

> **⚠️ 踩坑记录（2026-04 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500
> 2. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\U00002600-\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤
> 3. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500
> 4. **500 错误有随机性**，建议加 3 次重试（间隔 2-3 秒）

### Step 1: 发布函数（含重试 + 单独 CSRF）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN = "Bearer Token 上文"
FORUM_ID = 2

def publish_one(title, content_html, group_ids, attempt=1):
    """发布单篇文章，每次单独获取 CSRF"""
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": FORUM_ID,
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')  # ensure_ascii=False 保存中文

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')  # × 会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",
    "/path/to/article2.md",
]
CATEGORY = 12  # 专业知识

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    r = publish_one(title, html, [CATEGORY])
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} {title}")
    time.sleep(1.5)  # 间隔防限速
```

## 文章分类映射 (group_ids)

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 12 | 专业知识 | 地缘政治、交易框架、金融方法论、投资分析 |
| 11 | 经验技巧 | 工作技巧、方法心得、效率工具 |
| 41 | 案例复盘 | 交易复盘、事件分析、案例总结 |
| 13 | 培训学习 | 教学培训、学习笔记 |
| 47 | 工具推荐 | 工具分享、软件推荐 |
| 48 | 兴趣分享 | 兴趣爱好 |
| 49 | 综合分享 | 一般分享 |
| 6 | 行政事务 | 公司行政 |
| 7 | 人事相关 | 人事 |
| 8 | 财务相关 | 财务 |
| 43 | IT技术 | 技术类 |
| 44 | 业务流程 | 流程 |
| 45 | 工具使用 | 软件使用 |

**注意**: `group_ids` 是数组，可以传多个，如 `[12, 41]`

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

## 自动分类规则

根据文件标签或内容关键词自动选择 group_ids（取第一个匹配）：

- 含 `#地缘政治` 或 `#交易框架` 或 `#金融` → `12` 专业知识
- 含 `#交易复盘` 或 `#案例分析` → `41` 案例复盘
- 含 `#工具` 或 `#软件推荐` → `47` 工具推荐
- 含 `#培训` 或 `#学习` → `13` 培训学习
- 含 `#工作` 或 `#效率` → `11` 经验技巧
- 默认 → `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 401 unauthorized | CSRF Token 过期 | 重新从 GET /api/csrf 获取 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 间隔5秒以上再重试，批量发布时每篇间隔1.5-2秒 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

每天检查深圳市住建局官网是否有新的保障性住房（公租房/保障性租赁住房）申请批次开放。

检查目标：
1. https://zjj.sz.gov.cn/zfxx/bzfsq/index.html （保障性租赁住房）
2. https://zjj.sz.gov.cn/zfxx/ggfw/index.html#/dist/new/confirmRent （公租房认租申请）

方法：
1. 用 browser 工具访问 zjj.sz.gov.cn，找"保障性住房"或"公租房"相关公告
2. 重点看"认租"或"申请"相关通告，查找当前可申请的项目
3. 检查是否有龙华区（Longhua）的项目

输出：
- 如果有开放申请的项目：列出项目名称、申请时间、申请条件摘要
- 如果没有开放申请：告知"今日暂无非新批次开放申请"
- 每次都告知检查日期

注意事项：
- 找最新通告，认租通告一般在"通知公告"或"住房保障"栏目
- 重点关注龙华区、福田区的项目
- 发送结果到飞书：oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

**检查完成，报告已发送至飞书。**

📅 **检查日期**：2026年5月6日（星期三）

---

**今日检查结果摘要**：

✅ **有正在开放申请的批次**：

**面向高层次人才配租保障性租赁住房**
- **申请时间**：2026年4月24日18:00 → **2026年5月15日18:00**（剩余约9天）
- **类型**：保障性租赁住房
- **对象**："鹏城优才卡"持卡人（钻石/白金/金/银卡）及市委人才办确定的急需紧缺人才
- **申请条件**：深圳市全职工作、名下无自有住房、社保要求（钻石卡豁免）、未享受购房优惠政策
- **申请方式**：深圳市住建局官网 → 政务服务 → 住房保障服务 → 保障性租赁住房认租申请 → 高层次人才认租申请
- **咨询**：0755-88631666（工作日9-12点/14-18点）；福田红荔西路莲花大厦东座一楼大厅

⚠️ **注意**：此批次**仅面向高层次人才**，普通公租房（面向轮候库市民）暂无新批次开放。龙华区、福田区亦无新的公租房认租批次。

---

## 来源任务
- job_id: dd15667811fc | 任务名: 财经新闻推送 08:30
---
# Cron Job: 财经新闻推送 08:30

**Job ID:** dd15667811fc
**Run Time:** 2026-05
脚本 `/Users/mac/.hermes/scripts/xhs_topic_search.py` 存在且代码正常。**Round 1 已成功完成** — 状态文件 `/tmp/xhs_topic_state_1.json` 显示：

- 3个候选话题已生成并发送至飞书
- 用户已选择话题：「AI创作应否拥有著作权?争议背后的权利与伦理困境」
- 视频素材正在 `/tmp/xhs_gen_3/` 目录下生成（seg1-9.mp4, tts.mp3 等已就位）

**建议修复：**

1. 检查 Hermes cron 任务配置中该任务的注册方式，shebang 不应被拼入脚本路径
2. 可手动验证脚本：`/usr/bin/python3 /Users/mac/.hermes/scripts/xhs_topic_search.py 1` 单独运行时正常
3. 当前 round 1 已完成并进入视频生成阶段，无需重跑话题搜索

---

## 来源任务
- job_id: 793ce3c39848 | 任务名: Hermes 结构层提炼 11:30
---
# Cron Job: Hermes 结构层提炼 11:30

**Job ID:** 793ce3c39848
**Run Time:** 2026-05-06 11:38:13
**Schedule:** 30 11 * * *

## Prompt

[SYSTEM: The user has invoked the "wiki-platform-ops" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: wiki-platform-ops
description: 维基平台运维手册——进程管理、ngrok公网访问、OpenClaw/OpenPro绑定问题、Git Push凭证
---

# Wiki 平台运维手册

## 环境信息
- 项目路径：`/Volumes/256G/gesila_wiki/`
- Flask 端口：5001
- 启动命令：`cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py`
- GitHub 仓库：`https://github.com/gesilajerry/gesila_wiki.git`
- 分支：main

## 公网访问
- ngrok URL：`https://unbribing-undeep-maximina.ngrok-free.dev`
- ngrok token：`3B9DPZE4I2SawazXcJ4xZmdHdrS_25s2AS6UL5Qcqt9Lxb5sj`

### ngrok 配置（macOS native）
**配置文件**：`~/Library/Application Support/ngrok/ngrok.yml`

```yaml
version: "3"
agent:
  authtoken: 3B9DPZE4I2SawazXcJ4xZmdHdrS_25s2AS6UL5Qcqt9Lxb5sj
tunnels:
  wiki:
    proto: http
    addr: 5001
  gateway:
    proto: http
    addr: 18789
```

**启动指定隧道**：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <tunnel-name>`

**常见错误**：错误启动了 `gateway` tunnel（指向 18789/Hermes）而非 `wiki` tunnel（指向 5001/维基）。检查：
```bash
ps aux | grep ngrok | grep -v grep  # 看在跑哪个 tunnel
curl -s http://localhost:4040/api/tunnels  # ngrok web UI API，看实际 addr 是什么
```

### ngrok API（管理隧道）
```bash
# 查看当前 tunnels（包括 name、public_url、实际指向的 addr）
curl -s http://localhost:4040/api/tunnels | python3 -c "import sys,json; [print(t['name'], t['public_url'], '->', t['config']['addr']) for t in json.load(sys.stdin).get('tunnels',[])]"

# 删除某个 tunnel
curl -X DELETE http://localhost:4040/api/tunnels/<name>

# 创建新 tunnel（POST body 必须含 proto + addr，name 可选）
curl -X POST http://localhost:4040/api/tunnels \
  -H 'Content-Type: application/json' \
  -d '{"proto":"http","addr":"5001"}'
```

**故障排查流程：**
1. `ps aux | grep ngrok | grep -v grep` — 看在跑哪个 tunnel
2. `curl http://localhost:4040/api/tunnels` — 验证实际 addr 是否正确（wiki=5001, gateway=18789）
3. 若 tunnel 指向错误地址：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <正确tunnel名>`

### 常见错误：wiki tunnel 指向 gateway 的 18789
**症状**：wiki 的 ngrok URL 能通，但打开是 OpenClaw/Hermes 的页面，而非维基页面。
**原因**：启动了 `gateway` tunnel（18789）而非 `wiki` tunnel（5001）。
**修复**：`kill <ngrok_pid>` 后 `cd ~/Library/Application\ Support/ngrok && ./ngrok start wiki`

## OpenClaw/OpenPro 绑定问题
**症状**：ngrok URL 被 OpenClaw/OpenPro 拦截，跳转到 OpenPro 页面而不是 wiki。

**原因**：ngrok URL 同时绑定了 Wiki 项目和 OpenPro，OpenClaw/OpenPro 优先拦截。

**解决方法**：在 OpenClaw/OpenPro 控制台解除 URL 与 OpenPro 的绑定，只保留 Wiki 项目绑定。

## Git Push 凭证问题
**症状**：`git push` 超时，osxkeychain/gh auth git-credential 都无法读取凭证（网络到 GitHub 443 端口有代理拦截）。

**解法**：
1. 获取 token：`gh auth token`
2. 创建 `~/.netrc` 文件（权限 600）：
```
machine github.com
login gesilajerry
password {上面获取的token}
```
3. push：`GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main`

此方式比 gh auth/git-credential 更稳定，OpenClaw 自己的 cron 任务用 gh auth 方式也 push 超时失败。

## 目录结构（不动任何文件夹）
```
gesila_wiki/
  01_Inbox/日志/
  02_Knowledge/概念卡/、方法论/、项目复盘/
  03_Output/已发布/、汇报材料/
```
只往现有目录存文件，框架自动识别。

## OpenClaw Gateway 运维

### 症状（版本 2026.4.24 及之前）
- `openclaw status` / `openclaw update check` 等命令全部超时（30s+）
- `curl http://localhost:18789` 返回 connection refused
- `ps aux | grep openclaw` 显示 gateway 进程在跑但 0% CPU
- 日志 `~/.openclaw/logs/gateway.log` 最后一条是 `"starting channels and sidecars..."` 然后停住
- 日志不写，但 `kill -USR2 <pid>` 可强制刷新日志缓冲区

### 根因（版本 2026.4.24 及之前）
OpenClaw gateway 启动时每次都重新安装所有插件的 bundled runtime deps（npm 包），共 66 个包，在 `~/.openclaw/plugin-runtime-deps/` 目录下。
- 正常需 8-12 分钟
- npm ENOTEMPTY 锁冲突时可达 30 分钟+
- 日志停在 "starting..." 是因为在等插件安装完成，并非真卡住
- 日志文件 600MB+ 会导致日志轮转出问题

### 根因（版本 2026.4.25 — 2026.4.26）
**browser 插件的 CDP 初始化会卡死 gateway**（0% CPU，端口监听但不响应 HTTP）。
- 进程 PID 存在，端口 18789 监听，但所有 HTTP 请求超时
- 日志不写入（或写入极慢）
- Dev 模式（`OPENCLAW_PROFILE=dev`）因 `skipBootstrap: true` 能起来

**修复**：在 `openclaw.json` 中关闭 browser 插件：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['plugins']['entries']['browser'] = {'enabled': False}
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2)
```

### 根因（版本 2026.4.27+）— 升级后完全无法启动
**升级到 2026.4.27 后 gateway 卡在 "staging bundled runtime deps" 完全无响应。**

1. **plugin-runtime-deps 安装不完整**：同时存在 4 个版本目录（2026.4.24, 2026.4.25, 2026.4.27, unknown），版本之间文件互相引用导致缺失文件（如 `chutes-oauth-CUO6Kvd3.js`）
2. **npm 网络不通**：gateway 尝试安装 8 个缺失包（@agentclientprotocol/claude-agent-acp@0.31.0, @grammyjs/*, @homebridge/ciao@^1.3.6, acpx@0.6.1, chokidar@^5.0.0, grammy@^1.42.0 等），npm registry 访问超时后一直重试
3. **所有 openclaw CLI 命令全部超时**：`doctor`, `gateway stop`, `dashboard` 等都卡死
4. **launchctl 重启后 gateway 进程存在但不监听端口**：旧进程没真正被杀掉（launchd 管理混乱）

**Gateway token 位置**：`openclaw.json` 里存的是 `"***"`（生成后存在 `~/.openclaw/identity/device-auth.json` 的 `tokens.operator.token` 字段），该 token 是 device-to-device 认证，不用于 HTTP API。

**降级到 2026.4.25 方案**：
```bash
# 杀掉所有 gateway 进程
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 卸载 2026.4.27
rm -rf ~/.npm-global/lib/node_modules/openclaw

# 指定版本安装（--registry 避免网络问题）
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
```

**验证版本**：`openclaw --version` 和 `cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"`

### 诊断步骤
```bash
# 1. 查看 openclaw 版本
openclaw --version

# 2. 看进程状态
ps aux | grep "openclaw" | grep -v grep

# 3. 检查端口监听
curl -s --connect-timeout 5 "http://localhost:18789/health"

# 4. 检查 npm 包完整性
ls ~/.npm-global/lib/node_modules/openclaw/dist/index.js
cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"

# 5. 查看 plugin-runtime-deps 版本
ls ~/.openclaw/plugin-runtime-deps/

# 6. 检查 npm 锁冲突
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete

# 7. 强制刷新日志
kill -USR2 <pid>
tail -20 ~/.openclaw/logs/gateway.log

# 8. 查看 /tmp/openclaw/ 下的实时日志（JSON 格式）
tail -f /tmp/openclaw/openclaw-$(date +%Y-%m-%d).log
```

### 修复方案

**方案 A：清理后重启（推荐用于 2026.4.24 及之前版本）**
```bash
# 清空错误日志（保留文件避免权限问题）
truncate -s 0 ~/.openclaw/logs/gateway.err.log

# 删除 npm 缓存锁
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete
find ~/.openclaw/plugin-runtime-deps/ -name ".openclaw-npm-cache" -type d -exec rm -rf {} + 2>/dev/null

# 杀掉旧进程（注意：2026.4.27 版本进程杀掉后还会重启，需先卸载）
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：降级回 2026.4.25（2026.4.27 专用）**
```bash
# 杀掉所有进程
pkill -9 -f "openclaw" 2>/dev/null
rm -rf ~/.npm-global/lib/node_modules/openclaw
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：跳过飞书 channel 加速启动**
飞书 bot probe 超时（axios 30s × 4 accounts = 2 分钟延迟）：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['channels']['feishu']['enabled'] = False
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2, ensure_ascii=False)
```
重启后 gateway 约 3 分钟可 ready（vs 10 分钟带飞书）。

**方案 C：手动触发 npm 安装（减少重复安装）**
让 gateway 跑完一次后，下次重启会复用缓存。但 npm ENOTEMPTY 问题会反复出现，每次都要清锁。

### launchd 管理
```bash
# 查看状态
launchctl list | grep openclaw

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist

# 日志路径
~/.openclaw/logs/gateway.log（主日志）
~/.openclaw/logs/gateway.err.log（错误日志）
/tmp/openclaw/openclaw-YYYY-MM-DD.log（实时 JSON 日志）
```

### OpenClaw vs Hermes 定时任务关系
- OpenClaw gateway 内置 cron 调度（job_id 如 `7c55dfb106c3`）
- Hermes 有独立的 cron 系统（`~/.hermes/cron/jobs.json`）
- 两者可以并存；迁移时暂停 OpenClaw 任务，避免重复执行：
  `cronjob pause <job_id>`

---

## 性能优化（app.py）

**问题**：158+ 文件时，每次请求全量读文件，首页/图谱页慢到无法操作。

**优化方案**（app.py 已实现）：
1. 启动时预热：`app/__init__` 时 `_load_cards()` + `_build_graph()` 预加载，197张卡约0.1秒
2. 文件 mtime 检测：`_scan_mtimes()` 追踪文件变更，TTL 从4小时改为文件驱动刷新
3. 后台异步重建：缓存过期时返回旧数据同时后台线程重建，不阻塞请求
4. 图数据独立缓存：`build_graph()` 结果存入 `_cache['graph']`

**重启生效**：`kill <PID> && python3 app.py`

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是 Hermes Agent 的知识管理员。执行结构层提炼任务，从归档日志中提炼结构化知识卡片。

## 凭证
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: jYFMuuszh8g5h1bvb32Q1dcywimWG1TH
- 飞书会话: oc_1e92781cdf15f71fad7dcd8d5d386e76

## 执行步骤

### Step 1: 确定日期
当天日期（如：2026-04-26）

### Step 2: 读取归档日志
读取 `/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md` 和 `Hermes_YYYY-MM-DD_会话复盘.md`

### Step 3: 识别值得提炼的内容
结合当日 Hermes cron 任务执行内容，识别：
- 1-5个值得结构化的核心概念或方法论
- 有价值的工作流程、问题解决思路
- 可以复用的经验教训

### Step 4: 写入知识卡片

**写入路径：**
- 概念卡：`/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_XXX.md`
- 方法论：`/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_XXX.md`

**卡片格式：**
```
# {概念/方法论名称}

**背景**
{这段知识产生的上下文}

**核心洞察**
{最核心的内容，300-800字}

**对Jerry的意义**
{这个知识对他的价值}

**延伸思考**
{可以应用的其他场景}

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
报告提炼了几张卡片及标题。

### 重要规则
- **禁止 [SILENT]**
- 每张卡片300-800字
- 优先关注：小红书运营经验、飞书 API 使用技巧、Cron 任务管理、内容创作方法论

## Response

**结构层提炼完成 | 2026-05-06**

✅ **5张概念卡已写入知识库 + Git push完成**

| # | 卡片标题 | 核心概念 |
|---|---------|---------|
| 1 | 中国阻断禁令 | 中国以阻断法令反制美国伊朗石油制裁 |
| 2 | 情绪经济 | 87%消费者有明显压力，情绪消费万亿赛道 |
| 3 | 先进封装崛起 | 先进封装超越制程节点成为AI芯片新战场 |
| 4 | 腾讯混元12亿次日调用 | 国产大模型首在全球调用量维度领先 |
| 5 | AI智能体元年 | 智能体从感知到执行自主闭环的新阶段 |

**执行摘要：**
- 清理了之前运行遗留的5张重复卡片（无`Hermes_`前缀版本）
- Git commit `7ab7e2d`，已推送至 `origin/main`
- 飞书简报已发送至 `oc_1e92781cdf15f71fad7dcd8d5d386e76`
- 无小红书内容输出（今日财经/地缘政治/半导体方向不适合种草）

---

## 来源任务
- job_id: 11859c122c65 | 任务名: MLCC产业链晨报 10:15
---
# Cron Job: MLCC产业链晨报 10:15

**Job ID:** 11859c122c65
**Run Time:** 2026-05-06 12:26:34
**Schedule:** 25 12 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是MLCC产业链研究助手。请用 mmx search query 命令搜索MLCC/被动元件产业动态，生成MLCC产业链晨报。

## 搜索命令
```bash
mmx search query --q "MLCC 村田 被动元件 市场动态 2026" --output json
mmx search query --q "积层陶瓷电容 扩产 供需 2026" --output json
```

## 关键词（用于匹配和筛选）
村田, Murata, 太阳诱电, Taiyo Yuden, 三星电机, 国巨, Yageo, 华新科, 风华高科, 三环集团, MLCC, 被动元件

## 执行步骤
1. 用 subprocess 运行 mmx search query，合并多关键词搜索结果
2. 解析 JSON，按相关性取前 8-10 条
3. 清洗 snippet 中的 HTML 标签

## 输出格式（每条）
标题：[文章标题]
摘要：[二三十字核心内容摘要]
来源：[链接]
---
（空行分隔）

## 最后
今日关注：[一句话产业趋势点评]

## 发送
发送格式化简报到飞书：chat_id = oc_1e92781cdf15f71fad7dcd8d5d386e76

飞书凭证：APP_ID=cli_a96f4f328a785cd2，APP_SECRET=jYFMuuszh8g5h1bvb32Q1dcywimWG1TH

## Response

✅ MLCC产业链晨报已发送至飞书（code=0 成功）。

---

📊 **MLCC产业链晨报 | 2026年5月6日**

**【1】2026 被动元器件市场：缺货、涨价、替代趋势全解析**
缺货蔓延、涨价成风，高端料号"一货难求"。高容高压MLCC、车规级电阻、高端电感、钽电容最为紧张，普通0603/0402规格相对稳定。
来源：https://www.sohu.com/a/1013856202_122694876
---

**【2】2026年MLCC发展白皮书**
MLCC（多层陶瓷电容器）是电子信息产业底层基石，素有"电子工业大米"之称，支撑滤波、去耦、储能等核心功能。
来源：https://www.sohu.com/a/1016796734_120244537
---

**【3】上下游精准牵手，陶瓷电容器大会在穗举办**
2026年4月24日中国陶瓷电容器及材料大会暨MLCC行业年会在广州举办，推动产业链供需对接。
来源：https://so.html5.qq.com/page/real/search_news?docid=70000021_21869eb3f0214152
---

**【4】村田MLCC酝酿涨价**
村田或于4月底公布MLCC新定价策略。3月已率先针对部分电感涨价15%~35%，MLCC市场关注成本支撑与需求博弈。
来源：https://www.sohu.com/a/1010288582_121124376
---

**【5】AI浪潮下，高端MLCC国产替代加速**
AI服务器、AI PC等AI终端市场强劲拉动，MLCC需求量爆发，高端MLCC国产替代进程加速。
来源：https://www.sekorm.com/news/538070291.html
---

**【6】被动元器件涨价潮：村田宣布4月涨价**
村田正式对多层片式铁氧体磁珠、多层铁氧体功率电感、多层RF电感及共模扼流圈涨价，银价飙升为核心原因。
来源：https://so.html5.qq.com/page/real/search_news?docid=70000021
* * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

读取 /tmp/xhs_topic_state_1.json，如果 "chosen" 字段有值，
执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_video_gen.py 1

round=1，第一轮
如果 "chosen" 为空或文件不存在，什么都不做，直接退出。

## Response

脚本执行成功，10张图全部生成完毕。

**执行摘要：**
- **状态文件**：`/tmp/xhs_topic_state_1.json` 存在且 `chosen` 字段有值
- **话题**："AI创作应否拥有著作权?争议背后的权利与伦理困境..."
- **Bug 修复**：`scene_prompts` 变量在第219行被使用，但定义在第268行——`UnboundLocalError`。将 `scene_prompts` 定义移至图片生成循环之前，删除了冗余的重复定义。
- **图片生成**：10/10 张图全部生成成功，保存至 `/tmp/xhs_gen_1/`
- **脚本退出码**：0，执行完成

---

## 来源任务
- job_id: 6ce527bbbff4 | 任务名: 半导体热点晨报 11:15
---
# Cron Job: 半导体热点晨报 11:15

**Job ID:** 6ce527bbbff4
**Run Time:** 2026-05-06 13:11:31
**Schedule:** 5 13 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是半导体产业研究助手。请用 mmx search query 命令搜索半导体热点新闻，生成半导体热点晨报。

## 搜索命令
```bash
mmx search query --q "半导体 芯片 晶圆代工 先进制程 2026" --output json
mmx search query --q "TSMC ASML 半导体国产化 2026" --output json
```

## 关键词（用于匹配和筛选）
芯片, semiconductor, 晶圆代工, foundry, 先进制程, 光刻机, ASML, TSMC, 三星晶圆, 中芯国际, 先进封装, HBM, 芯片法案, 半导体国产化, AI芯片

## 执行步骤
1. 用 subprocess 运行 mmx search query，合并多关键词搜索结果
2. 解析 JSON，按相关性取前 8-10 条
3. 清洗 snippet 中的 HTML 标签

## 输出格式（每条）
标题：[文章标题]
摘要：[二三十字核心内容摘要]
来源：[链接]
---
（空行分隔）

## 最后
今日关注：[一句话产业趋势点评]

## 发送
发送格式化简报到飞书：chat_id = oc_1e92781cdf15f71fad7dcd8d5d386e76

飞书凭证：APP_ID=cli_a96f4f328a785cd2，APP_SECRET=jYFMuuszh8g5h1bvb32Q1dcywimWG1TH

## Response

📊 **半导体热点晨报 | 2026年5月6日**

---

**标题：** 2026先进封装与光互连引领AI半导体新周期
**摘要：** SEMICON China 2026深度观察，先进封装与光互连点燃AI半导体新引擎，AI算力需求爆发驱动新增长周期。
**来源：** https://www.sohu.com/a/1017511214_121838582

---

**标题：** 先进封装成AI芯片新战场，国内产业链迎来机会
**摘要：** 台积电法说会将大尺寸CoWoS封装列为核心战略，2026/2027年分别推出5.5倍和9.5倍光罩尺寸方案，支持12层HBM和多芯片集成。
**来源：** https://www.szguiyu.com/article.html

---

**标题：** 晶圆代工角逐1nm制程，摩尔定律终极考场
**摘要：** 2nm战鼓擂响，半导体行业目光投向1nm（A10）节点，芯片制造工艺从纳米时代迈向埃米时代的分水岭。
**来源：** https://ee.ofweek.com/tag-台积电3nm.HTM

---

**标题：** ASML新型光刻机28亿一台，台积电暂不引进
**摘要：** 台积电联席副首席运营官称2029年前不采购ASML新一代High-NA EUV光刻机，称旧设备仍够用，ASML美股应声下跌超4%。
**来源：** https://so.html5.qq.com/page/real/search_news?docid=70000021_05369e9f93d71152

---

**标题：** SEMI预计2023-2026年全球200mm晶圆厂产能增加14%
**摘要：** SEMI报告指出，到2026年全球将新增12个200mm晶圆厂，产能增加14%，半导体制造持续扩张。
**来源：** https://www.toutiao.com/topic/7504149547132815369

---

**标题：** 三星得州芯片工厂投资超1800亿，推迟至2026年投产
**摘要：** 三星美国得克萨斯州泰勒工厂因市场增势放缓调整建设速度，量产时间从2024年底推迟至2026年，已获美国政府64亿美元芯片法案补贴。
**来源：** https://m.zmtpc.com/article/21836.html

---

**标题：** 晶圆代工2026年产值有望逼近2500亿美元
**摘要：** 2025年产值突破2000亿美元，预计2026年同比增长23.5%逼近2500亿美元，AI应用成核心增长动力，成熟制程持续承压。
**来源：** https://www.toutiao.com/article/7623977190832538175

---

**标题：** 英飞凌功率半导体涨价在即，AI数据中心需求强劲
**摘要：** 英飞凌宣布自2026年4月1日起上调功率开关与相关芯片价格，因供给持续吃紧及原材料成本攀升，AI数据中心需求为主要驱动力。
**来源：** https://ee.ofweek.com/tag-国产机.HTM

---

🌐 **今日关注：** AI驱动先进封装需求爆发，台积电CoWoS扩产与三星晶圆代工延期形成鲜明对比，2026年半导体产业链分化格局加速。

---

## 来源任务
- job_id: 35f355d04bd1 | 任务名: 财经新闻推送 14:30
---
# Cron Job: 财经新闻推送 14:30

**Job ID:** 35f355d04bd1
**Run Time:** 2026-05-06 15:36:33
**Schedule:** 35 15 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是财经新闻助手。请用 mmx search query 命令搜索财经新闻，生成财经热点简报。

## 搜索命令
```bash
mmx search query --q "财经新闻 股市 经济政策 2026" --output json
mmx search query --q "economy stock market news China 2026" --output json
```

## 执行步骤
1. 用 subprocess 运行 mmx search query，合并 2-3 个关键词的搜索结果
2. 解析 JSON 结果，按相关性取前 8-10 条
3. 清洗 snippet 中的 HTML 标签

## 输出格式（每条）
标题：[文章标题]
摘要：[二三十字核心内容摘要]
来源：[链接]
---
（空行分隔）

## 最后
今日关注：[一句话点评当天最重要新闻的意义]

## 发送
发送格式化简报到飞书：chat_id = oc_1e92781cdf15f71fad7dcd8d5d386e76

飞书凭证：APP_ID=cli_a96f4f328a785cd2，APP_SECRET=jYFMuuszh8g5h1bvb32Q1dcywimWG1TH

## Response

📊 **财经热点简报
* * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_topic_search.py 2

round=2，第二轮
状态文件: /tmp/xhs_topic_state_2.json
飞书格式: "🔥 今日小红书话题候选（第二轮，回复数字选择）\n1. ...\n回复 1/2/3（第二轮，22:00 出视频）"

## Response

第二轮话题已发送飞书，等待用户回复选择。

**候选话题：**
1. **达沃斯论坛热议AI社会影响** — AGI 5年内实现可能性，AI如何重塑工作与人类未来
2. **AI创作应否拥有著作权？** — 独创性争议、权利与伦理困境
3. **达沃斯论坛热议AI就业变革** — 2026达沃斯聚焦AI对就业和技能的变革影响

---

## 来源任务
- job_id: 123a75f4f1f6 | 任务名: xhs-video-gen-r2
---
# Cron Job: xhs-video-gen-r2

**Job ID:** 123a75f4f1f6
**Run Time:** 2026-05-06 17:30:58
**Schedule:** 30 17 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

读取 /tmp/xhs_topic_state_2.json，如果 "chosen" 字段有值，
执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_video_gen.py 2

round=2，第二轮
如果 "chosen" 为空或文件不存在，什么都不做，直接退出。

## Response

[SILENT]

---

## 来源任务
- job_id: 9e21f79ee5f5 | 任务名: AI热点推送 18:00
---
# Cron Job: AI热点推送 18:00

**Job ID:** 9e21f79ee5f5
**Run Time:** 2026-05-06 18:41:06
**Schedule:** 40 18 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是AI科技新闻助手。请用 mmx search query 命令搜索AI热点新闻，生成AI热点简报。

## 搜索命令
```bash
mmx search query --q "AI人工智能最新新闻 2026" --output json
mmx search query --q "OpenAI GPT Claude 最新消息 2026" --output json
```

## 执行步骤
1. 用 subprocess 运行 mmx search query，合并 2-3 个关键词的搜索结果
2. 解析 JSON 结果，按相关性取前 8-10 条
3. 清洗 snippet 中的 HTML 标签

## 输出格式（每条）
标题：[文章标题]
摘要：[二三十字核心内容摘要]
来源：[链接]
---
（空行分隔）

## 最后
今日关注：[一句话点评当天最重要AI新闻的意义]

## 发送
发送格式化简报到飞书：chat_id = oc_1e92781cdf15f71fad7dcd8d5d386e76

飞书凭证：APP_ID=cli_a96f4f328a785cd2，APP_SECRET=jYFMuuszh8g5h1bvb32Q1dcywimWG1TH

## Response

**🤖 AI科技热点简报 · 2026年5月6日**

---

【1】苹果Siri AI功能跳票：2.5亿美元和解诉讼
摘要：苹果Siri语音助手AI升级推迟，以2.5亿美元和解股东诉讼，AI落地承诺再度延期引发市场担忧。
来源：https://k.sina.com.cn/article_7857201856_1d45362c0019054nmy.html

---

【2】OpenAI发布GPT-5.5：多任务能力获突破性进展
摘要：GPT-5.5在编程、复杂任务规划和多工具协同方面实现突破，已向付费用户开放。
来源：https://www.cnblogs.com/terrorpig/p/19946914

---

【3】斯坦福大学发布《2026年人工智能指数报告》
摘要：报告显示美国仍领跑AI模型开发，中国2025年贡献30个杰出模型，差距持续收窄。
来源：https://so.html5.qq.com/page/real/search_news?docid=70000021_13769ef2ac364452

---

【4】大语言模型军备竞赛：2026年离AGI还有多远？
摘要：MoE稀疏激活、长上下文等技术推动AGI路径探索，参数规模突破10万亿成焦点。
来源：https://blog.csdn.net/IRpickstars/article/details/158261271

---

【5】深圳发布AI终端产业计划：2026年规模剑指万亿
摘要：深圳聚焦9大AI终端品类，力争2026年人工智能终端产业规模达到1万亿元。
来源：https://www.toutiao.com/topic/7499360817469622335/

---

【6】OpenAI硬件战略调整：2026年起自研AI芯片
摘要：OpenAI组建20人芯片团队，引入AMD MI300并与博通、台积电合作，计划2026年生产定制AI芯片。
来源：https://www.ennews.com/news-85242.html

---

【7】世界模型成AGI核心：2026年自动驾驶与机器人商业化拐点
摘要：世界模型成为产业焦点，2026年自动驾驶与工业垂直场景将率先迎来商业化拐点。
来源：https://www.sohu.com/a/1003261415_121924584

---

【8】面壁智能获数亿元新投资：大模型进入中场战事
摘要：面壁智能MiniCPM系列在汽车、手机、AIPC等领域规模化落地，AI端侧爆发态势明显。
来源：https://so.html5.qq.com/page/real/search_news?docid=70000021