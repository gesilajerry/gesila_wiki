# Hermes Cron 推送记录 | 2026-05-26

## 来源任务

- job_id: 3c9cf2c9d74e | 任务名: 财经新闻推送 20:30
  文件: 2026-05-26_21-11-10.md

- job_id: d2e8d49ca8ba | 任务名: MLCC产业链晚报 22:00
  文件: 2026-05-26_20-02-51.md

- job_id: 4414069ce615 | 任务名: 原子级制造科研晚报 19:10
  文件: 2026-05-26_19-11-47.md

- job_id: 7eabbc256ad5 | 任务名: xhs-vangogh-r2
  文件: 2026-05-26_17-35-17.md

- job_id: 1e7dc62ef5dc | 任务名: xhs-vangogh-r5
  文件: 2026-05-26_14-05-08.md

- job_id: 793ce3c39848 | 任务名: Hermes 结构层提炼 11:30
  文件: 2026-05-26_11-34-15.md

- job_id: 6ce527bbbff4 | 任务名: 半导体热点晨报 11:15
  文件: 2026-05-26_11-03-50.md

- job_id: acf7034225ef | 任务名: xhs-vangogh-r1
  文件: 2026-05-26_10-08-09.md

- job_id: 11859c122c65 | 任务名: MLCC产业链晨报 10:15
  文件: 2026-05-26_10-03-45.md

- job_id: dd15667811fc | 任务名: 财经新闻推送 08:30
  文件: 2026-05-26_09-12-16.md

- job_id: 70c597681993 | 任务名: gesila-compile-daily
  文件: 2026-05-26_09-01-06.md

- job_id: c620d75134bb | 任务名: xhs-vangogh-r4
  文件: 2026-05-26_07-05-27.md

- job_id: c88c6506add2 | 任务名: AI热点推送 04:00
  文件: 2026-05-26_05-01-39.md

- job_id: 5c0b040cdb63 | 任务名: 半导体热点晚报 23:00
  文件: 2026-05-25_23-51-33.md

- job_id: 4325b9d1d400 | 任务名: Wiki Git Pull
  文件: 2026-05-25_23-08-02.md

- job_id: 475e0d05da28 | 任务名: Hermes 结构层提炼 23:00
  文件: 2026-05-25_23-07-46.md

- job_id: fdba14bc8010 | 任务名: Wiki Git Push
  文件: 2026-05-25_23-02-23.md

- job_id: 037201748e42 | 任务名: AI热点推送 22:00
  文件: 2026-05-25_22-49-17.md

- job_id: d4fde929cb2e | 任务名: xhs-vangogh-r3
  文件: 2026-05-25_22-45-58.md

---

## 财经新闻推送 20:30 (job_id: 3c9cf2c9d74e)

# Cron Job: 财经新闻推送 20:30

**Job ID:** 3c9cf2c9d74e
**Run Time:** 2026-05-26 21:11:10
**Schedule:** 10 21 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

**再次实测确认（2026-05-26）**：用 `-t 24h` 执行三组搜索（半导体/芯片/出口管制、晶圆代工/台积电/中芯国际、半导体国产化/AI芯片），全部8条结果均为歌曲《24H》、尿量正常值等医疗/音乐内容，无一与半导体相关。改用日期约束 `"2026年05月26日"` 直接嵌入 query 后，三组搜索均返回半导体产业当日新闻。**结论：`-t 24h` 在任何关键词组合下均不可用，必须用 query 内嵌日期方式。**

**推荐的标准搜索模式（cron/自动化场景）**：
```python
from datetime import datetime
today = datetime.now().strftime('%%Y年%%m月%%d日')
# 三组并发搜索
kw_groups = [
    f"半导体 芯片 美国出口管制 {today}",
    f"晶圆代工 台积电 中芯国际 {today}",
    f"半导体国产化 芯片设计 AI芯片 {today}",
]
```

## mmx text chat 正确用法

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

## execute_code vs terminal 执行选择

> **重要**：在 `execute_code` 沙箱（Python 3.14）中，用 heredoc 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。

**正确做法**：用 `write_file` 先写脚本到 `/tmp/`，再用 `terminal` 执行：
```python
# ❌ execute_code heredoc 中会报 SyntaxError
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF

# ✅ 正确：先 write_file 到 /tmp/，再 terminal 执行
write_file("/tmp/publish.py", script_content)
terminal("python3 /tmp/publish.py")
```

**适用场景**：所有涉及多行字符串（含 regex、JSON 或复杂文本）的 Python 脚本，在 cron 或需要写文件执行的场景中，都应该用 write_file + terminal 方式。

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行
>
> ### 额外发现：curly quotes（"" 中文弯引号）导致 SyntaxError（2026-05-25 实测）
>
> 在 `execute_code` 沙箱（Python 3.14）中，即使弯引号出现在字符串**内部**（不在行首），也会触发 `SyntaxError: invalid syntax`：
> ```python
> title = "特斯拉FSD"监督版"正式入华"
> # SyntaxError: invalid syntax（行内任意位置有 "" 都会报）
> ```
> **解决方案**：将 `""` 替换为中文直角引号 `「」` 或纯 ASCII 替代品（如 `/`），不要保留弯引号：
> ```python
> title = "特斯拉FSD「监督版」正式入华"  # ✅ 可用
> title = "特斯拉FSD/监督版/正式入华"     # ✅ 替代方案
> ```
> 此问题同样会影响飞书 post 消息 content 中的文本，确认文本不含弯引号后再发送。

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你财经新闻助手。请用 mmx search 命令搜索过去24小时内发生的财经新闻，生成财经今日热点简报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "宏观经济 货币政策 金融市场"
```
```bash
mmx search query -t 24h --limit 8 "A股 上市公司 财报"
```
```bash
mmx search query -t 24h --limit 8 "房地产 股市 基金"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【市场动态】【行业观察】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
📈 财经晚报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【市场动态】
（3-5条，每条：标题 + 摘要）

【行业观察】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（综合管理-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "财经晚报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 2, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

📈 **财经晚报 | 2026年05月26日**

---

**【今日要闻】**

1. **央行月末净投放6055亿元** 5月25日央行开展6000亿元1年期MLF操作及2580亿元7天期逆回购操作，因到期5000亿元MLF及10亿元逆回购，实现单日净投放3570亿元。专家表示央行将继续多措并举确保流动性充裕。

2. **欧洲央行暗示6月或继续收紧政策** 欧洲央行首席经济学家连恩表示，通胀面临上行压力，可能会在6月进一步上调通胀预期，暗示无需纠正市场对6月加息的猜测，货币政策方向趋紧。

3. **美伊谈判预期升温国际油价大跌** 有迹象显示美伊正朝着达成重开霍尔木兹海峡协议取得进展，美原油下跌近6%，盘中一度跌破90美元/桶关口至89.40美元，地缘风险缓和推动全球避险资产价格回落。

4. **美股因阵亡将士纪念日休市** 5月25日美股休市一日，布伦特原油期货、贵金属期货等提前收市，洲际交易所整体金融市场交投清淡。

---

**【市场动态】**

1. **A股融资余额历史首次突破2.9万亿元** 截至5月25日A股融资余额达29035.40亿元，单日增加约253.19亿元，2026年以来累计增长3793.84亿元，融资融券活跃度持续提升。

2. **A股早盘震荡整理上证指数跌0.84%** 5月26日早盘上证指数报4117.85点下跌0.84%，深证成指跌0.87%，创业板指跌0.51%，非银金融板块表现稳健涨幅0.24%逆市走强。

3. **贵金属板块集体拉升招金黄金涨停** 受美元走弱及美伊谈判预期影响，黄金价格回升至4575美元/盎司附近，A股贵金属板块大幅走强，招金黄金涨停，西部黄金涨超7%。

4. **国芯科技抗量子金融POS芯片量产超10万颗** 国芯科技在业绩说明会上表示，基于自主开源RISC-V架构的抗量子金融POS芯片已实现超10万颗量产，深度融合AI与量子安全技术，今年争取扭亏为盈。

5. **瑞威资管港股收盘大涨16.79%** 港股瑞威资管报收1.53港元上涨16.79%，当日成交额9.96万港元，中单资金净流入1.72万港元。

---

**【行业观察】**

1. **房地产ETF普遍小幅回调** 南方中证全指房地产ETF跌0.94%成交1.79亿元，华夏房地产ETF开盘跌0.30%，银华房地产ETF午盘跌1.48%，板块延续震荡整理格局。

2. **城投债ETF连续三日净流入16.94亿元** 城投债ETF主力资金净流入3.46亿元，近3日合计流入16.94亿元居全市场第一梯队，显示机构资金在市场波动中向债市避险。

3. **河南「十五五」推进现房销售与REITs创新** 河南发布"十五五"规划推进现房销售，同时中金开元旅业REIT已申报，底层资产为浙三地开元名都酒店，估值18.04亿元，房地产融资渠道持续创新。

4. **京基智农股价连续5天下跌累计跌幅8.48%** 京基智农5月26日跌1.04%报13.28元，已连续5天下跌，南方基金旗下1只产品持有316.5万股浮亏389.3万元，总市值70.42亿元。

---

**【今日关注】**

月末资金面与地缘政治双重信号值得高度关注。央行单日净投放6055亿元，叠加MLF超额续做，流动性呵护意图明确；而美伊谈判突破预期推动原油大跌6%、黄金回升，则显示地缘风险正在快速消退。对于A股而言，融资余额突破2.9万亿创历史新高，本身是市场信心升温的信号，但需警惕杠杆资金加速入场与大盘震荡整理的短期矛盾。操作上，当前宜保持攻守兼备的配置思路：流动性宽松受益的非银金融与贵金属板块可继续持有，同时对房地产等仍处调整期的行业保持审慎。

---

✅ **已发布至 Koala 博客（综合管理-专业知识）**
✅ **已发送至飞书群**


---

## MLCC产业链晚报 22:00 (job_id: d2e8d49ca8ba)

# Cron Job: MLCC产业链晚报 22:00

**Job ID:** d2e8d49ca8ba
**Run Time:** 2026-05-26 20:02:51
**Schedule:** 0 20 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

**再次实测确认（2026-05-26）**：用 `-t 24h` 执行三组搜索（半导体/芯片/出口管制、晶圆代工/台积电/中芯国际、半导体国产化/AI芯片），全部8条结果均为歌曲《24H》、尿量正常值等医疗/音乐内容，无一与半导体相关。改用日期约束 `"2026年05月26日"` 直接嵌入 query 后，三组搜索均返回半导体产业当日新闻。**结论：`-t 24h` 在任何关键词组合下均不可用，必须用 query 内嵌日期方式。**

**推荐的标准搜索模式（cron/自动化场景）**：
```python
from datetime import datetime
today = datetime.now().strftime('%%Y年%%m月%%d日')
# 三组并发搜索
kw_groups = [
    f"半导体 芯片 美国出口管制 {today}",
    f"晶圆代工 台积电 中芯国际 {today}",
    f"半导体国产化 芯片设计 AI芯片 {today}",
]
```

## mmx text chat 正确用法

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

## execute_code vs terminal 执行选择

> **重要**：在 `execute_code` 沙箱（Python 3.14）中，用 heredoc 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。

**正确做法**：用 `write_file` 先写脚本到 `/tmp/`，再用 `terminal` 执行：
```python
# ❌ execute_code heredoc 中会报 SyntaxError
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF

# ✅ 正确：先 write_file 到 /tmp/，再 terminal 执行
write_file("/tmp/publish.py", script_content)
terminal("python3 /tmp/publish.py")
```

**适用场景**：所有涉及多行字符串（含 regex、JSON 或复杂文本）的 Python 脚本，在 cron 或需要写文件执行的场景中，都应该用 write_file + terminal 方式。

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行
>
> ### 额外发现：curly quotes（"" 中文弯引号）导致 SyntaxError（2026-05-25 实测）
>
> 在 `execute_code` 沙箱（Python 3.14）中，即使弯引号出现在字符串**内部**（不在行首），也会触发 `SyntaxError: invalid syntax`：
> ```python
> title = "特斯拉FSD"监督版"正式入华"
> # SyntaxError: invalid syntax（行内任意位置有 "" 都会报）
> ```
> **解决方案**：将 `""` 替换为中文直角引号 `「」` 或纯 ASCII 替代品（如 `/`），不要保留弯引号：
> ```python
> title = "特斯拉FSD「监督版」正式入华"  # ✅ 可用
> title = "特斯拉FSD/监督版/正式入华"     # ✅ 替代方案
> ```
> 此问题同样会影响飞书 post 消息 content 中的文本，确认文本不含弯引号后再发送。

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是MLCC产业链研究助手。请用 mmx search 命令搜索过去24小时内发生的MLCC/被动元器件产业动态，生成MLCC产业晚报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "MLCC 被动元件 村田 太阳诱电"
```
```bash
mmx search query -t 24h --limit 8 "MLCC产能 扩产 价格走势"
```
```bash
mmx search query -t 24h --limit 8 "消费电子 汽车电子 MLCC需求"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【产能动态】【市场观察】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
🔋 MLCC产业晚报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【产能动态】
（3-5条，每条：标题 + 摘要）

【市场观察】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "MLCC产业链晚报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 1, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

🔋 MLCC产业晚报 | 2026年05月26日

【今日要闻】
三星电机MLCC三年产能已被预订一空。受AI数据中心建设加速及汽车电子化驱动，未来三年产能全部被客户提前锁定，供需紧张格局难以短期缓解。

MLCC概念股大面积涨停，风华高科五日三板再创新高。5月25日板块整体涨5.16%，风华高科实现五个交易日内第三次涨停，股价报37.62元，距历史高点仅一步之遥，年内累计涨幅131.08%。

村田时隔三年重启涨价，高端MLCC涨幅最高35%。3月17日发布涨价函，针对AI服务器高容MLCC、高端车规级MLCC全面提价15%-35%，新价格已于3月底执行。

【产能动态】
日韩MLCC厂商产能利用率已突破90%。集邦咨询数据显示，2-4月供应商产能稼动率持续回升，村田、三星电机、太阳诱电等接单出货比稳定保持在1以上。

三星电机已与大客户签订约1.5万亿韩元硅电容供应合同，覆盖至2027-2028年，锁定规模大周期长，验证高端被动元件供应将持续偏紧。

新增高端产能有限，扩产速度远追不上需求增速。原厂2026年新增高端MLCC产能十分有限，高容产品交期已明显拉长。

【市场观察】
AI服务器MLCC用量较传统服务器翻3倍。英伟达GB300单台搭载约30000颗MLCC，用量为普通手机30倍；NVL72单机柜MLCC消耗量更高达40000颗以上。

纯电车单车MLCC用量约为燃油车6倍。车用MLCC单车约6000颗，电动化、智能化推动车规级需求持续放量。

消费电子与AI服务器需求呈两极分化。集邦咨询指出，实体AI引爆高端MLCC需求，但消费电子市场仍陷成本寒冬。

【今日关注】
本轮MLCC超级周期与上一轮库存周期有本质区别——需求来自AI数据中心的真实硬件建置和汽车电动化的结构性放量，而非渠道囤货哄抬，周期持续性或远超预期。对国内厂商而言，能否在AI服务器用高容MLCC上实现量产突破，是分享这轮超级周期红利的关键。

---
✅ **已发布到Koala**（市场销售-专业知识）
❌ **飞书发送失败**（cron沙箱环境无FEISHU环境变量，系统将自动通过 deliver 机制投递）


---

## 原子级制造科研晚报 19:10 (job_id: 4414069ce615)

# Cron Job: 原子级制造科研晚报 19:10

**Job ID:** 4414069ce615
**Run Time:** 2026-05-26 19:11:47
**Schedule:** 10 19 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

**再次实测确认（2026-05-26）**：用 `-t 24h` 执行三组搜索（半导体/芯片/出口管制、晶圆代工/台积电/中芯国际、半导体国产化/AI芯片），全部8条结果均为歌曲《24H》、尿量正常值等医疗/音乐内容，无一与半导体相关。改用日期约束 `"2026年05月26日"` 直接嵌入 query 后，三组搜索均返回半导体产业当日新闻。**结论：`-t 24h` 在任何关键词组合下均不可用，必须用 query 内嵌日期方式。**

**推荐的标准搜索模式（cron/自动化场景）**：
```python
from datetime import datetime
today = datetime.now().strftime('%%Y年%%m月%%d日')
# 三组并发搜索
kw_groups = [
    f"半导体 芯片 美国出口管制 {today}",
    f"晶圆代工 台积电 中芯国际 {today}",
    f"半导体国产化 芯片设计 AI芯片 {today}",
]
```

## mmx text chat 正确用法

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

## execute_code vs terminal 执行选择

> **重要**：在 `execute_code` 沙箱（Python 3.14）中，用 heredoc 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。

**正确做法**：用 `write_file` 先写脚本到 `/tmp/`，再用 `terminal` 执行：
```python
# ❌ execute_code heredoc 中会报 SyntaxError
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF

# ✅ 正确：先 write_file 到 /tmp/，再 terminal 执行
write_file("/tmp/publish.py", script_content)
terminal("python3 /tmp/publish.py")
```

**适用场景**：所有涉及多行字符串（含 regex、JSON 或复杂文本）的 Python 脚本，在 cron 或需要写文件执行的场景中，都应该用 write_file + terminal 方式。

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行
>
> ### 额外发现：curly quotes（"" 中文弯引号）导致 SyntaxError（2026-05-25 实测）
>
> 在 `execute_code` 沙箱（Python 3.14）中，即使弯引号出现在字符串**内部**（不在行首），也会触发 `SyntaxError: invalid syntax`：
> ```python
> title = "特斯拉FSD"监督版"正式入华"
> # SyntaxError: invalid syntax（行内任意位置有 "" 都会报）
> ```
> **解决方案**：将 `""` 替换为中文直角引号 `「」` 或纯 ASCII 替代品（如 `/`），不要保留弯引号：
> ```python
> title = "特斯拉FSD「监督版」正式入华"  # ✅ 可用
> title = "特斯拉FSD/监督版/正式入华"     # ✅ 替代方案
> ```
> 此问题同样会影响飞书 post 消息 content 中的文本，确认文本不含弯引号后再发送。

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

[SYSTEM: The user has invoked the "wiki-platform-ops" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: wiki-platform-ops
description: 维基平台运维手册——进程管理、ngrok公网访问、OpenClaw/OpenPro绑定问题、Git Push凭证
---

# Wiki 平台运维手册

## 环境信息
- 项目路径：`/Volumes/256G/gesila_wiki/`
- Flask 端口：5001
- 启动命令：`cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py`
- GitHub 仓库：`https://github.com/gesilajerry/gesila_wiki.git`
- 分支：main

## 公网访问
- ngrok URL：`https://unbribing-undeep-maximina.ngrok-free.dev`
- ngrok token：`3B9DPZE4I2SawazXcJ4xZmdHdrS_25s2AS6UL5Qcqt9Lxb5sj`

### ngrok 配置（macOS native）
**配置文件**：`~/Library/Application Support/ngrok/ngrok.yml`

```yaml
version: "3"
agent:
  authtoken: 3B9DPZE4I2SawazXcJ4xZmdHdrS_25s2AS6UL5Qcqt9Lxb5sj
tunnels:
  wiki:
    proto: http
    addr: 5001
  gateway:
    proto: http
    addr: 18789
```

**启动指定隧道**：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <tunnel-name>`

**常见错误**：错误启动了 `gateway` tunnel（指向 18789/Hermes）而非 `wiki` tunnel（指向 5001/维基）。检查：
```bash
ps aux | grep ngrok | grep -v grep  # 看在跑哪个 tunnel
curl -s http://localhost:4040/api/tunnels  # ngrok web UI API，看实际 addr 是什么
```

### ngrok API（管理隧道）
```bash
# 查看当前 tunnels（包括 name、public_url、实际指向的 addr）
curl -s http://localhost:4040/api/tunnels | python3 -c "import sys,json; [print(t['name'], t['public_url'], '->', t['config']['addr']) for t in json.load(sys.stdin).get('tunnels',[])]"

# 删除某个 tunnel
curl -X DELETE http://localhost:4040/api/tunnels/<name>

# 创建新 tunnel（POST body 必须含 proto + addr，name 可选）
curl -X POST http://localhost:4040/api/tunnels \
  -H 'Content-Type: application/json' \
  -d '{"proto":"http","addr":"5001"}'
```

**故障排查流程：**
1. `ps aux | grep ngrok | grep -v grep` — 看在跑哪个 tunnel
2. `curl http://localhost:4040/api/tunnels` — 验证实际 addr 是否正确（wiki=5001, gateway=18789）
3. 若 tunnel 指向错误地址：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <正确tunnel名>`

### 常见错误：wiki tunnel 指向 gateway 的 18789
**症状**：wiki 的 ngrok URL 能通，但打开是 OpenClaw/Hermes 的页面，而非维基页面。
**原因**：启动了 `gateway` tunnel（18789）而非 `wiki` tunnel（5001）。
**修复**：`kill <ngrok_pid>` 后 `cd ~/Library/Application\ Support/ngrok && ./ngrok start wiki`

## OpenClaw/OpenPro 绑定问题
**症状**：ngrok URL 被 OpenClaw/OpenPro 拦截，跳转到 OpenPro 页面而不是 wiki。

**原因**：ngrok URL 同时绑定了 Wiki 项目和 OpenPro，OpenClaw/OpenPro 优先拦截。

**解决方法**：在 OpenClaw/OpenPro 控制台解除 URL 与 OpenPro 的绑定，只保留 Wiki 项目绑定。

## GitHub 推送凭证（必须）

**当前有效 Token**：`[GITHUB_TOKEN_REDACTED]`（GitHub Classic，40位）

**⚠️ GitHub Classic Token 说明**
- 必须是 Classic Token，不是新版 Fine-grained PAT
- 长度固定 40 位
- 设置路径：GitHub → Settings → Developer settings → Personal access tokens → Classic
- 权限需要 `repo` 全权限

**第一步**：创建 `~/.netrc`（权限必须 600）：
```
machine github.com
login gesilajerry
password [GITHUB_TOKEN_REDACTED]
```

**第二步**：关闭 SSL 验证（必须）：
```bash
git config --global http.sslVerify false
```
原因：macOS 上的 HTTPS 代理做 SSL interception，导致 `git pull/push` 报错 `LibreSSL SSL_connect: SSL`。关闭 SSL 验证绕过代理的证书拦截。

**第三步**：推送：
```bash
cd /Volumes/256G/gesila_wiki
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**❌ 错误做法**：把 token 嵌入 git remote URL（`https://user:token@github.com/`）——token 会出现在进程列表和日志中，不安全。

**✅ 正确做法**：统一用 netrc + credential.helper，remote URL 保持干净。

### Git 双向同步脚本

**脚本路径**：`/Users/mac/.hermes/scripts/git_sync_wiki.py`

**cron job ID**：`61c28cf385bb`
**调度时间**：`0 23 * * *`（每天 23:00）

**用途**：另一个 agent（Trae）往 GitHub 推送后，本地每日 23:00 拉取同步。

**手动触发**：
```bash
/usr/bin/python3 /Users/mac/.hermes/scripts/git_sync_wiki.py
```

**已知踩坑**：
- `git remote URL` 里嵌 token → 进程列表暴露，风险
- netrc 权限不是 600 → git 不读取
- Token 用了旧版（<40位）→ 认证失败（GitHub 已不支持密码认证，必须用 Classic PAT）
- cron prompt 里写明文 token → 触发安全扫描拦截，改用 Python 脚本读取 token

## 目录结构（不动任何文件夹）
```
gesila_wiki/
  01_Inbox/日志/
  02_Knowledge/概念卡/、方法论/、项目复盘/
  03_Output/已发布/、汇报材料/
```
只往现有目录存文件，框架自动识别。

## OpenClaw Gateway 运维

### 症状（版本 2026.4.24 及之前）
- `openclaw status` / `openclaw update check` 等命令全部超时（30s+）
- `curl http://localhost:18789` 返回 connection refused
- `ps aux | grep openclaw` 显示 gateway 进程在跑但 0% CPU
- 日志 `~/.openclaw/logs/gateway.log` 最后一条是 `"starting channels and sidecars..."` 然后停住
- 日志不写，但 `kill -USR2 <pid>` 可强制刷新日志缓冲区

### 根因（版本 2026.4.24 及之前）
OpenClaw gateway 启动时每次都重新安装所有插件的 bundled runtime deps（npm 包），共 66 个包，在 `~/.openclaw/plugin-runtime-deps/` 目录下。
- 正常需 8-12 分钟
- npm ENOTEMPTY 锁冲突时可达 30 分钟+
- 日志停在 "starting..." 是因为在等插件安装完成，并非真卡住
- 日志文件 600MB+ 会导致日志轮转出问题

### 根因（版本 2026.4.25 — 2026.4.26）
**browser 插件的 CDP 初始化会卡死 gateway**（0% CPU，端口监听但不响应 HTTP）。
- 进程 PID 存在，端口 18789 监听，但所有 HTTP 请求超时
- 日志不写入（或写入极慢）
- Dev 模式（`OPENCLAW_PROFILE=dev`）因 `skipBootstrap: true` 能起来

**修复**：在 `openclaw.json` 中关闭 browser 插件：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['plugins']['entries']['browser'] = {'enabled': False}
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2)
```

### 根因（版本 2026.4.27+）— 升级后完全无法启动
**升级到 2026.4.27 后 gateway 卡在 "staging bundled runtime deps" 完全无响应。**

1. **plugin-runtime-deps 安装不完整**：同时存在 4 个版本目录（2026.4.24, 2026.4.25, 2026.4.27, unknown），版本之间文件互相引用导致缺失文件（如 `chutes-oauth-CUO6Kvd3.js`）
2. **npm 网络不通**：gateway 尝试安装 8 个缺失包（@agentclientprotocol/claude-agent-acp@0.31.0, @grammyjs/*, @homebridge/ciao@^1.3.6, acpx@0.6.1, chokidar@^5.0.0, grammy@^1.42.0 等），npm registry 访问超时后一直重试
3. **所有 openclaw CLI 命令全部超时**：`doctor`, `gateway stop`, `dashboard` 等都卡死
4. **launchctl 重启后 gateway 进程存在但不监听端口**：旧进程没真正被杀掉（launchd 管理混乱）

**Gateway token 位置**：`openclaw.json` 里存的是 `"***"`（生成后存在 `~/.openclaw/identity/device-auth.json` 的 `tokens.operator.token` 字段），该 token 是 device-to-device 认证，不用于 HTTP API。

**降级到 2026.4.25 方案**：
```bash
# 杀掉所有 gateway 进程
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 卸载 2026.4.27
rm -rf ~/.npm-global/lib/node_modules/openclaw

# 指定版本安装（--registry 避免网络问题）
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
```

**验证版本**：`openclaw --version` 和 `cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"`

### 诊断步骤
```bash
# 1. 查看 openclaw 版本
openclaw --version

# 2. 看进程状态
ps aux | grep "openclaw" | grep -v grep

# 3. 检查端口监听
curl -s --connect-timeout 5 "http://localhost:18789/health"

# 4. 检查 npm 包完整性
ls ~/.npm-global/lib/node_modules/openclaw/dist/index.js
cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"

# 5. 查看 plugin-runtime-deps 版本
ls ~/.openclaw/plugin-runtime-deps/

# 6. 检查 npm 锁冲突
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete

# 7. 强制刷新日志
kill -USR2 <pid>
tail -20 ~/.openclaw/logs/gateway.log

# 8. 查看 /tmp/openclaw/ 下的实时日志（JSON 格式）
tail -f /tmp/openclaw/openclaw-$(date +%Y-%m-%d).log
```

### 修复方案

**方案 A：清理后重启（推荐用于 2026.4.24 及之前版本）**
```bash
# 清空错误日志（保留文件避免权限问题）
truncate -s 0 ~/.openclaw/logs/gateway.err.log

# 删除 npm 缓存锁
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete
find ~/.openclaw/plugin-runtime-deps/ -name ".openclaw-npm-cache" -type d -exec rm -rf {} + 2>/dev/null

# 杀掉旧进程（注意：2026.4.27 版本进程杀掉后还会重启，需先卸载）
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：降级回 2026.4.25（2026.4.27 专用）**
```bash
# 杀掉所有进程
pkill -9 -f "openclaw" 2>/dev/null
rm -rf ~/.npm-global/lib/node_modules/openclaw
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：跳过飞书 channel 加速启动**
飞书 bot probe 超时（axios 30s × 4 accounts = 2 分钟延迟）：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['channels']['feishu']['enabled'] = False
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2, ensure_ascii=False)
```
重启后 gateway 约 3 分钟可 ready（vs 10 分钟带飞书）。

**方案 C：手动触发 npm 安装（减少重复安装）**
让 gateway 跑完一次后，下次重启会复用缓存。但 npm ENOTEMPTY 问题会反复出现，每次都要清锁。

### launchd 管理
```bash
# 查看状态
launchctl list | grep openclaw

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist

# 日志路径
~/.openclaw/logs/gateway.log（主日志）
~/.openclaw/logs/gateway.err.log（错误日志）
/tmp/openclaw/openclaw-YYYY-MM-DD.log（实时 JSON 日志）
```

### OpenClaw vs Hermes 定时任务关系
- OpenClaw gateway 内置 cron 调度（job_id 如 `7c55dfb106c3`）
- Hermes 有独立的 cron 系统（`~/.hermes/cron/jobs.json`）
- 两者可以并存；迁移时暂停 OpenClaw 任务，避免重复执行：
  `cronjob pause <job_id>`

---

## 性能优化（app.py）

**问题**：158+ 文件时，每次请求全量读文件，首页/图谱页慢到无法操作。

**优化方案**（app.py 已实现）：
1. 启动时预热：`app/__init__` 时 `_load_cards()` + `_build_graph()` 预加载，197张卡约0.1秒
2. 文件 mtime 检测：`_scan_mtimes()` 追踪文件变更，TTL 从4小时改为文件驱动刷新
3. 后台异步重建：缓存过期时返回旧数据同时后台线程重建，不阻塞请求
4. 图数据独立缓存：`build_graph()` 结果存入 `_cache['graph']`

**重启生效**：`kill <PID> && python3 app.py`

---

## 结构层提炼任务（定时 cron 执行）

每晚21:30自动执行，从当日归档日志中提炼知识卡片。

### Step 1: 确定日期
当天日期（如：`2026-05-11`）

### Step 2: 读取归档日志
```
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_会话复盘.md
```

**⚠️ 解析要点：推送日报文件超大（~460KB）**
- 文件过大是因为每个任务块的 Prompt 都完整嵌入了调用的 SKILL.md 内容（整个技能文件）
- 实际 AI 生成的 Response 内容才是需要提取的知识素材
- 搜索 AI 简报内容时用 emoji 标记（如 `"🤖 AI热点简报"`）或 Response 标记来定位
- 各行业简报（MLCC/原子级制造/半导体/财经）的内容在 Response 段落，篇幅通常 2-20KB

### Step 3: 识别值得提炼的内容
识别 1-5 个值得结构化的核心概念或方法论，优先关注：
- 小红书运营经验
- 飞书 API 使用技巧
- Cron 任务管理
- 内容创作方法论
- 产业链洞察

### Step 4: 写入知识卡片
```
/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_名称.md
/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_名称.md
```

格式：
```
# {名称}

**背景**
**核心洞察**
**对Jerry的意义**
**延伸思考**

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
用 tenant_access_token 方式发送飞书 post 消息到 `oc_1e92781cdf15f71fad7dcd8d5d386e76`，报告提炼了几张卡片及标题。

凭证：
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: [FEISHU_SECRET_REDACTED]

---

## 已蒸馏内容模块（gesila_compile）

### 架构
- **原料来源**：`/Volumes/256G/gesila_wiki/`（只读，不触碰原内容）
- **编译输出**：`/Volumes/256G/gesila_compile/`（新增蒸馏内容）
- **Git共享**：`gesila_compile/.git` 是 `gesila_wiki/.git` 的软链接，推送到同一个 GitHub 仓库

### 编译目录结构
```
gesila_compile/
├── sources/     ← 已蒸馏来源（01_Inbox日志 + 02_Knowledge快速引用）
├── entities/    ← 已蒸馏实体（人物/公司/产品，自动合并同名）
├── concepts/    ← 已蒸馏概念（方法论/框架，自动合并同名）
├── syntheses/   ← 已蒸馏综合（跨主题分析，待扩展）
├── distill.py   ← 编译脚本
├── index.md     ← 总入口
└── log.md       ← 操作日志
```

### 蒸馏脚本使用
```bash
# 全量蒸馏（首次运行，对所有历史内容）
cd /Volumes/256G/gesila_compile && python3 distill.py --full

# 增量蒸馏（cron自动跑，只处理新增/修改）
cd /Volumes/256G/gesila_compile && python3 distill.py --incremental
```

### 蒸馏后Git同步（distill.py内置）
`distill.py` 末尾自动调用 `git_sync()`，逻辑：
1. `git status --porcelain` 检测是否有变更
2. `git add .` 暂存所有变更
3. `git commit -m "YYYY-MM-DD HH:MM 自动蒸馏同步"`
4. `git push origin main` 推送到 GitHub

**注意**：`git push` 若被拒绝（remote有新内容），是因为本地和remote有冲突。错误信息：
```
! [rejected] main -> main (fetch first)
error: failed to push some refs to 'https://github.com/gesilajerry/gesila_wiki.git'
```
**解法**：先 `git stash` → `git pull origin main --rebase` → `git stash pop` → `git push origin main`

### Wiki平台展示集成
**已蒸馏页面**：`http://127.0.0.1:5001/compile`

app.py 已添加：
- `COMPILE_ROOT = "/Volumes/256G/gesila_compile"` 配置
- `COMPILE_SUBDIRS` 映射（sources→已蒸馏来源, entities→已蒸馏实体, concepts→已蒸馏概念, syntheses→已蒸馏综合）
- `get_compile_cards()` / `_load_compile_cards()` 缓存加载
- `/compile` 首页路由
- `/compile/category/<cat>` 分类路由
- `/compile/card/<slug>` 详情路由
- search路由已合并 wiki + compile 双搜索

**重启生效**：
```bash
# 先找到旧进程
ps aux | grep "wiki_platform/app\.py" | grep -v grep

# kill后重启
kill <PID>
cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py &
```

### Cron定时任务
- job_id: `70c597681993`
- 时间: `0 9 * * *`（每天09:00）
- 任务: `cd /Volumes/256G/gesila_compile && python3 distill.py --incremental`

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是原子级制造产业与科研动态研究助手。请用 mmx search 命令搜索过去24小时内发生的原子级制造相关动态，生成原子级制造科研晚报，完成后同步发布到飞书和Koala博客。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "原子级制造 团簇离子束 纳米材料"
```
```bash
mmx search query -t 24h --limit 8 "原子级制造 量子点 薄膜沉积"
```
```bash
mmx search query -t 24h --limit 8 "原子级制造 纳米科技 科研突破"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【科研进展】【产业动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
⚛️ 原子级制造科研晚报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【科研进展】
（3-5条，每条：标题 + 摘要）

【产业动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "原子级制造科研晚报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 4, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

⚛️ 原子级制造科研晚报 | 20260526

【今日要闻】
• **华为发布韬定律引燃半导体行情，抓住产业链重构三条主线，关注量子突破预热千亿赛道**——2026年5月25日华为提出韬(τ)定律，全球半导体产业迎来范式转折
• **华为放话2031年芯片到1.4纳米**——华为预测到2031年基于韬定律实现1.4纳米制程同等晶体管密度
• **工信部重磅发布大力推动金刚石、石墨烯发展**——TICNN发布工信部政策，推动原子级材料产业化

【科研进展】
• **我国团队研发新一代大原子模型，性能登顶国际榜单**
• **原子层沉积(ALD)薄膜制备技术**——实现原子级薄膜制造的核心工艺
• **Forge Nano领航粉末ALD新纪元，支持代包覆服务，推动ALD产业化进程**
• **中国造纳米材料亮相全球顶级会议，推动纳米材料进入原子级时代**

【产业动态】
• **半导体开启先进制程与成熟制程共振新周期，国产制造端与设备端有望充分受益**
• **PEEK材料概念异动拉升，肯特股份逼近20%涨停**——高性能PEEK材料在原子级制造领域需求激增
• **华为韬定律凭什么掀翻摩尔牌桌**——华为从几何缩微转向拓扑性能革命，重新定义芯片演进逻辑

【今日关注】
2026年5月25日华为正式提出「韬(τ)定律」，标志着全球半导体产业从「几何缩微」时代迈向「拓扑性能革命」新范式。不同于摩尔定律依赖晶体管密度翻倍，韬定律通过拓扑结构创新突破物理极限，华为预测到2031年可实现1.4纳米制程同等晶体管密度。与此同时，工信部重磅发文推动金刚石、石墨烯等原子级材料产业化，我国团队的新一代大原子模型亦登顶国际榜单——从材料、模型到工艺，原子级制造全链条正在加速闭合。这一轮竞争不再只是设备的比拼，而是从底层物理理解到系统级创新的综合较量。

**标签**：原子级制造 × 韬定律 × 半导体 × 纳米材料 × ALD

---
✅ **已发布**：Koala博客（研发工程-专业知识）+ 飞书推送


---

## xhs-vangogh-r2 (job_id: 7eabbc256ad5)

# Cron Job: xhs-vangogh-r2

**Job ID:** 7eabbc256ad5
**Run Time:** 2026-05-26 17:35:17
**Schedule:** 30 17 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_vangogh_gen.py 2

每天自动运行，梵高融合风图文：10张原创插画 + 专属情绪文案 + BGM + 慢速TTS，产出发飞书。

## Response

✅ **第2篇梵高融合风图文已完成**

**主题：窗景四季**
文案：同一个窗子，住着四个不同的世界。

| 序号 | 场景 | 状态 |
|------|------|------|
| 1/10 | 窗外的春天，柳树抽芽绿色 | ✓ |
| 2/10 | 夏日窗景，梧桐叶遮住了半边天 | ✓ |
| 3/10 | 秋天窗台，银杏叶落满了窗台 | ✓ |
| 4/10 | 冬天窗景，雪花覆盖了所有屋顶 | ✓ |
| 5/10 | 窗外的老槐树，春天满树白花 | ✓ |
| 6/10 | 窗景中的远山，四季颜色不同 | ✓ |
| 7/10 | 雨后窗景，玻璃上挂着水珠 | ✓ |
| 8/10 | 夜晚窗景，对面楼里亮起万家灯火 | ✓ |
| 9/10 | 晨曦中的窗景，第一缕阳光洒入 | ✓ |
| 10/10 | 窗景中的小路，通向未知的地方 | ✓ |

📤 10张图片已发送飞书  
✅ 小红书已发布成功


---

## xhs-vangogh-r5 (job_id: 1e7dc62ef5dc)

# Cron Job: xhs-vangogh-r5

**Job ID:** 1e7dc62ef5dc
**Run Time:** 2026-05-26 14:05:08
**Schedule:** 0 14 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_vangogh_gen.py 5

每天自动运行，梵高融合风图文：10张原创插画 + 专属情绪文案 + 小红书自动发布。

## Response

**小红书梵高融合风图文日更完成**

**主题**：雨后森林 🌿
**生成**：10张原创插画（梵高厚笔触 + 雨后森林意境）

**已发布文案**：
> 雨停了，每一片叶子都在发光。

---
*Daily xhs-vangogh pipeline #5 executed successfully — 10 images generated, posted to 小红书.*


---

## Hermes 结构层提炼 11:30 (job_id: 793ce3c39848)

# Cron Job: Hermes 结构层提炼 11:30

**Job ID:** 793ce3c39848
**Run Time:** 2026-05-26 11:34:15
**Schedule:** 30 11 * * *

## Prompt

[SYSTEM: The user has invoked the "wiki-platform-ops" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: wiki-platform-ops
description: 维基平台运维手册——进程管理、ngrok公网访问、OpenClaw/OpenPro绑定问题、Git Push凭证
---

# Wiki 平台运维手册

## 环境信息
- 项目路径：`/Volumes/256G/gesila_wiki/`
- Flask 端口：5001
- 启动命令：`cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py`
- GitHub 仓库：`https://github.com/gesilajerry/gesila_wiki.git`
- 分支：main

## 公网访问
- ngrok URL：`https://unbribing-undeep-maximina.ngrok-free.dev`
- ngrok token：`3B9DPZE4I2SawazXcJ4xZmdHdrS_25s2AS6UL5Qcqt9Lxb5sj`

### ngrok 配置（macOS native）
**配置文件**：`~/Library/Application Support/ngrok/ngrok.yml`

```yaml
version: "3"
agent:
  authtoken: 3B9DPZE4I2SawazXcJ4xZmdHdrS_25s2AS6UL5Qcqt9Lxb5sj
tunnels:
  wiki:
    proto: http
    addr: 5001
  gateway:
    proto: http
    addr: 18789
```

**启动指定隧道**：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <tunnel-name>`

**常见错误**：错误启动了 `gateway` tunnel（指向 18789/Hermes）而非 `wiki` tunnel（指向 5001/维基）。检查：
```bash
ps aux | grep ngrok | grep -v grep  # 看在跑哪个 tunnel
curl -s http://localhost:4040/api/tunnels  # ngrok web UI API，看实际 addr 是什么
```

### ngrok API（管理隧道）
```bash
# 查看当前 tunnels（包括 name、public_url、实际指向的 addr）
curl -s http://localhost:4040/api/tunnels | python3 -c "import sys,json; [print(t['name'], t['public_url'], '->', t['config']['addr']) for t in json.load(sys.stdin).get('tunnels',[])]"

# 删除某个 tunnel
curl -X DELETE http://localhost:4040/api/tunnels/<name>

# 创建新 tunnel（POST body 必须含 proto + addr，name 可选）
curl -X POST http://localhost:4040/api/tunnels \
  -H 'Content-Type: application/json' \
  -d '{"proto":"http","addr":"5001"}'
```

**故障排查流程：**
1. `ps aux | grep ngrok | grep -v grep` — 看在跑哪个 tunnel
2. `curl http://localhost:4040/api/tunnels` — 验证实际 addr 是否正确（wiki=5001, gateway=18789）
3. 若 tunnel 指向错误地址：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <正确tunnel名>`

### 常见错误：wiki tunnel 指向 gateway 的 18789
**症状**：wiki 的 ngrok URL 能通，但打开是 OpenClaw/Hermes 的页面，而非维基页面。
**原因**：启动了 `gateway` tunnel（18789）而非 `wiki` tunnel（5001）。
**修复**：`kill <ngrok_pid>` 后 `cd ~/Library/Application\ Support/ngrok && ./ngrok start wiki`

## OpenClaw/OpenPro 绑定问题
**症状**：ngrok URL 被 OpenClaw/OpenPro 拦截，跳转到 OpenPro 页面而不是 wiki。

**原因**：ngrok URL 同时绑定了 Wiki 项目和 OpenPro，OpenClaw/OpenPro 优先拦截。

**解决方法**：在 OpenClaw/OpenPro 控制台解除 URL 与 OpenPro 的绑定，只保留 Wiki 项目绑定。

## GitHub 推送凭证（必须）

**当前有效 Token**：`[GITHUB_TOKEN_REDACTED]`（GitHub Classic，40位）

**⚠️ GitHub Classic Token 说明**
- 必须是 Classic Token，不是新版 Fine-grained PAT
- 长度固定 40 位
- 设置路径：GitHub → Settings → Developer settings → Personal access tokens → Classic
- 权限需要 `repo` 全权限

**第一步**：创建 `~/.netrc`（权限必须 600）：
```
machine github.com
login gesilajerry
password [GITHUB_TOKEN_REDACTED]
```

**第二步**：关闭 SSL 验证（必须）：
```bash
git config --global http.sslVerify false
```
原因：macOS 上的 HTTPS 代理做 SSL interception，导致 `git pull/push` 报错 `LibreSSL SSL_connect: SSL`。关闭 SSL 验证绕过代理的证书拦截。

**第三步**：推送：
```bash
cd /Volumes/256G/gesila_wiki
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**❌ 错误做法**：把 token 嵌入 git remote URL（`https://user:token@github.com/`）——token 会出现在进程列表和日志中，不安全。

**✅ 正确做法**：统一用 netrc + credential.helper，remote URL 保持干净。

### Git 双向同步脚本

**脚本路径**：`/Users/mac/.hermes/scripts/git_sync_wiki.py`

**cron job ID**：`61c28cf385bb`
**调度时间**：`0 23 * * *`（每天 23:00）

**用途**：另一个 agent（Trae）往 GitHub 推送后，本地每日 23:00 拉取同步。

**手动触发**：
```bash
/usr/bin/python3 /Users/mac/.hermes/scripts/git_sync_wiki.py
```

**已知踩坑**：
- `git remote URL` 里嵌 token → 进程列表暴露，风险
- netrc 权限不是 600 → git 不读取
- Token 用了旧版（<40位）→ 认证失败（GitHub 已不支持密码认证，必须用 Classic PAT）
- cron prompt 里写明文 token → 触发安全扫描拦截，改用 Python 脚本读取 token

## 目录结构（不动任何文件夹）
```
gesila_wiki/
  01_Inbox/日志/
  02_Knowledge/概念卡/、方法论/、项目复盘/
  03_Output/已发布/、汇报材料/
```
只往现有目录存文件，框架自动识别。

## OpenClaw Gateway 运维

### 症状（版本 2026.4.24 及之前）
- `openclaw status` / `openclaw update check` 等命令全部超时（30s+）
- `curl http://localhost:18789` 返回 connection refused
- `ps aux | grep openclaw` 显示 gateway 进程在跑但 0% CPU
- 日志 `~/.openclaw/logs/gateway.log` 最后一条是 `"starting channels and sidecars..."` 然后停住
- 日志不写，但 `kill -USR2 <pid>` 可强制刷新日志缓冲区

### 根因（版本 2026.4.24 及之前）
OpenClaw gateway 启动时每次都重新安装所有插件的 bundled runtime deps（npm 包），共 66 个包，在 `~/.openclaw/plugin-runtime-deps/` 目录下。
- 正常需 8-12 分钟
- npm ENOTEMPTY 锁冲突时可达 30 分钟+
- 日志停在 "starting..." 是因为在等插件安装完成，并非真卡住
- 日志文件 600MB+ 会导致日志轮转出问题

### 根因（版本 2026.4.25 — 2026.4.26）
**browser 插件的 CDP 初始化会卡死 gateway**（0% CPU，端口监听但不响应 HTTP）。
- 进程 PID 存在，端口 18789 监听，但所有 HTTP 请求超时
- 日志不写入（或写入极慢）
- Dev 模式（`OPENCLAW_PROFILE=dev`）因 `skipBootstrap: true` 能起来

**修复**：在 `openclaw.json` 中关闭 browser 插件：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['plugins']['entries']['browser'] = {'enabled': False}
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2)
```

### 根因（版本 2026.4.27+）— 升级后完全无法启动
**升级到 2026.4.27 后 gateway 卡在 "staging bundled runtime deps" 完全无响应。**

1. **plugin-runtime-deps 安装不完整**：同时存在 4 个版本目录（2026.4.24, 2026.4.25, 2026.4.27, unknown），版本之间文件互相引用导致缺失文件（如 `chutes-oauth-CUO6Kvd3.js`）
2. **npm 网络不通**：gateway 尝试安装 8 个缺失包（@agentclientprotocol/claude-agent-acp@0.31.0, @grammyjs/*, @homebridge/ciao@^1.3.6, acpx@0.6.1, chokidar@^5.0.0, grammy@^1.42.0 等），npm registry 访问超时后一直重试
3. **所有 openclaw CLI 命令全部超时**：`doctor`, `gateway stop`, `dashboard` 等都卡死
4. **launchctl 重启后 gateway 进程存在但不监听端口**：旧进程没真正被杀掉（launchd 管理混乱）

**Gateway token 位置**：`openclaw.json` 里存的是 `"***"`（生成后存在 `~/.openclaw/identity/device-auth.json` 的 `tokens.operator.token` 字段），该 token 是 device-to-device 认证，不用于 HTTP API。

**降级到 2026.4.25 方案**：
```bash
# 杀掉所有 gateway 进程
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 卸载 2026.4.27
rm -rf ~/.npm-global/lib/node_modules/openclaw

# 指定版本安装（--registry 避免网络问题）
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
```

**验证版本**：`openclaw --version` 和 `cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"`

### 诊断步骤
```bash
# 1. 查看 openclaw 版本
openclaw --version

# 2. 看进程状态
ps aux | grep "openclaw" | grep -v grep

# 3. 检查端口监听
curl -s --connect-timeout 5 "http://localhost:18789/health"

# 4. 检查 npm 包完整性
ls ~/.npm-global/lib/node_modules/openclaw/dist/index.js
cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"

# 5. 查看 plugin-runtime-deps 版本
ls ~/.openclaw/plugin-runtime-deps/

# 6. 检查 npm 锁冲突
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete

# 7. 强制刷新日志
kill -USR2 <pid>
tail -20 ~/.openclaw/logs/gateway.log

# 8. 查看 /tmp/openclaw/ 下的实时日志（JSON 格式）
tail -f /tmp/openclaw/openclaw-$(date +%Y-%m-%d).log
```

### 修复方案

**方案 A：清理后重启（推荐用于 2026.4.24 及之前版本）**
```bash
# 清空错误日志（保留文件避免权限问题）
truncate -s 0 ~/.openclaw/logs/gateway.err.log

# 删除 npm 缓存锁
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete
find ~/.openclaw/plugin-runtime-deps/ -name ".openclaw-npm-cache" -type d -exec rm -rf {} + 2>/dev/null

# 杀掉旧进程（注意：2026.4.27 版本进程杀掉后还会重启，需先卸载）
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：降级回 2026.4.25（2026.4.27 专用）**
```bash
# 杀掉所有进程
pkill -9 -f "openclaw" 2>/dev/null
rm -rf ~/.npm-global/lib/node_modules/openclaw
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：跳过飞书 channel 加速启动**
飞书 bot probe 超时（axios 30s × 4 accounts = 2 分钟延迟）：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['channels']['feishu']['enabled'] = False
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2, ensure_ascii=False)
```
重启后 gateway 约 3 分钟可 ready（vs 10 分钟带飞书）。

**方案 C：手动触发 npm 安装（减少重复安装）**
让 gateway 跑完一次后，下次重启会复用缓存。但 npm ENOTEMPTY 问题会反复出现，每次都要清锁。

### launchd 管理
```bash
# 查看状态
launchctl list | grep openclaw

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist

# 日志路径
~/.openclaw/logs/gateway.log（主日志）
~/.openclaw/logs/gateway.err.log（错误日志）
/tmp/openclaw/openclaw-YYYY-MM-DD.log（实时 JSON 日志）
```

### OpenClaw vs Hermes 定时任务关系
- OpenClaw gateway 内置 cron 调度（job_id 如 `7c55dfb106c3`）
- Hermes 有独立的 cron 系统（`~/.hermes/cron/jobs.json`）
- 两者可以并存；迁移时暂停 OpenClaw 任务，避免重复执行：
  `cronjob pause <job_id>`

---

## 性能优化（app.py）

**问题**：158+ 文件时，每次请求全量读文件，首页/图谱页慢到无法操作。

**优化方案**（app.py 已实现）：
1. 启动时预热：`app/__init__` 时 `_load_cards()` + `_build_graph()` 预加载，197张卡约0.1秒
2. 文件 mtime 检测：`_scan_mtimes()` 追踪文件变更，TTL 从4小时改为文件驱动刷新
3. 后台异步重建：缓存过期时返回旧数据同时后台线程重建，不阻塞请求
4. 图数据独立缓存：`build_graph()` 结果存入 `_cache['graph']`

**重启生效**：`kill <PID> && python3 app.py`

---

## 结构层提炼任务（定时 cron 执行）

每晚21:30自动执行，从当日归档日志中提炼知识卡片。

### Step 1: 确定日期
当天日期（如：`2026-05-11`）

### Step 2: 读取归档日志
```
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_会话复盘.md
```

**⚠️ 解析要点：推送日报文件超大（~460KB）**
- 文件过大是因为每个任务块的 Prompt 都完整嵌入了调用的 SKILL.md 内容（整个技能文件）
- 实际 AI 生成的 Response 内容才是需要提取的知识素材
- 搜索 AI 简报内容时用 emoji 标记（如 `"🤖 AI热点简报"`）或 Response 标记来定位
- 各行业简报（MLCC/原子级制造/半导体/财经）的内容在 Response 段落，篇幅通常 2-20KB

### Step 3: 识别值得提炼的内容
识别 1-5 个值得结构化的核心概念或方法论，优先关注：
- 小红书运营经验
- 飞书 API 使用技巧
- Cron 任务管理
- 内容创作方法论
- 产业链洞察

### Step 4: 写入知识卡片
```
/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_名称.md
/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_名称.md
```

格式：
```
# {名称}

**背景**
**核心洞察**
**对Jerry的意义**
**延伸思考**

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
用 tenant_access_token 方式发送飞书 post 消息到 `oc_1e92781cdf15f71fad7dcd8d5d386e76`，报告提炼了几张卡片及标题。

凭证：
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: [FEISHU_SECRET_REDACTED]

---

## 已蒸馏内容模块（gesila_compile）

### 架构
- **原料来源**：`/Volumes/256G/gesila_wiki/`（只读，不触碰原内容）
- **编译输出**：`/Volumes/256G/gesila_compile/`（新增蒸馏内容）
- **Git共享**：`gesila_compile/.git` 是 `gesila_wiki/.git` 的软链接，推送到同一个 GitHub 仓库

### 编译目录结构
```
gesila_compile/
├── sources/     ← 已蒸馏来源（01_Inbox日志 + 02_Knowledge快速引用）
├── entities/    ← 已蒸馏实体（人物/公司/产品，自动合并同名）
├── concepts/    ← 已蒸馏概念（方法论/框架，自动合并同名）
├── syntheses/   ← 已蒸馏综合（跨主题分析，待扩展）
├── distill.py   ← 编译脚本
├── index.md     ← 总入口
└── log.md       ← 操作日志
```

### 蒸馏脚本使用
```bash
# 全量蒸馏（首次运行，对所有历史内容）
cd /Volumes/256G/gesila_compile && python3 distill.py --full

# 增量蒸馏（cron自动跑，只处理新增/修改）
cd /Volumes/256G/gesila_compile && python3 distill.py --incremental
```

### 蒸馏后Git同步（distill.py内置）
`distill.py` 末尾自动调用 `git_sync()`，逻辑：
1. `git status --porcelain` 检测是否有变更
2. `git add .` 暂存所有变更
3. `git commit -m "YYYY-MM-DD HH:MM 自动蒸馏同步"`
4. `git push origin main` 推送到 GitHub

**注意**：`git push` 若被拒绝（remote有新内容），是因为本地和remote有冲突。错误信息：
```
! [rejected] main -> main (fetch first)
error: failed to push some refs to 'https://github.com/gesilajerry/gesila_wiki.git'
```
**解法**：先 `git stash` → `git pull origin main --rebase` → `git stash pop` → `git push origin main`

### Wiki平台展示集成
**已蒸馏页面**：`http://127.0.0.1:5001/compile`

app.py 已添加：
- `COMPILE_ROOT = "/Volumes/256G/gesila_compile"` 配置
- `COMPILE_SUBDIRS` 映射（sources→已蒸馏来源, entities→已蒸馏实体, concepts→已蒸馏概念, syntheses→已蒸馏综合）
- `get_compile_cards()` / `_load_compile_cards()` 缓存加载
- `/compile` 首页路由
- `/compile/category/<cat>` 分类路由
- `/compile/card/<slug>` 详情路由
- search路由已合并 wiki + compile 双搜索

**重启生效**：
```bash
# 先找到旧进程
ps aux | grep "wiki_platform/app\.py" | grep -v grep

# kill后重启
kill <PID>
cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py &
```

### Cron定时任务
- job_id: `70c597681993`
- 时间: `0 9 * * *`（每天09:00）
- 任务: `cd /Volumes/256G/gesila_compile && python3 distill.py --incremental`

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是 Hermes Agent 的知识管理员。执行结构层提炼任务，从归档日志中提炼结构化知识卡片。

## 凭证
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: [FEISHU_SECRET_REDACTED]
- 飞书会话: oc_1e92781cdf15f71fad7dcd8d5d386e76

## 执行步骤

### Step 1: 确定日期
当天日期（如：2026-04-26）

### Step 2: 读取归档日志
读取 `/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md` 和 `Hermes_YYYY-MM-DD_会话复盘.md`

### Step 3: 识别值得提炼的内容
结合当日 Hermes cron 任务执行内容，识别：
- 1-5个值得结构化的核心概念或方法论
- 有价值的工作流程、问题解决思路
- 可以复用的经验教训

### Step 4: 写入知识卡片

**写入路径：**
- 概念卡：`/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_XXX.md`
- 方法论：`/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_XXX.md`

**卡片格式：**
```
# {概念/方法论名称}

**背景**
{这段知识产生的上下文}

**核心洞察**
{最核心的内容，300-800字}

**对Jerry的意义**
{这个知识对他的价值}

**延伸思考**
{可以应用的其他场景}

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
报告提炼了几张卡片及标题。

### 重要规则
- **禁止 [SILENT]**
- 每张卡片300-800字
- 优先关注：小红书运营经验、飞书 API 使用技巧、Cron 任务管理、内容创作方法论

## Response

结构层提炼任务已完成。以下是执行摘要：

---

## 📚 Hermes 结构层提炼报告 | 2026-05-25

**日志来源**：21个Cron任务，507KB归档日志
**Git提交**：`82fd2c7` ✅ 已推送

### 提炼卡片（3张）

**🔧 方法论**
**小红书BHR自动化发布踩坑与修复方案**
根因：`get_ws_url()` 依赖 `/tmp/chrome_xhs.log` 但文件未写入；修复方案是直接 `curl http://127.0.0.1:9543/json` 获取WS URL，绕过日志文件依赖。

**💡 概念卡**
**人形机器人量产元年产业信号**
2026年5月月出货量突破400台（去年全年<400台），英伟达物理AI收入60亿美元，产业化临界点已至，A股机器人板块将持续受益。

**💡 概念卡**
**物理AI数据赛道OriginFlow融资样本**
专注物理AI训练数据的OriginFlow 5个月融资破5亿，人形机器人量产元年直接拉动高质量物理交互数据需求，数据产权和采集渠道是核心壁垒。

---

**飞书简报**已发送至 `oc_1e92781cdf15f71fad7dcd8d5d386e76`（message_id: `om_x100b6e6504660094c376e22aa1036d0`）


---

## 半导体热点晨报 11:15 (job_id: 6ce527bbbff4)

# Cron Job: 半导体热点晨报 11:15

**Job ID:** 6ce527bbbff4
**Run Time:** 2026-05-26 11:03:50
**Schedule:** 0 11 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## mmx text chat 正确用法

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

## execute_code vs terminal 执行选择

> **重要**：在 `execute_code` 沙箱（Python 3.14）中，用 heredoc 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。

**正确做法**：用 `write_file` 先写脚本到 `/tmp/`，再用 `terminal` 执行：
```python
# ❌ execute_code heredoc 中会报 SyntaxError
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF

# ✅ 正确：先 write_file 到 /tmp/，再 terminal 执行
write_file("/tmp/publish.py", script_content)
terminal("python3 /tmp/publish.py")
```

**适用场景**：所有涉及多行字符串（含 regex、JSON 或复杂文本）的 Python 脚本，在 cron 或需要写文件执行的场景中，都应该用 write_file + terminal 方式。

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行
>
> ### 额外发现：curly quotes（"" 中文弯引号）导致 SyntaxError（2026-05-25 实测）
>
> 在 `execute_code` 沙箱（Python 3.14）中，即使弯引号出现在字符串**内部**（不在行首），也会触发 `SyntaxError: invalid syntax`：
> ```python
> title = "特斯拉FSD"监督版"正式入华"
> # SyntaxError: invalid syntax（行内任意位置有 "" 都会报）
> ```
> **解决方案**：将 `""` 替换为中文直角引号 `「」` 或纯 ASCII 替代品（如 `/`），不要保留弯引号：
> ```python
> title = "特斯拉FSD「监督版」正式入华"  # ✅ 可用
> title = "特斯拉FSD/监督版/正式入华"     # ✅ 替代方案
> ```
> 此问题同样会影响飞书 post 消息 content 中的文本，确认文本不含弯引号后再发送。

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你半导体产业研究助手。请用 mmx search 命令搜索过去24小时内发生的半导体热点新闻，生成半导体产业晨报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "半导体 芯片 美国出口管制"
```
```bash
mmx search query -t 24h --limit 8 "晶圆代工 台积电 中芯国际"
```
```bash
mmx search query -t 24h --limit 8 "半导体国产化 芯片设计 AI芯片"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【技术进展】【市场动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
💡 半导体产业晨报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【技术进展】
（3-5条，每条：标题 + 摘要）

【市场动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "半导体热点晨报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 1, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

💡 半导体产业晨报 | 20260526

【今日要闻】

1. 华为发布"韬(τ)定律"：以"时间缩微"替代"几何缩微"
5月25日，华为何庭波在IEEE ISCAS 2026发布半导体新定律，首次由中国主导定义全球芯片演进范式，被视为换道加速标志性节点。

2. A股+港股半导体暴涨：中芯国际港股涨近15%
华虹半导体A股20CM涨停，中芯国际A股涨停（+18.78%），芯片链近十只个股封20%涨停板，港股芯片股集体大涨。

3. 黄仁勋、苏姿丰同期访台，ComputeX前密集"密访"台积电供应链
英伟达CEO黄仁勋、AMD CEO苏姿丰同期抵台，行程涵盖拜访张忠谋、魏哲家等，AI芯片CoWoS封装产能紧张为重点议题。

【技术进展】

1. "韬定律"破除EUV依赖瓶颈，逻辑折叠开辟后摩尔新路径
以优化信号传播时延为核心，有望部分绕过先进制程光刻设备限制。

2. SK海力士发布iHBM散热技术，解决HBM高密度堆叠热瓶颈

3. 炬芯科技CIM存内计算商业化落地，能效比6.4TOPS/W

【市场动态】

1. 科创半导体ETF吸金，华夏ETF近5日净流入超13亿
2. 年内17家A股半导体公司赴港二次上市，芯片设计企业为主力
3. 中芯国际业绩预告：上半年净利润同比增长45%以上
4. 台积电2025年分红人均57.9万元，人才争夺战持续升级

【今日关注】

华为"韬定律"标志着中国半导体从追赶者向规则定义者转变，"时间缩微"思路有望部分绕开EUV光刻限制。但技术成熟度与生态配套仍是现实考验。二级市场已率先反应——中芯国际业绩超预期、港股暴涨；黄仁勋、苏姿丰收台则从侧面印证：先进封装产能才是当前产业链最稀缺资源。

---
✅ Koala发布成功（article_id=1hZnTtjq692jTM6e，综合管理-专业知识）
✅ 飞书发送成功


---

## xhs-vangogh-r1 (job_id: acf7034225ef)

# Cron Job: xhs-vangogh-r1

**Job ID:** acf7034225ef
**Run Time:** 2026-05-26 10:08:09
**Schedule:** 0 10 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_vangogh_gen.py 1

每天自动运行，梵高融合风图文：10张原创插画 + 专属情绪文案 + BGM + 慢速TTS，产出发飞书。

## Response

**梵高融合风图文日更完成** ✅

- **主题**：午后花园
- **产出**：10张原创插画 + 小红书文案
- **文案**：*最好的时光，不过是在花园里虚度。*
- **发布状态**：已成功发布


---

## MLCC产业链晨报 10:15 (job_id: 11859c122c65)

# Cron Job: MLCC产业链晨报 10:15

**Job ID:** 11859c122c65
**Run Time:** 2026-05-26 10:03:45
**Schedule:** 0 10 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## mmx text chat 正确用法

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

## execute_code vs terminal 执行选择

> **重要**：在 `execute_code` 沙箱（Python 3.14）中，用 heredoc 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。

**正确做法**：用 `write_file` 先写脚本到 `/tmp/`，再用 `terminal` 执行：
```python
# ❌ execute_code heredoc 中会报 SyntaxError
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF

# ✅ 正确：先 write_file 到 /tmp/，再 terminal 执行
write_file("/tmp/publish.py", script_content)
terminal("python3 /tmp/publish.py")
```

**适用场景**：所有涉及多行字符串（含 regex、JSON 或复杂文本）的 Python 脚本，在 cron 或需要写文件执行的场景中，都应该用 write_file + terminal 方式。

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行
>
> ### 额外发现：curly quotes（"" 中文弯引号）导致 SyntaxError（2026-05-25 实测）
>
> 在 `execute_code` 沙箱（Python 3.14）中，即使弯引号出现在字符串**内部**（不在行首），也会触发 `SyntaxError: invalid syntax`：
> ```python
> title = "特斯拉FSD"监督版"正式入华"
> # SyntaxError: invalid syntax（行内任意位置有 "" 都会报）
> ```
> **解决方案**：将 `""` 替换为中文直角引号 `「」` 或纯 ASCII 替代品（如 `/`），不要保留弯引号：
> ```python
> title = "特斯拉FSD「监督版」正式入华"  # ✅ 可用
> title = "特斯拉FSD/监督版/正式入华"     # ✅ 替代方案
> ```
> 此问题同样会影响飞书 post 消息 content 中的文本，确认文本不含弯引号后再发送。

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是MLCC产业链研究助手。请用 mmx search 命令搜索过去24小时内发生的MLCC/被动元器件产业动态，生成MLCC产业晨报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "MLCC 被动元件 村田 太阳诱电"
```
```bash
mmx search query -t 24h --limit 8 "MLCC产能 扩产 价格走势"
```
```bash
mmx search query -t 24h --limit 8 "消费电子 汽车电子 MLCC需求"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【产能动态】【市场观察】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
🔋 MLCC产业晨报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【产能动态】
（3-5条，每条：标题 + 摘要）

【市场观察】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "MLCC产业链晨报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 1, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

🔋 MLCC产业晨报 | 20260526

【今日要闻】

1. MLCC涨价潮来袭：日韩大厂减产，AI、车规级MLCC涨价15%-35%，交期拉长至20周以上。英伟达新机型大幅增加MLCC用量，高端产能持续紧缺。

2. TrendForce：AI芯片需求致高端MLCC供需偏紧，代理商展开预防性囤购，供应商调价回应，ODM与供应商议价结果陆续落地。

3. 中信建投维持"MLCC超级周期"评级：高端需求扩张与消费类渠道囤购升温有望催化MLCC价格反转。

4. A股MLCC概念股涨停潮：龙头封单近8亿元，风华高科、三环集团、国瓷材料等受资金追捧。

【产能动态】

1. 日韩大厂减产效应持续释放：村田、太阳诱电、TDK高端AI服务器及车规级MLCC产能收缩，缺口较年初扩大约20%。

2. 昀冢科技：MLCC产品主要应用于消费电子，暂未应用于AI算力服务器，产能偏向消费类。

3. 国内厂商扩产分化：风华高科、三环集团聚焦AI/车规高端扩产；部分中小厂商消费类MLCC产能利用率承压，尾部产能出清中。

【市场观察】

1. AI服务器MLCC单机用量提升超50%：英伟达GB200较传统服务器MLCC用量大增，高端产品量价齐升。

2. 车规级MLCC涨价传导：涨价15%-35%向下游传导，新能源车单车MLCC用量增至3000-5000颗。

3. 消费电子需求弱复苏：手机库存回补力度低于预期，AI PC换机潮尚未形成规模。

4. 新能源乘用车渗透率首超60%：4月达61.4%，汽车电子化率提升为MLCC超级周期提供长期支撑。

【今日关注】

AI服务器与车规级MLCC双轮驱动正在重塑产业格局。日韩大厂战略性减产叠加AI算力需求爆发，使本轮涨价周期与2017-2018年单纯囤货行情有本质区别——当前涨价的底层支撑是真实的高端需求缺口。预计涨价周期延续至2026年底，高端产品供需再平衡仍需18个月以上。国内龙头在国产替代窗口期加速切入AI供应链，有望实现从消费类向工业/车规级的产品结构升级。


---

## 财经新闻推送 08:30 (job_id: dd15667811fc)

# Cron Job: 财经新闻推送 08:30

**Job ID:** dd15667811fc
**Run Time:** 2026-05-26 09:12:16
**Schedule:** 10 9 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## mmx text chat 正确用法

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

## execute_code vs terminal 执行选择

> **重要**：在 `execute_code` 沙箱（Python 3.14）中，用 heredoc 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。

**正确做法**：用 `write_file` 先写脚本到 `/tmp/`，再用 `terminal` 执行：
```python
# ❌ execute_code heredoc 中会报 SyntaxError
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF

# ✅ 正确：先 write_file 到 /tmp/，再 terminal 执行
write_file("/tmp/publish.py", script_content)
terminal("python3 /tmp/publish.py")
```

**适用场景**：所有涉及多行字符串（含 regex、JSON 或复杂文本）的 Python 脚本，在 cron 或需要写文件执行的场景中，都应该用 write_file + terminal 方式。

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行
>
> ### 额外发现：curly quotes（"" 中文弯引号）导致 SyntaxError（2026-05-25 实测）
>
> 在 `execute_code` 沙箱（Python 3.14）中，即使弯引号出现在字符串**内部**（不在行首），也会触发 `SyntaxError: invalid syntax`：
> ```python
> title = "特斯拉FSD"监督版"正式入华"
> # SyntaxError: invalid syntax（行内任意位置有 "" 都会报）
> ```
> **解决方案**：将 `""` 替换为中文直角引号 `「」` 或纯 ASCII 替代品（如 `/`），不要保留弯引号：
> ```python
> title = "特斯拉FSD「监督版」正式入华"  # ✅ 可用
> title = "特斯拉FSD/监督版/正式入华"     # ✅ 替代方案
> ```
> 此问题同样会影响飞书 post 消息 content 中的文本，确认文本不含弯引号后再发送。

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你财经新闻助手。请用 mmx search 命令搜索过去24小时内发生的财经新闻，生成财经今日热点简报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "宏观经济 货币政策 金融市场"
```
```bash
mmx search query -t 24h --limit 8 "A股 上市公司 财报"
```
```bash
mmx search query -t 24h --limit 8 "房地产 股市 基金"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【市场动态】【行业观察】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
📈 财经晨报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【市场动态】
（3-5条，每条：标题 + 摘要）

【行业观察】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（综合管理-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "财经晨报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 2, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

📈 **财经晨报 | 20260526**

**【今日要闻】**

1. **美伊谈判取得重大进展，霍尔木兹海峡有望重开**  
   美国与伊朗原则上已达成结束中东战争的协议，30天内将全面恢复霍尔木兹海峡航运。消息传出后国际油价大跌逾6%，WTI原油跌破90美元/桶，市场风险情绪显著改善。

2. **华为发布半导体"韬(τ)定律"，重新定义芯片演进路径**  
   华为提出以"时间缩微"替代"几何缩微"的新路径，通过逻辑折叠等技术持续提升晶体管密度，预计2031年国产高端芯片可达1.4纳米制程同等水平。A股半导体板块大涨，中芯国际尾盘涨停成交超372亿元。

3. **央行月末"补水"3570亿元，人民币汇率创3年新高**  
   央行开展6000亿元1年期MLF操作及2580亿元7天期逆回购，单日净投放3570亿元。在岸、离岸人民币对美元汇率齐创2023年2月以来新高，流动性保持充裕。

**【市场动态】**

1. **A股放量大涨，上证指数站上4100点**  
   受美伊和谈及科技股带动，A股三大指数集体上扬，上证指数稳站4100点，深成指涨1.66%，创业板指大涨2.10%收复4000点整数位，科创50涨近6%。

2. **百亿级私募仓位升至82%以上，"科技+能源"均衡布局**  
   截至5月15日，百亿级股票私募仓位指数结束连续4周下跌，超六成私募仓位在80%以上，配置方向以业绩兑现的优质科技股及估值偏低的能源板块为主。

3. **保险资金加码权益配置，国家队仓位逼近公募**  
   截至一季度末，保险公司股票持仓规模达3.84万亿元，逼近同期公募主动权益基金3.99万亿元规模。中国平安耗资超16.8亿港元增持中国人寿、农业银行H股，"耐心资本"持续托市。

4. **宇树科技IPO将于6月1日上会，"具身智能第一股"呼之欲出**  
   宇树科技2026年一季度营收4.23亿元同比增68%，但扣非净利润同比降52.55%至4025万元；公司预计上半年营收10.52亿-11.28亿元，同比增幅约35%-45%。

**【行业观察】**

1. **半导体业绩爆发，一季度利润同比增148%**  
   2026年一季度A股半导体板块营收增速达30.89%，归母净利润同比大增148%，科创板整体净利润同比+207%，成为最强主线。

2. **消费市场平稳向新，1-4月社零增长3.2%**  
   商务部数据显示，1-4月商品和服务零售合并计算同比增长3.2%，其中服务零售额增长5.6%，以旧换新、离境退税等政策效果持续显现。

3. **几内亚拟推铝土矿出口管制，扰动全球铝产业链**  
   全球最大铝土矿生产国几内亚宣布将于6月公布出口管制措施，铝土矿占全球产量逾三分之一，管控措施或进一步扰动有色金属供应链。

4. **房地产ETF份额持续缩减，资金观望情绪浓厚**  
   房地产ETF南方基金单日份额减少1.22亿份，资金净流出1.34亿元，行业估值处于历史低位但市场信心恢复仍需时间。

**【今日关注】**

美伊和谈的戏剧性进展正在重塑全球风险资产定价逻辑。霍尔木兹海峡若30天内恢复通航，全球油轮运力将得到极大缓解，能源价格中枢有望进一步下移，通胀压力减轻将为各国央行货币政策释放更大空间。对A股而言，科技与能源两大主线在外部环境缓和叠加国内政策发力的共振下，有望延续震荡上行格局，但需警惕汇率波动及地缘变数带来的短期扰动。

---
✅ **Koala博客已发布**（综合管理-专业知识，forum_id=2）  
✅ **飞书已发送**（oc_1e92781cdf15f71fad7dcd8d5d386e76）


---

## gesila-compile-daily (job_id: 70c597681993)

# Cron Job: gesila-compile-daily

**Job ID:** 70c597681993
**Run Time:** 2026-05-26 09:01:06
**Schedule:** 0 9 * * *

## Prompt

[SYSTEM: The user has invoked the "llm-wiki" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: llm-wiki
description: LLM-Wiki 知识管理系统 — 四层架构（raw/ → wiki/ → skills/），支持资料摄入（ingest）、智能查询（query）、健康巡检（lint）三个核心操作。
version: 1.0.0
tags: [knowledge-management, wiki, rag, knowledge-base]
language: zh
triggers:
  - 知识管理系统
  - 帮我管理知识库
  - 摄入资料
  - 查询知识库
  - 知识库巡检
  - wiki知识库
---

# LLM-Wiki 知识管理系统

## 系统架构

四层架构：

| 层级 | 名称 | 路径 | 职责 |
|------|------|------|------|
| L0 | 操作层 | `skills/*/SKILL.md` | ingest / query / lint 三个技能入口 |
| L1 | 原始资料层 | `raw/01-articles/` 等 | 待处理的原始资料，只读不修改 |
| L1.5 | 归档层 | `raw/09-archive/YYYY-MM/` | 已处理文件归档 |
| L2 | 知识编译层 | `wiki/sources/` `wiki/entities/` `wiki/concepts/` `wiki/syntheses/` | 核心价值层，结构化知识文件 + 索引 |
| L3 | 规则层 | `skills/*/SKILL.md` `AGENTS.md` `SOUL.md` | 技能行为定义、Agent 规范 |

## 分面分类体系

| 分类 | 图标 | 说明 | 路径 |
|------|------|------|------|
| Sources | 📄 | 原始资料的 1:1 摘要 | `wiki/sources/` |
| Entities | 👤 | 人物、公司、机构、产品等可指对象 | `wiki/entities/` |
| Concepts | 💡 | 框架、方法论、理论、模式等抽象知识 | `wiki/concepts/` |
| Syntheses | 📊 | 跨主题综合分析报告 | `wiki/syntheses/` |

---

# Skill: Ingest（资料摄入）

**目的**: 将原始资料摄入知识库，提炼、翻译、知识网络化后编译为结构化知识文件。

**触发方式**:
- `/ingest <path>` — 指定文件摄入
- `/ingest` — 全量摄入（扫描 raw/ 所有子目录，排除 09-archive/）

## 流程步骤

### 步骤 0: 已摄取检查（去重）

文件在 `raw/09-archive/` = 已摄入，跳过；文件在 `raw/01-articles/` = 待处理。

### 步骤 1: 读取源文件

读取 .md 完整内容。文件名含中文引号/斜杠时，用 Python os.listdir() + 转义读取。

### 步骤 2: 提炼核心 + 翻译

提取主题（Theme）、翻译为英文（English）、提取概念（Concepts）。

### 步骤 3: 创建来源摘要

输出路径: `wiki/sources/摘要-xxx.md`，内容为原始资料的 1:1 结构化摘要。

### 步骤 4: 知识网络化

- 概念/实体页面已存在？→ 增量合并新信息
- 有冲突？→ 暂停，报告用户三个选项：保留双方 / 覆盖 / 放弃
- source 页面中有不存在的 [[链接]]？→ 自动创建 stub 占位页（P0/P1/P2 优先级）

### 步骤 5: 归档源文件

原始位置 `raw/01-articles/` → 归档到 `raw/09-archive/YYYY-MM/`，重命名格式: `01-articles-{主题}-{日期}.md`

### 步骤 6: 更新索引

注册到对应的 `index-*.md`（sources / entities / concepts / syntheses）

### 步骤 7: 记录日志

追加到 `wiki/log.md`，格式: `ingest | {文件名} | {处理结果}`

---

# Skill: Query（智能查询）

**目的**: 基于知识库内容，对用户问题进行精准检索、深度阅读、综合回答，可选地固化为 synthesis。

**触发方式**:
- `/query <问题>` — 用户直接提问
- 自然语言提问 — "我的笔记里关于X的内容"

## 流程步骤

### 步骤 1: 查阅总入口

读取 `wiki/index.md`（总入口，含分类概览 + 索引路径），定位相关分类。

### 步骤 2: 读取分类索引

读取对应 `index-*.md`（仅目标分类的索引列表），搜索匹配条目，定位目标页面列表。

### 步骤 3: 深度阅读

读取目标 .md 文件完整内容，理解并提炼核心结论、要点、关系。追溯关联的 [[双链]] 内容。

### 步骤 4: 综合回答

- 直接回答问题
- 提供关键要点
- 展示逻辑关系
- 双链引用格式：`[[页面名]]`
- 必要时用 Markdown 引用块 `>` 引用原文

### 步骤 5: 高价值固化（可选）

评估回答价值（是否 > 2 段？是否有分析/对比价值？）。如值得固化，询问用户是否创建 synthesis 页面（路径: `wiki/syntheses/xxx.md`）。

### 步骤 6: 记录日志

追加到 `wiki/log.md`：`query | {问题} | {查阅页面} | {是否创建synthesis} | {时间戳}`

---

# Skill: Lint（健康巡检）

**目的**: 对知识库进行健康体检，检测索引一致性、双向链接健康度、认知冲突。

**触发方式**: `/lint` 命令

## 流程步骤

### 步骤 1: 索引一致性检查

- 🟡 黄灯：文件存在但未注册到索引
- 🔴 红灯：索引指向不存在的文件（幽灵条目）

### 步骤 2: 双向链接健康检查

扫描 wiki/ 所有 .md 文件，提取所有 `[[...]]` 双链：
- 🔴 死链：目标页面不存在
- 🟡 孤儿页面：被引用 = 0 次（不做删除，仅标记）
- 🟢 正常：被引用 > 0 次

注：代码块（` ``` `）内的双链跳过不检。

### 步骤 3: 认知冲突审查

全局搜索 `#知识冲突` 标签：
- 🔴 红灯：存在未解决冲突
- 🟢 绿灯：无冲突

注：代码块内匹配跳过不检。

## 报告格式

```markdown
## 🩺 知识库健康体检报告 - YYYY-MM-DD

### 🟢 绿灯项
（正常项列表）

### 🟡 黄灯项
**孤儿页面:**
- [[页面A]] — 被引用 0 次

**未同步索引:**
- wiki/sources/xxx.md — 未注册到 index-sources.md

### 🔴 红灯项
**死链:**
- [[不存在页面]] — 被 [[页面C]] 引用

**未解决冲突:**
- wiki/concepts/xxx.md — #知识冲突

### 🔧 修复建议
[按 P0/P1/P2 优先级列出修复建议]
```

## 修复优先级

| 等级 | 定义 | 处理方式 |
|------|------|---------|
| P0 | 被引用 ≥ 5 次的枢纽概念页面 | 独立撰写内容，从已有 source 提取摘要 |
| P1 | 被引用 2-4 次的常用概念 | 从已有 source 页提取摘要填充 |
| P2 | 被引用 1 次的边缘页面 | 先创建 stub，后续自然填充 |

## 硬约束

1. **仅读扫描**: 生成报告前，禁止修改、删除、重命名任何文件
2. **手动确认**: 报告后等待用户确认再执行修复
3. **静默日志**: 修复完成后，在 `wiki/log.md` 追加日志

---

# 双链引用规范

| 规则 | 约定 |
|------|------|
| 1 | 每引用某个 Wiki 页面信息 → 使用 `[[页面名]]` 标注 |
| 2 | 整段引用同一页面 → 段落首尾各引用一次 |
| 3 | 引用特定原文 → 使用 Markdown 引用块 `>` 引用原文内容 |

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

cd /Volumes/256G/gesila_compile && python3 distill.py --incremental 2>&1

## Response

[SILENT]


---

## xhs-vangogh-r4 (job_id: c620d75134bb)

# Cron Job: xhs-vangogh-r4

**Job ID:** c620d75134bb
**Run Time:** 2026-05-26 07:05:27
**Schedule:** 0 7 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_vangogh_gen.py 4

每天自动运行，梵高融合风图文：10张原创插画 + 专属情绪文案 + 小红书自动发布。

## Response

✅ **小红书梵高融合风图文流水线完成**

**主题**：草原野花
**生成内容**：10张原创插画（梵高厚笔触 + 扁平治愈构图）

| # | 文案 |
|---|------|
| 1 | 草原上开满了野花，色彩斑斓... |
| 2 | 野花丛中，蝴蝶和蜜蜂忙碌... |
| 3 | 虞美人在风中摇曳，如火燃烧... |
| 4 | 草原野花与远处的雪山相映... |
| 5 | 一朵巨大的野花，占据了整个画面... |
| 6 | 野花在夕阳下变成金色... |
| 7 | 雨后的野花，花瓣上挂着雨滴... |
| 8 | 风中的野花，摇曳如波浪... |
| 9 | 野花丛中，一只野兔在吃草... |
| 10 | 一朵孤零零的野花，在岩石缝里绽放... |

**发布状态**：`✓ posted: 不需要名字，野蛮生长就够了。`


---

## AI热点推送 04:00 (job_id: c88c6506add2)

# Cron Job: AI热点推送 04:00

**Job ID:** c88c6506add2
**Run Time:** 2026-05-26 05:01:39
**Schedule:** 0 5 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## mmx text chat 正确用法

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

## execute_code vs terminal 执行选择

> **重要**：在 `execute_code` 沙箱（Python 3.14）中，用 heredoc 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。

**正确做法**：用 `write_file` 先写脚本到 `/tmp/`，再用 `terminal` 执行：
```python
# ❌ execute_code heredoc 中会报 SyntaxError
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF

# ✅ 正确：先 write_file 到 /tmp/，再 terminal 执行
write_file("/tmp/publish.py", script_content)
terminal("python3 /tmp/publish.py")
```

**适用场景**：所有涉及多行字符串（含 regex、JSON 或复杂文本）的 Python 脚本，在 cron 或需要写文件执行的场景中，都应该用 write_file + terminal 方式。

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行
>
> ### 额外发现：curly quotes（"" 中文弯引号）导致 SyntaxError（2026-05-25 实测）
>
> 在 `execute_code` 沙箱（Python 3.14）中，即使弯引号出现在字符串**内部**（不在行首），也会触发 `SyntaxError: invalid syntax`：
> ```python
> title = "特斯拉FSD"监督版"正式入华"
> # SyntaxError: invalid syntax（行内任意位置有 "" 都会报）
> ```
> **解决方案**：将 `""` 替换为中文直角引号 `「」` 或纯 ASCII 替代品（如 `/`），不要保留弯引号：
> ```python
> title = "特斯拉FSD「监督版」正式入华"  # ✅ 可用
> title = "特斯拉FSD/监督版/正式入华"     # ✅ 替代方案
> ```
> 此问题同样会影响飞书 post 消息 content 中的文本，确认文本不含弯引号后再发送。

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你AI科技新闻助手。请用 mmx search 命令搜索过去24小时内发生的AI科技热点新闻，生成AI今日热点简报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "人工智能 AI大模型 GPT"
```
```bash
mmx search query -t 24h --limit 8 "AI创业 融资 技术突破"
```
```bash
mmx search query -t 24h --limit 8 "自动驾驶 机器人 智能硬件"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【技术进展】【行业动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
🤖 AI晨报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【技术进展】
（3-5条，每条：标题 + 摘要）

【行业动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（综合管理-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "AI晨报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 2, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

🤖 AI晨报 | 2026年5月26日

【今日要闻】

1. 中国AI大模型周调用量达7.941万亿Token，是美国的2.11倍，实现生态级全面超越。

2. 阿里Qwen3.7-Max位列全球大模型榜单第五，性能接近GPT/Claude最强模型，中美差距缩至2.7%。

3. 特斯拉FSD监督版正式入华，更名为"特斯拉辅助驾驶"，L2级本质未变，需理性管理市场期望。

4. 国家发改委：加快具身智能训练基础设施建设，机器人将"进工厂、进商场、进家庭"。

---

【技术进展】

1. OpenAI发布GPT-5预览版，聚焦多模态原生融合；GPT-5.5刷新编程天花板，gramBench通过率创历史新高。

2. 腾讯应用宝上线免费"吐司"APP开发工具，自然语言需求即可AI全流程自主开发。

3. 深圳AI终端展：捷视飞通全球首发"灵眸"四足机器人（应急救援款+导盲款），国产特种机器人产业化提速。

4. 中国信通院联合三大运营商发起AI WAN行动，打通算力网络最后一公里。

---

【行业动态】

1. DeepSeek完成约700亿元首轮融资，估值450亿美元，刷新国内科技初创公司首轮融资最高纪录。

2. 月之暗面完成D轮20亿美元融资，投后估值突破200亿美元，年内融资总额超39亿美元。

3. Anthropic洽谈300亿美元融资，估值有望突破9000亿美元超越OpenAI，全球AI双寡头格局加速成型。

4. 一季度全球AI领域融资超1100亿元，同比激增185.4%，中小模型公司生存压力加剧。

---

【今日关注】

当前AI行业呈现"资本向头部集中、技术向实体渗透"两大趋势。具身智能获国家政策背书，但从赛场走向工厂仍有距离。真正的变量在于算力底座——高端芯片封锁持续背景下，大模型生态的可持续性取决于自主可控的算力突破进度。

---
✅ 已发布至 Koala 博客（综合管理-专业知识）
✅ 已发送至飞书群


---

## 半导体热点晚报 23:00 (job_id: 5c0b040cdb63)

# Cron Job: 半导体热点晚报 23:00

**Job ID:** 5c0b040cdb63
**Run Time:** 2026-05-25 23:51:33
**Schedule:** 50 23 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## mmx text chat 正确用法

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

## execute_code vs terminal 执行选择

> **重要**：在 `execute_code` 沙箱（Python 3.14）中，用 heredoc 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。

**正确做法**：用 `write_file` 先写脚本到 `/tmp/`，再用 `terminal` 执行：
```python
# ❌ execute_code heredoc 中会报 SyntaxError
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF

# ✅ 正确：先 write_file 到 /tmp/，再 terminal 执行
write_file("/tmp/publish.py", script_content)
terminal("python3 /tmp/publish.py")
```

**适用场景**：所有涉及多行字符串（含 regex、JSON 或复杂文本）的 Python 脚本，在 cron 或需要写文件执行的场景中，都应该用 write_file + terminal 方式。

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行
>
> ### 额外发现：curly quotes（"" 中文弯引号）导致 SyntaxError（2026-05-25 实测）
>
> 在 `execute_code` 沙箱（Python 3.14）中，即使弯引号出现在字符串**内部**（不在行首），也会触发 `SyntaxError: invalid syntax`：
> ```python
> title = "特斯拉FSD"监督版"正式入华"
> # SyntaxError: invalid syntax（行内任意位置有 "" 都会报）
> ```
> **解决方案**：将 `""` 替换为中文直角引号 `「」` 或纯 ASCII 替代品（如 `/`），不要保留弯引号：
> ```python
> title = "特斯拉FSD「监督版」正式入华"  # ✅ 可用
> title = "特斯拉FSD/监督版/正式入华"     # ✅ 替代方案
> ```
> 此问题同样会影响飞书 post 消息 content 中的文本，确认文本不含弯引号后再发送。

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你半导体产业研究助手。请用 mmx search 命令搜索过去24小时内发生的半导体热点新闻，生成半导体产业晚报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "半导体 芯片 美国出口管制"
```
```bash
mmx search query -t 24h --limit 8 "晶圆代工 台积电 中芯国际"
```
```bash
mmx search query -t 24h --limit 8 "半导体国产化 芯片设计 AI芯片"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【技术进展】【市场动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
💡 半导体产业晚报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【技术进展】
（3-5条，每条：标题 + 摘要）

【市场动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（市场销售-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "半导体热点晚报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 1, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

💡 半导体产业晚报 | 20260525

【今日要闻】

**华为发布半导体"韬(τ)定律"，何庭波称可实现1.4nm等效制程**
在2026国际电路与系统研讨会(ISCAS 2026)上，华为董事何庭波正式发表"韬(τ)定律"，提出以"时间缩微"替代传统"几何缩微"，通过逻辑折叠技术提升晶体管密度。华为预计2031年可实现等效1.4nm制程性能，旗下一款新麒麟芯片将于今年秋季发布。

**中芯国际盘中涨超17%创历史新高，市值突破1.24万亿元**
5月25日A股中芯国际大幅拉升，盘中涨幅最高达17.65%，股价最高触及155元创历史新高，总市值突破1.24万亿元。科创芯片板块集体爆发，带动华虹公司、盛美上海、拓荆科技等多股跟涨。

**美国对华半导体出口管制持续收紧，国产替代逻辑持续强化**
在美国持续收紧芯片出口管制的背景下，中国AI芯片产业迎来战略窗口期。2025年中国AI加速卡国产厂商交付量达165万片，市场份额升至41%，英伟达占55%，差距正在缩小。

【技术进展】

**华为"麒麟2026"手机芯片将首次落地逻辑折叠技术**
华为何庭波透露，代号"麒麟2026"的手机芯片是逻辑折叠技术在消费级芯片上的首次成功商用。晶体管密度提升53.5%达238 MTr/mm²，大核能效提升41%，最高频率提升12.7%主频达3.1GHz，预计2031年可达5.0GHz。

**阿里达摩院玄铁9系列成功适配安卓16，RISC-V芯片里程碑式突破**
阿里达摩院宣布玄铁9系列高性能处理器已完成对安卓16操作系统的适配，成为全球首款成功运行最新版安卓系统的RVA23兼容RISC-V处理器，为RISC-V在AI终端领域的规模化商业落地奠定基础。

**半导体ETF集体走强，科创芯片ETF单日涨超5%**
受华为韬定律发布及中芯国际业绩双重催化，科创芯片ETF南方(159325)上涨4.94%，科创半导体ETF华夏(588170)收涨5.76%，半导体产业链全线走强。

【市场动态】

**中芯国际406亿并购过会，科创板最大并购重组交易落地**
中芯国际耗资406亿元完成收购中芯北方剩余49%股权，实现100%控股。该交易刷新科创板并购重组交易总金额纪录，将直接增厚归母净利润，消除少数股东沟通成本，强化国内晶圆代工产能布局。

**台积电Q1净利润大增58%却传降薪，成本管控压力显现**
台积电2026年第一季度净利润大增58%，但却传出降薪消息，反映出半导体龙头在扩产周期下面临的成本管控压力。全球晶圆代工产值预计2026年将增长至2032亿美元以上，增幅约19%。

**AI服务器需求拉动高端MLCC涨价，风华高科5天3板**
AI服务器需求爆发拉动高端MLCC持续涨价，概念股风华高科5天3板续创历史新高，昀冢科技涨超10%。被动元件板块延续强势，成为本轮行情的延伸热点。

**伯恩斯坦报告：AI存储超级周期共振，国产半导体设备迎三年黄金爆发期**
伯恩斯坦发布研报认为，在存储芯片产能集中扩张与AI算力超级周期共振下，国产半导体设备将迎来三年黄金爆发期。外资源深机构对中国半导体产业链的认可度正在提升。

【今日关注】

华为"韬定律"的发布标志着中国半导体产业从被动追随转向主动定义规则。尽管新定律的实际落地效果仍待时间检验，但其提出的"以时间换空间"思路——通过架构创新而非制程微缩来提升性能——为受限于EUV光刻机的中国芯片业提供了一条可行路径。中芯国际今日的大涨既是并购重组利好兑现，也是市场对国产替代逻辑的再一次投票。展望下半年，随着AI算力需求持续扩散和国产化率提升，半导体全产业链有望延续景气周期，但需警惕估值过高的回调风险。


---

## Wiki Git Pull (job_id: 4325b9d1d400)

# Cron Job: Wiki Git Pull

**Job ID:** 4325b9d1d400
**Run Time:** 2026-05-25 23:08:02
**Schedule:** 0 23 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/git_wiki_pull.py

从 GitHub 拉取远程更改到本地 wiki。

## Response

[SILENT]


---

## Hermes 结构层提炼 23:00 (job_id: 475e0d05da28)

# Cron Job: Hermes 结构层提炼 23:00

**Job ID:** 475e0d05da28
**Run Time:** 2026-05-25 23:07:46
**Schedule:** 0 23 * * *

## Prompt

[SYSTEM: The user has invoked the "wiki-platform-ops" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: wiki-platform-ops
description: 维基平台运维手册——进程管理、ngrok公网访问、OpenClaw/OpenPro绑定问题、Git Push凭证
---

# Wiki 平台运维手册

## 环境信息
- 项目路径：`/Volumes/256G/gesila_wiki/`
- Flask 端口：5001
- 启动命令：`cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py`
- GitHub 仓库：`https://github.com/gesilajerry/gesila_wiki.git`
- 分支：main

## 公网访问
- ngrok URL：`https://unbribing-undeep-maximina.ngrok-free.dev`
- ngrok token：`3B9DPZE4I2SawazXcJ4xZmdHdrS_25s2AS6UL5Qcqt9Lxb5sj`

### ngrok 配置（macOS native）
**配置文件**：`~/Library/Application Support/ngrok/ngrok.yml`

```yaml
version: "3"
agent:
  authtoken: 3B9DPZE4I2SawazXcJ4xZmdHdrS_25s2AS6UL5Qcqt9Lxb5sj
tunnels:
  wiki:
    proto: http
    addr: 5001
  gateway:
    proto: http
    addr: 18789
```

**启动指定隧道**：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <tunnel-name>`

**常见错误**：错误启动了 `gateway` tunnel（指向 18789/Hermes）而非 `wiki` tunnel（指向 5001/维基）。检查：
```bash
ps aux | grep ngrok | grep -v grep  # 看在跑哪个 tunnel
curl -s http://localhost:4040/api/tunnels  # ngrok web UI API，看实际 addr 是什么
```

### ngrok API（管理隧道）
```bash
# 查看当前 tunnels（包括 name、public_url、实际指向的 addr）
curl -s http://localhost:4040/api/tunnels | python3 -c "import sys,json; [print(t['name'], t['public_url'], '->', t['config']['addr']) for t in json.load(sys.stdin).get('tunnels',[])]"

# 删除某个 tunnel
curl -X DELETE http://localhost:4040/api/tunnels/<name>

# 创建新 tunnel（POST body 必须含 proto + addr，name 可选）
curl -X POST http://localhost:4040/api/tunnels \
  -H 'Content-Type: application/json' \
  -d '{"proto":"http","addr":"5001"}'
```

**故障排查流程：**
1. `ps aux | grep ngrok | grep -v grep` — 看在跑哪个 tunnel
2. `curl http://localhost:4040/api/tunnels` — 验证实际 addr 是否正确（wiki=5001, gateway=18789）
3. 若 tunnel 指向错误地址：`cd ~/Library/Application\ Support/ngrok && ./ngrok start <正确tunnel名>`

### 常见错误：wiki tunnel 指向 gateway 的 18789
**症状**：wiki 的 ngrok URL 能通，但打开是 OpenClaw/Hermes 的页面，而非维基页面。
**原因**：启动了 `gateway` tunnel（18789）而非 `wiki` tunnel（5001）。
**修复**：`kill <ngrok_pid>` 后 `cd ~/Library/Application\ Support/ngrok && ./ngrok start wiki`

## OpenClaw/OpenPro 绑定问题
**症状**：ngrok URL 被 OpenClaw/OpenPro 拦截，跳转到 OpenPro 页面而不是 wiki。

**原因**：ngrok URL 同时绑定了 Wiki 项目和 OpenPro，OpenClaw/OpenPro 优先拦截。

**解决方法**：在 OpenClaw/OpenPro 控制台解除 URL 与 OpenPro 的绑定，只保留 Wiki 项目绑定。

## GitHub 推送凭证（必须）

**当前有效 Token**：`[GITHUB_TOKEN_REDACTED]`（GitHub Classic，40位）

**⚠️ GitHub Classic Token 说明**
- 必须是 Classic Token，不是新版 Fine-grained PAT
- 长度固定 40 位
- 设置路径：GitHub → Settings → Developer settings → Personal access tokens → Classic
- 权限需要 `repo` 全权限

**第一步**：创建 `~/.netrc`（权限必须 600）：
```
machine github.com
login gesilajerry
password [GITHUB_TOKEN_REDACTED]
```

**第二步**：关闭 SSL 验证（必须）：
```bash
git config --global http.sslVerify false
```
原因：macOS 上的 HTTPS 代理做 SSL interception，导致 `git pull/push` 报错 `LibreSSL SSL_connect: SSL`。关闭 SSL 验证绕过代理的证书拦截。

**第三步**：推送：
```bash
cd /Volumes/256G/gesila_wiki
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**❌ 错误做法**：把 token 嵌入 git remote URL（`https://user:token@github.com/`）——token 会出现在进程列表和日志中，不安全。

**✅ 正确做法**：统一用 netrc + credential.helper，remote URL 保持干净。

### Git 双向同步脚本

**脚本路径**：`/Users/mac/.hermes/scripts/git_sync_wiki.py`

**cron job ID**：`61c28cf385bb`
**调度时间**：`0 23 * * *`（每天 23:00）

**用途**：另一个 agent（Trae）往 GitHub 推送后，本地每日 23:00 拉取同步。

**手动触发**：
```bash
/usr/bin/python3 /Users/mac/.hermes/scripts/git_sync_wiki.py
```

**已知踩坑**：
- `git remote URL` 里嵌 token → 进程列表暴露，风险
- netrc 权限不是 600 → git 不读取
- Token 用了旧版（<40位）→ 认证失败（GitHub 已不支持密码认证，必须用 Classic PAT）
- cron prompt 里写明文 token → 触发安全扫描拦截，改用 Python 脚本读取 token

## 目录结构（不动任何文件夹）
```
gesila_wiki/
  01_Inbox/日志/
  02_Knowledge/概念卡/、方法论/、项目复盘/
  03_Output/已发布/、汇报材料/
```
只往现有目录存文件，框架自动识别。

## OpenClaw Gateway 运维

### 症状（版本 2026.4.24 及之前）
- `openclaw status` / `openclaw update check` 等命令全部超时（30s+）
- `curl http://localhost:18789` 返回 connection refused
- `ps aux | grep openclaw` 显示 gateway 进程在跑但 0% CPU
- 日志 `~/.openclaw/logs/gateway.log` 最后一条是 `"starting channels and sidecars..."` 然后停住
- 日志不写，但 `kill -USR2 <pid>` 可强制刷新日志缓冲区

### 根因（版本 2026.4.24 及之前）
OpenClaw gateway 启动时每次都重新安装所有插件的 bundled runtime deps（npm 包），共 66 个包，在 `~/.openclaw/plugin-runtime-deps/` 目录下。
- 正常需 8-12 分钟
- npm ENOTEMPTY 锁冲突时可达 30 分钟+
- 日志停在 "starting..." 是因为在等插件安装完成，并非真卡住
- 日志文件 600MB+ 会导致日志轮转出问题

### 根因（版本 2026.4.25 — 2026.4.26）
**browser 插件的 CDP 初始化会卡死 gateway**（0% CPU，端口监听但不响应 HTTP）。
- 进程 PID 存在，端口 18789 监听，但所有 HTTP 请求超时
- 日志不写入（或写入极慢）
- Dev 模式（`OPENCLAW_PROFILE=dev`）因 `skipBootstrap: true` 能起来

**修复**：在 `openclaw.json` 中关闭 browser 插件：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['plugins']['entries']['browser'] = {'enabled': False}
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2)
```

### 根因（版本 2026.4.27+）— 升级后完全无法启动
**升级到 2026.4.27 后 gateway 卡在 "staging bundled runtime deps" 完全无响应。**

1. **plugin-runtime-deps 安装不完整**：同时存在 4 个版本目录（2026.4.24, 2026.4.25, 2026.4.27, unknown），版本之间文件互相引用导致缺失文件（如 `chutes-oauth-CUO6Kvd3.js`）
2. **npm 网络不通**：gateway 尝试安装 8 个缺失包（@agentclientprotocol/claude-agent-acp@0.31.0, @grammyjs/*, @homebridge/ciao@^1.3.6, acpx@0.6.1, chokidar@^5.0.0, grammy@^1.42.0 等），npm registry 访问超时后一直重试
3. **所有 openclaw CLI 命令全部超时**：`doctor`, `gateway stop`, `dashboard` 等都卡死
4. **launchctl 重启后 gateway 进程存在但不监听端口**：旧进程没真正被杀掉（launchd 管理混乱）

**Gateway token 位置**：`openclaw.json` 里存的是 `"***"`（生成后存在 `~/.openclaw/identity/device-auth.json` 的 `tokens.operator.token` 字段），该 token 是 device-to-device 认证，不用于 HTTP API。

**降级到 2026.4.25 方案**：
```bash
# 杀掉所有 gateway 进程
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 卸载 2026.4.27
rm -rf ~/.npm-global/lib/node_modules/openclaw

# 指定版本安装（--registry 避免网络问题）
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
```

**验证版本**：`openclaw --version` 和 `cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"`

### 诊断步骤
```bash
# 1. 查看 openclaw 版本
openclaw --version

# 2. 看进程状态
ps aux | grep "openclaw" | grep -v grep

# 3. 检查端口监听
curl -s --connect-timeout 5 "http://localhost:18789/health"

# 4. 检查 npm 包完整性
ls ~/.npm-global/lib/node_modules/openclaw/dist/index.js
cat ~/.npm-global/lib/node_modules/openclaw/package.json | python3 -c "import json,sys; print(json.load(sys.stdin)['version'])"

# 5. 查看 plugin-runtime-deps 版本
ls ~/.openclaw/plugin-runtime-deps/

# 6. 检查 npm 锁冲突
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete

# 7. 强制刷新日志
kill -USR2 <pid>
tail -20 ~/.openclaw/logs/gateway.log

# 8. 查看 /tmp/openclaw/ 下的实时日志（JSON 格式）
tail -f /tmp/openclaw/openclaw-$(date +%Y-%m-%d).log
```

### 修复方案

**方案 A：清理后重启（推荐用于 2026.4.24 及之前版本）**
```bash
# 清空错误日志（保留文件避免权限问题）
truncate -s 0 ~/.openclaw/logs/gateway.err.log

# 删除 npm 缓存锁
find ~/.openclaw/plugin-runtime-deps/ -name "*.lock" -delete
find ~/.openclaw/plugin-runtime-deps/ -name ".openclaw-npm-cache" -type d -exec rm -rf {} + 2>/dev/null

# 杀掉旧进程（注意：2026.4.27 版本进程杀掉后还会重启，需先卸载）
pkill -9 -f "openclaw-gateway"
pkill -9 -f "openclaw/dist/index.js"

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：降级回 2026.4.25（2026.4.27 专用）**
```bash
# 杀掉所有进程
pkill -9 -f "openclaw" 2>/dev/null
rm -rf ~/.npm-global/lib/node_modules/openclaw
npm install -g openclaw@2026.4.25 --registry=https://registry.npmmirror.com
launchctl bootout gui/$(id -u)/ai.openclaw.gateway 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist
```

**方案 B：跳过飞书 channel 加速启动**
飞书 bot probe 超时（axios 30s × 4 accounts = 2 分钟延迟）：
```python
import json
with open('/Users/mac/.openclaw/openclaw.json') as f:
    d = json.load(f)
d['channels']['feishu']['enabled'] = False
with open('/Users/mac/.openclaw/openclaw.json', 'w') as f:
    json.dump(d, f, indent=2, ensure_ascii=False)
```
重启后 gateway 约 3 分钟可 ready（vs 10 分钟带飞书）。

**方案 C：手动触发 npm 安装（减少重复安装）**
让 gateway 跑完一次后，下次重启会复用缓存。但 npm ENOTEMPTY 问题会反复出现，每次都要清锁。

### launchd 管理
```bash
# 查看状态
launchctl list | grep openclaw

# 重启
launchctl bootout gui/$(id -u)/ai.openclaw.gateway
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.openclaw.gateway.plist

# 日志路径
~/.openclaw/logs/gateway.log（主日志）
~/.openclaw/logs/gateway.err.log（错误日志）
/tmp/openclaw/openclaw-YYYY-MM-DD.log（实时 JSON 日志）
```

### OpenClaw vs Hermes 定时任务关系
- OpenClaw gateway 内置 cron 调度（job_id 如 `7c55dfb106c3`）
- Hermes 有独立的 cron 系统（`~/.hermes/cron/jobs.json`）
- 两者可以并存；迁移时暂停 OpenClaw 任务，避免重复执行：
  `cronjob pause <job_id>`

---

## 性能优化（app.py）

**问题**：158+ 文件时，每次请求全量读文件，首页/图谱页慢到无法操作。

**优化方案**（app.py 已实现）：
1. 启动时预热：`app/__init__` 时 `_load_cards()` + `_build_graph()` 预加载，197张卡约0.1秒
2. 文件 mtime 检测：`_scan_mtimes()` 追踪文件变更，TTL 从4小时改为文件驱动刷新
3. 后台异步重建：缓存过期时返回旧数据同时后台线程重建，不阻塞请求
4. 图数据独立缓存：`build_graph()` 结果存入 `_cache['graph']`

**重启生效**：`kill <PID> && python3 app.py`

---

## 结构层提炼任务（定时 cron 执行）

每晚21:30自动执行，从当日归档日志中提炼知识卡片。

### Step 1: 确定日期
当天日期（如：`2026-05-11`）

### Step 2: 读取归档日志
```
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_推送日报.md
/Volumes/256G/gesila_wiki/01_Inbox/日志/Hermes_YYYY-MM-DD_会话复盘.md
```

**⚠️ 解析要点：推送日报文件超大（~460KB）**
- 文件过大是因为每个任务块的 Prompt 都完整嵌入了调用的 SKILL.md 内容（整个技能文件）
- 实际 AI 生成的 Response 内容才是需要提取的知识素材
- 搜索 AI 简报内容时用 emoji 标记（如 `"🤖 AI热点简报"`）或 Response 标记来定位
- 各行业简报（MLCC/原子级制造/半导体/财经）的内容在 Response 段落，篇幅通常 2-20KB

### Step 3: 识别值得提炼的内容
识别 1-5 个值得结构化的核心概念或方法论，优先关注：
- 小红书运营经验
- 飞书 API 使用技巧
- Cron 任务管理
- 内容创作方法论
- 产业链洞察

### Step 4: 写入知识卡片
```
/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_名称.md
/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_名称.md
```

格式：
```
# {名称}

**背景**
**核心洞察**
**对Jerry的意义**
**延伸思考**

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
用 tenant_access_token 方式发送飞书 post 消息到 `oc_1e92781cdf15f71fad7dcd8d5d386e76`，报告提炼了几张卡片及标题。

凭证：
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: [FEISHU_SECRET_REDACTED]

---

## 已蒸馏内容模块（gesila_compile）

### 架构
- **原料来源**：`/Volumes/256G/gesila_wiki/`（只读，不触碰原内容）
- **编译输出**：`/Volumes/256G/gesila_compile/`（新增蒸馏内容）
- **Git共享**：`gesila_compile/.git` 是 `gesila_wiki/.git` 的软链接，推送到同一个 GitHub 仓库

### 编译目录结构
```
gesila_compile/
├── sources/     ← 已蒸馏来源（01_Inbox日志 + 02_Knowledge快速引用）
├── entities/    ← 已蒸馏实体（人物/公司/产品，自动合并同名）
├── concepts/    ← 已蒸馏概念（方法论/框架，自动合并同名）
├── syntheses/   ← 已蒸馏综合（跨主题分析，待扩展）
├── distill.py   ← 编译脚本
├── index.md     ← 总入口
└── log.md       ← 操作日志
```

### 蒸馏脚本使用
```bash
# 全量蒸馏（首次运行，对所有历史内容）
cd /Volumes/256G/gesila_compile && python3 distill.py --full

# 增量蒸馏（cron自动跑，只处理新增/修改）
cd /Volumes/256G/gesila_compile && python3 distill.py --incremental
```

### 蒸馏后Git同步（distill.py内置）
`distill.py` 末尾自动调用 `git_sync()`，逻辑：
1. `git status --porcelain` 检测是否有变更
2. `git add .` 暂存所有变更
3. `git commit -m "YYYY-MM-DD HH:MM 自动蒸馏同步"`
4. `git push origin main` 推送到 GitHub

**注意**：`git push` 若被拒绝（remote有新内容），是因为本地和remote有冲突。错误信息：
```
! [rejected] main -> main (fetch first)
error: failed to push some refs to 'https://github.com/gesilajerry/gesila_wiki.git'
```
**解法**：先 `git stash` → `git pull origin main --rebase` → `git stash pop` → `git push origin main`

### Wiki平台展示集成
**已蒸馏页面**：`http://127.0.0.1:5001/compile`

app.py 已添加：
- `COMPILE_ROOT = "/Volumes/256G/gesila_compile"` 配置
- `COMPILE_SUBDIRS` 映射（sources→已蒸馏来源, entities→已蒸馏实体, concepts→已蒸馏概念, syntheses→已蒸馏综合）
- `get_compile_cards()` / `_load_compile_cards()` 缓存加载
- `/compile` 首页路由
- `/compile/category/<cat>` 分类路由
- `/compile/card/<slug>` 详情路由
- search路由已合并 wiki + compile 双搜索

**重启生效**：
```bash
# 先找到旧进程
ps aux | grep "wiki_platform/app\.py" | grep -v grep

# kill后重启
kill <PID>
cd /Volumes/256G/gesila_wiki/wiki_platform && python3 app.py &
```

### Cron定时任务
- job_id: `70c597681993`
- 时间: `0 9 * * *`（每天09:00）
- 任务: `cd /Volumes/256G/gesila_compile && python3 distill.py --incremental`

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是 Hermes Agent 的知识管理员。执行结构层提炼任务，从归档日志中提炼结构化知识卡片。

## 凭证
- APP_ID: cli_a96f4f328a785cd2
- APP_SECRET: [FEISHU_SECRET_REDACTED]
- 飞书会话: oc_1e92781cdf15f71fad7dcd8d5d386e76

## 执行步骤

### Step 1: 确定日期
当天日期（如：2026-04-26）

### Step 2: 读取归档日志
读取 `Hermes_YYYY-MM-DD_推送日报.md` 和 `Hermes_YYYY-MM-DD_会话复盘.md`

### Step 3: 识别值得提炼的内容
识别1-5个值得结构化的核心概念或方法论，优先关注：
- 小红书运营经验
- 飞书 API 使用技巧
- Cron 任务管理
- 内容创作方法论

### Step 4: 写入知识卡片
- 概念卡：`/Volumes/256G/gesila_wiki/02_Knowledge/概念卡/Hermes_YYYY-MM-DD_XXX.md`
- 方法论：`/Volumes/256G/gesila_wiki/02_Knowledge/方法论/Hermes_YYYY-MM-DD_XXX.md`

格式：
```
# {名称}

**背景**
**核心洞察**
**对Jerry的意义**
**延伸思考**

**标签**：Hermes × 关键词1 × 关键词2
```

### Step 5: Git 提交
```bash
cd /Volumes/256G/gesila_wiki && git add -A && git commit -m "结构层：Hermes YYYY-MM-DD提炼完成" && git push
```

### Step 6: 发飞书简报
报告提炼了几张卡片及标题。

### 重要规则
- **禁止 [SILENT]**

## Response

**结构层提炼任务完成报告**

**日期**：2026-05-25

**归档来源**：21个Cron任务（窗口：2026-05-24 22:34 → 2026-05-25 21:13）

**提炼成果**：3张知识卡片

---

**📌 方法论（2张）**

1. **BHR WebSocket修复** — 小红书梵高图文流水线 BHR 发布失败，`get_ws_url()` 依赖的日志文件不存在，Chrome 实际在 9543 端口运行。修复方案：直接 curl `http://127.0.0.1:9543/json` 获取 WebSocket URL 替代日志读取。

2. **mmx chat改写不生效应对** — `mmx text chat` 有时返回原文而非改写内容（透传模式）。应对策略：明确指定改写风格、或手动编写小红书风格文案（每句≤20字、emoji增强节奏、第一句抓眼球）。

---

**📌 概念卡（1张）**

3. **飞书Post消息格式规范** — 飞书 post 消息的 `content` 必须是 `[[{...}]]`（数组的数组），每个内层数组 = 一个段落。外层 payload.content 才是 `json.dumps()` 后的字符串。230001 错误 90% 是 content 格式问题而非 token 问题。

---

**Git推送**：成功（commit `45a3c96`），推送前发现 `Hermes_2026-05-25_推送日报.md`（507KB）中嵌入了类似 GitHub Token 的内容导致 secret scanning 拦截，通过 soft reset 只保留知识卡片目录避开风险。

**飞书通知**：已发送 ✅


---

## Wiki Git Push (job_id: fdba14bc8010)

# Cron Job: Wiki Git Push

**Job ID:** fdba14bc8010
**Run Time:** 2026-05-25 23:02:23
**Schedule:** 45 22 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/git_wiki_push.py

推送本地 wiki 更改到 GitHub。

## Response

❌ **Wiki Git 推送失败 — GH013 Secret Scanning 拦截**

**根因定位**：发现 `01_Inbox/日志/Hermes_2026-05-25_推送日报.md` 在 commit `60cdac5` 中包含 GitHub PAT 模式文本 `[TOKEN_PREFIX_REDACTED]`（脱敏占位符但仍触发扫描），触发 GitHub Secret Scanning Push Protection。

**已尝试的修复**：
1. ✅ 定位敏感文件 + 字节级替换 `GHP_REDACTED` → `[GITHUB_TOKEN]`
2. ✅ `git commit --amend` 重写 1124c66（替换后新版）
3. ❌ 推送仍报 GH013 —— GitHub 仍指向 `60cdac5` commit 内路径

**关键发现**：`1124c66` 是 `--amend` 后的新 commit，tree 内容已正确脱敏，但 GitHub Secret Scanning 基于 commit graph 扫描所有父提交，`60cdac5` 仍在 HEAD 的祖先链中且包含 `ghp_` 模式。

**推荐解法（需人工确认）**：强制推送到 `eba4918`（昨天正常推送的commit），重置远程分支：

```
git push --force origin eba4918:main
```

或在 GitHub Secret Scanning 页面点击允许该 secret：
https://github.com/gesilajerry/gesila_wiki/security/secret-scanning/unblock-secret/3EDiYoPiAlRTIWEF8UaDuG3pR3r


---

## AI热点推送 22:00 (job_id: 037201748e42)

# Cron Job: AI热点推送 22:00

**Job ID:** 037201748e42
**Run Time:** 2026-05-25 22:49:17
**Schedule:** 35 22 * * *

## Prompt

[SYSTEM: The user has invoked the "minimax-search" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: minimax-search
description: MiniMax 联网搜索工具 — 通过 mmx search query 执行实时全网搜索，返回中文结果
category: research
---

# MiniMax 联网搜索

## 搜索命令

```bash
mmx search query --q "关键词" --output json
```

## 返回格式（JSON）

```json
{
  "organic": [
    {
      "title": "文章标题",
      "link": "https://example.com/article",
      "snippet": "文章摘要内容...",
      "date": "2026-04-27 16:39:56"
    }
  ],
  "base_resp": {"status_code": 0, "status_msg": "success"}
}
```

## 执行模式（在 execute_code 或 terminal 中）

推荐在 `execute_code` 中用 Python subprocess 调用：

```python
import subprocess, json

def mmx_search(query, max_results=10):
    cmd = f'mmx search query --q "{query}" --output json'
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=30)
    data = json.loads(r.stdout)
    results = []
    for item in data.get('organic', [])[:max_results]:
        results.append({
            'title': item.get('title', ''),
            'link': item.get('link', ''),
            'snippet': item.get('snippet', '').replace('</p><p>', ' ').replace('<p>', '').replace('</p>', ''),
            'date': item.get('date', '')
        })
    return results
```

## 搜索多条（多关键词合并去重）

```python
def search_briefing(keywords, max_results=5, max_total=12):
    all_results = {}
    for kw in keywords:
        try:
            results = mmx_search(kw, max_results=max_results)  # 注意参数名是 max_results，不是 max_per_kw
            for r in results:
                # 用 title 做去重 key
                key = r['title'][:30]
                if key not in all_results:
                    all_results[key] = r
        except Exception as e:
            print(f"搜索 '{kw}' 失败: {e}")
    
    items = list(all_results.values())
    items.sort(key=lambda x: x.get('date', ''), reverse=True)
    return items[:max_total]
```

## 摘要清洗

`snsippet` 字段包含 HTML 标签（`</p><p>`），需要清洗：

```python
import re
def clean_snippet(html):
    text = re.sub(r'<[^>]+>', '', html)  # 去除所有HTML标签
    text = text.replace('﻿', '').strip()
    return text[:200]  # 保留前200字符
```

## 飞书 Post 消息发送（踩坑总结）

飞书 post 消息 content 格式极易报 230001 错误（invalid message content），正确格式：

```python
import requests, json

# 正确的 content 格式：数组的数组 [[{...}], [{...}], ...]
# 每个内层数组 = 一个段落（paragraph），外层数组包含所有段落
content = [
    [{"tag": "text", "text": "第一行内容"}],
    [{"tag": "text", "text": "第二行内容"}],
    [{"tag": "text", "text": "第三行内容"}],
]

post_content = {
    "zh_cn": {
        "title": "标题",
        "content": content  # 直接放数组对象，不要再 json.dumps
    }
}

payload = {
    "receive_id": chat_id,
    "msg_type": "post",
    "content": json.dumps(post_content, ensure_ascii=False)
}

r = requests.post(
    "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id",
    headers={
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json; charset=utf-8"
    },
    data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
    verify=False
)
```

关键点：
- content 是 `[[{...}]]`（数组的数组），不是 `[{...}]`（扁平数组）——错误格式会导致 230001
- content 字段本身是对象（直接放数组），不是字符串；外层 payload.content 才是 json.dumps 后的字符串
- 报错 230001 通常是 content 格式不对，不是 JSON 字符串或 token 问题
- 先用 text 类型消息测试 chat_id 和 token 是否正确
- 用 `requests` 而非 `urllib.request`——Python 3.9 urllib 对飞书文件上传和部分 POST 会持续返回 500，改用 requests 解决

## 注意事项

- `mmx search` 返回中文结果，适合财经/AI/产业类搜索
- 每个查询 timeout=30 秒
- 关键词用空格分隔会变成短语匹配，效果更好
- 搜索结果默认按相关性排序，不是时间
- 如需时间排序，搜索 query 中可加日期如 "AI新闻 2026年4月"

## 新闻监控场景：提高搜索新鲜度的技巧

当搜索近期新闻时，以下策略可显著提升结果新鲜度：

1. **中文日期约束**：在 query 中加入具体日期的汉字形式，如 `"2026年5月9日 纳米"`，比 `"nanotech May 9 2026"` 返回结果更新
2. **中文实体名优先**：搜索中国科技新闻时，用中文专有名词直接搜索（如 `"汉原2号"`、`"本源悟空"`），英文名往往无结果
3. **多轮渐进式搜索**：先搜索宽泛关键词（如 `原子级制造 2026年5月`），再根据发现的实体名做二次精准搜索
4. **snippet 清洗**：返回的 snippet 含 HTML 标签，用 `re.sub(r'<[^>]+>', '', text)` 彻底去除，不要只用简单的字符串 replace
5. **搜索结果为空时的处理**：尝试去掉具体日期，改用 `"2026年5月 纳米制造 突破"` 等月级别约束，避免日期过细导致漏筛

## ⚠️ 重要：`-t 24h` 时间过滤标志会导致结果乱码

**问题**：`mmx search query -t 24h --limit 8 "关键词"` 这种写法（-t 24h 时间过滤）会返回完全无关的内容（歌曲、医疗信息等），不是新闻。**2026-05-21 实测确认**。

**正确做法**：去掉 `-t 24h` 标志，改用查询关键词中包含日期约束：
```bash
# ❌ 错误：-t 24h 导致结果乱码
mmx search query -t 24h --limit 8 "MLCC 被动元件"

# ✅ 正确：查询词中加日期（年/月/日汉字形式）
mmx search query --limit 8 "MLCC 被动元件 村田 太阳诱电 2026年5月21日"
mmx search query --limit 8 "MLCC产能 扩产 价格 2026年5月"
```

**实测对比（2026-05-21）**：
- 搜索 `"MLCC 被动元件 村田 太阳诱电 2026年5月21日"` → 返回三星电机68亿订单、村田/太阳诱电涨价等4条当日相关结果
- 搜索 `"MLCC 被动元件 -t 24h"` → 返回歌曲《24H》、尿量正常值等8条完全无关内容

## mmx text chat 正确用法

**mmx chat 不存在，正确命令是 `mmx text chat`**：不要用 `mmx chat`，会报 `Unknown command: mmx chat`。正确用法：

```bash
mmx text chat --model MiniMax-M2 --message "你的提示词"
```

注意：
- `--no-stream` 标志需要值，不能单独使用；改用非交互模式直接传 `--message`
- `mmx text chat --no-stream` 会报错 `Flag --no-stream requires a value`
- **`stdin.communicate(input=...)` 方式不生效**——mmx text chat 不从 stdin 读取，会报 `Missing required argument: --message`
- **正确方式**：将提示词写入临时文件，用 shell 字符串替换传入：

```python
import subprocess
from pathlib import Path

prompt = "你的提示词内容..."
Path("/tmp/mmx_prompt.txt").write_text(prompt)
cmd = 'mmx text chat --model MiniMax-M2 --message "$(cat /tmp/mmx_prompt.txt)"'
r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=120)
data = json.loads(r.stdout)
text = data['content'][1]['text']  # 对话回复在 content[1]
```

**mmx chat 改写不生效的替代方案**：如果 `mmx text chat` 返回原文而非改写内容，手动编写小红书风格文案。

## ⚠️ mmx image generate 在 execute_code 中会超时

**问题**：`mmx image generate` 在 `execute_code` 中执行时，即使 timeout=120 也会超时（sandbox 硬限制约 120s）。

**正确做法**：用 `terminal` 工具或 `subprocess.Popen` 后台运行，或在 `execute_code` 中用 `timeout=300`：
```python
# ✅ 推荐：terminal 工具（无超时限制）
proc = subprocess.Popen(img_cmd, shell=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
# 等待完成后检查文件
r = subprocess.run("ls -lt /tmp/semi_news* | head -3", shell=True, capture_output=True, text=True)

# ✅ 或在 execute_code 中用 300s 超时
r = subprocess.run(img_cmd, shell=True, capture_output=True, text=True, timeout=300)
data = json.loads(r.stdout)
img_path = data['saved'][0]  # 返回格式 {"saved": ["/path/to/file"]}
```

**返回格式**：图片路径在 JSON 的 `saved` 字段（数组）：
```json
{"saved": ["/tmp/semi_news_001.jpg"]}
```

## ⚠️ cron 环境中 Feishu token 不可用的处理

**问题**：cron 定时任务的沙箱环境没有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量，`feishu-env-credentials` skill 方式会失败。

**判断**：执行 `subprocess.run(['bash', '-c', 'echo $FEISHU_APP_ID'])` 返回空则说明环境变量不可用。

**解决方案**：
- 如果 cron job 配置了 `deliver: 'platform:chat_id:thread_id'`，系统会自动投递到飞书，无需手动调用 API
- 手动飞书发送失败不等于 cron 投递失败，以系统投递结果为准

## execute_code vs terminal 执行选择

> **重要**：在 `execute_code` 沙箱（Python 3.14）中，用 heredoc 方式写多行 Python 脚本时，跨行的 raw string regex（如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。

**正确做法**：用 `write_file` 先写脚本到 `/tmp/`，再用 `terminal` 执行：
```python
# ❌ execute_code heredoc 中会报 SyntaxError
python3 << 'PYEOF'
html = re.sub(r'^---\n[\s\S]+?\n---\n', '', md)
PYEOF

# ✅ 正确：先 write_file 到 /tmp/，再 terminal 执行
write_file("/tmp/publish.py", script_content)
terminal("python3 /tmp/publish.py")
```

**适用场景**：所有涉及多行字符串（含 regex、JSON 或复杂文本）的 Python 脚本，在 cron 或需要写文件执行的场景中，都应该用 write_file + terminal 方式。

[SYSTEM: The user has invoked the "blog-publisher" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: blog-publisher
description: 将 Markdown 文件批量发布到内网博客系统（https://192.168.30.198:8443），支持自动分类
triggers:
  - 内网博客发布
  - blog publisher
  - 批量发文章
  - 博客批量发布
---

# 内网博客批量发布

通过 Bearer Token + CSRF 认证，将 Markdown 文件直接发布到内网博客系统。

## 认证凭证（需自行获取）

> **⚠️ 凭证不能硬编码，必须在运行时获取并持久化。**

**获取方式：**

```bash
# 登录获取 Bearer Token（username/password 由管理员提供）
curl -s -X POST 'https://192.168.30.198:8443/api/user/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}' | jq -r '.data.token'

# 验证 Token 是否有效
curl -s 'https://192.168.30.198:8443/api/user' \
  -H 'Authorization: Bearer <TOKEN>' | jq '.data.uid'
```

**获取到 Token 后**，保存到 `~/.hermes/skills/blog-publisher/token.txt`，后续调用自动读取：

```python
TOKEN = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser().read_text().strip()
```

**首次使用流程：**
1. 向管理员申请账号（用户名 + 密码）
2. 调用登录接口获取 Bearer Token
3. 将 Token 写入 `~/.hermes/skills/blog-publisher/token.txt`
4. 后续发布直接读取文件，无需重复登录

## cron job 配置存储位置

**文件**: `/Users/mac/.hermes/cron/jobs.json`

**结构**（注意是 `id` 不是 `job_id`）：
```json
{"jobs": [{"id": "abc123", "name": "...", "prompt": "...", "skills": [...], ...}]}
```

**修改方式**: 直接编辑 `jobs.json`，用 `job['id']` 做 key 映射，再写回文件后 `json.dump(data, f, ensure_ascii=False, indent=2)` 保存。不需要通过 cronjob tool 的 update 接口（那样只能改部分字段）。

**cron job prompt 里的 blog-publisher 踩坑**：
- `publish_one` 函数体内硬编码 `"forum_id": 2` ——必须改成 `"forum_id": forum_id`（引用参数）
- 函数签名要加 `forum_id` 参数：`def publish_one(title, html, group_ids, forum_id=2, attempt=1):`
- 调用处要显式传：`publish_one(title, html, [12], forum_id=2)`
- 重试递归调用 `return publish_one(title, html, group_ids, attempt + 1)` — forum_id 默认值自动沿用，不需要额外传

## 发布流程（批量版）

### 关键注意事项

> **踩坑记录（2026-04/05 实测）：**
> 1. **每次请求必须单独获取 CSRF Token**——Token 不可以跨请求复用，否则 500。Bearer Token 有时效，发布失败先用 GET /api/user 验证 token 是否有效。
> 2. **Bearer Token 有效但 API 返回 401**——此时 CSRF 也必定失效，需重新获取 CSRF 后再重试（不是重新登录）。
> 3. **禁止用 emoji 正则过滤内容**——Python 3.14 的 emoji 范围 `\\U00002600-\\U000026FF` 会误删 CJK 中文字符，导致 HTML 乱码。中文内容原样传即可，无需过滤。
> 4. **标题中的 `×`(U+00D7) 必须替换为 `x`**——服务端 JSON 解析遇到 `×` 会返回 500。
> 5. **500 错误有随机性**，建议加 3 次重试（间隔 3-5 秒）。

### Step 1: 发布函数（含重试 + 单独 CSRF + 可选 forum_id）

```python
import urllib.request, json, ssl, time
from pathlib import Path

ctx = ssl.create_default_context()
ctx.check_hostname = False
ctx.verify_mode = ssl.CERT_NONE
TOKEN_FILE = Path('~/.hermes/skills/blog-publisher/token.txt').expanduser()

def get_token():
    """每次从文件读取最新 Token（支持运行时刷新）"""
    return TOKEN_FILE.read_text().strip()

def publish_one(title, content_html, group_ids, forum_id=2, attempt=1):
    """发布单篇文章，每次单独获取 CSRF。
    forum_id: 板块ID（默认2=综合管理，1=市场销售，4=研发工程）
    """
    TOKEN = get_token()  # ← 每次运行时读取，不硬编码
    try:
        # 每次单独获取 CSRF，不要复用
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            "forum_id": forum_id,  # ← 板块ID，不能写死2
            "title": title,
            "content": content_html,
            "group_ids": group_ids,
            "type": "blog"
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            'https://192.168.30.198:8443/api/discussion',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
                'Accept': 'application/json'
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(3)
            return publish_one(title, content_html, group_ids, forum_id=forum_id, attempt=attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}: {e.reason}'}
```

**板块对应 forum_id：**
- 综合管理（GeneralManagement）→ `forum_id=2`
- 研发工程（Develop）→ `forum_id=4`
- 市场销售（Marketing_Sale）→ `forum_id=1`

```python
# 示例调用
publish_one(title, html, [5], forum_id=2)   # 综合管理
publish_one(title, html, [16], forum_id=4)  # 研发工程
publish_one(title, html, [15], forum_id=1)  # 市场销售
```

### Step 2: Markdown 转 HTML

```python
import re

def md_to_html(md_text):
    """不含 emoji 过滤（Python 3.14 regex bug 会损坏 CJK 字符）"""
    html = md_text
    html = re.sub(r'^---\n[\s\S]+?\n---\n', '', html)
    html = re.sub(r'^# (.+)$', '<h1>\\1</h1>', html, flags=re.MULTILINE)
    html = re.sub(r'^## (.+)$', '<h2>\\1</h2>', html, flags=re.MULTILINE)
    html = re.sub(r'^### (.+)$', '<h3>\\1</h3>', html, flags=re.MULTILINE)
    html = re.sub(r'^#### (.+)$', '<h4>\\1</h4>', html, flags=re.MULTILINE)
    html = re.sub(r'^---$', '<hr>', html, flags=re.MULTILINE)
    html = re.sub(r'```(?:\w*)\n([\s\S]*?)\n```', '<pre><code>\\1</code></pre>', html)
    html = re.sub(r'`([^`]+)`', '<code>\\1</code>', html)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    # × 符号可能导致 500，标题级别在调用处处理
    lines = html.split('\n')
    in_table = False
    new_lines = []
    for line in lines:
        if '|' in line and line.strip().startswith('|'):
            cells = [c.strip() for c in line.strip().strip('|').split('|')]
            if not in_table:
                new_lines.append('<table border="1" cellpadding="8" cellspacing="0">')
                in_table = True
            is_header = all(re.match(r'[-:]+$', c.replace(' ', '')) for c in cells)
            tag = 'th' if is_header else 'td'
            new_lines.append('<tr>' + ''.join(
                f'<{tag}>{c}</{tag}>' for c in cells
                if not re.match(r'^[-:]+$', c.replace(' ', ''))
            ) + '</tr>')
        else:
            if in_table:
                new_lines.append('</table>')
                in_table = False
            new_lines.append(line)
    if in_table:
        new_lines.append('</table>')
    return '\n'.join(new_lines)

def extract_title(md):
    """提取标题并处理特殊字符"""
    m = re.search(r'^#\s+(.+)$', md, re.MULTILINE)
    title = m.group(1).strip() if m else '无标题'
    title = title.replace('×', 'x')   # × 会导致服务端 500
    title = title.replace('$', '美元')  # $ 也会导致服务端 500
    return title
```

### Step 3: 批量发布

```python
files = [
    "/path/to/article1.md",   # 综合管理
    "/path/to/article2.md",   # 研发工程
    "/path/to/article3.md",   # 市场销售
]

# 【重要】必须根据内容判断 forum_id：
# - 综合管理 → forum_id=2, group_ids=[5]
# - 研发工程 → forum_id=4, group_ids=[16]
# - 市场销售 → forum_id=1, group_ids=[15]

mapping = {
    "article1.md": (2, [5]),    # 综合管理
    "article2.md": (4, [16]),   # 研发工程
    "article3.md": (1, [15]),   # 市场销售
}

for fp in files:
    md = Path(fp).read_text(encoding='utf-8')
    title = extract_title(md)
    html = md_to_html(md)
    forum_id, group_ids = mapping.get(Path(fp).name, (2, [5]))  # 默认综合管理
    r = publish_one(title, html, group_ids, forum_id=forum_id)
    status = '✅' if r.get('success') else f"❌ {r.get('error')}"
    print(f"{status} [{Path(fp).name}] {title}")
    time.sleep(10)  # 间隔≥10秒防服务端限速
```

## 网站板块结构（发布前必须先选板块）

网站顶部有三个一级板块 tab，发文章前**必须先点击对应板块**，再在板块内发布。

### 三个板块的 forum_id 和 group_ids（必须严格区分）

| 一级板块 | forum_id | route_name | group_ids（blog类型） |
|----------|----------|------------|---------------------|
| **综合管理** | **2** | GeneralManagement | [5] |
| **研发工程** | **4** | Develop | [16] |
| **市场销售** | **1** | Marketing_Sale | [15] |

> ⚠️ **forum_id 区分板块**：所有文章全发到综合管理（forum_id=2）的原因找到了——旧版 skill 写死了 forum_id=2。三个板块 forum_id 各不同（见下表），发布时必须根据内容选对 forum_id。

### 一级板块（tab）→ forum_id 对照

| 一级板块 | 板块含义 | forum_id |
|----------|---------|----------|
| **综合管理** | General Management | 2 |
| **研发工程** | Develop | 4 |
| **市场销售** | Marketing Sale | 1 |

### 各板块内容判断规则

根据标题+内容关键词判断发到哪个板块：

| 判断关键词 | 一级板块 | forum_id |
|-----------|---------|----------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、管理复盘 | **综合管理** | 2 |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** | 4 |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** | 1 |

**如果仍然无法判断** → 发"综合管理"（forum_id=2，最通用）

### 各板块内 group_ids

**综合管理（forum_id=2）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 5 | 综合管理-Blog | 通用管理文章 |
| 7 | 人事相关 | 人事、招聘 |
| 8 | 财务相关 | 财务、报销、成本 |
| 44 | 业务流程 | 流程、规范、制度 |

**研发工程（forum_id=4）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 16 | 研发工程-Blog | 通用研发文章 |
| 12 | 专业知识 | 技术简报、行业研究、科技动态（2026-05-11 实测有效） |

**市场销售（forum_id=1）：**

| group_id | 分类名称 | 适用内容 |
|----------|---------|---------|
| 15 | 市场销售-Blog | 通用市场文章 |

**注意**: `group_ids` 是数组，可以传多个。发布时 **forum_id 必须与一级板块对应**，group_ids 在对应板块内选择。

**默认**（无法判断时）：forum_id=2（综合管理），group_ids=[5]

## 发布前检查清单

> **重要**：用户说"发到考拉Blog"时，**先查 wiki 是否有现成文档**，不要自己重写。
> 用户明确说过"是wiki沉淀的内容 不用写新写的"——自己写的内容不如 wiki 里经过迭代的沉淀内容。

**判断方法**：
- 用户提到的话题在 wiki 方法论/项目复盘目录有对应文件 → 直接用 wiki 原文发布
- 用户没有提到 wiki 或明确说"帮我写一篇" → 才自己起草

**Wiki 知识库路径**：`/Volumes/256G/gesila_wiki/02_Knowledge/`

```bash
# 搜索 wiki 中的相关文档
ls /Volumes/256G/gesila_wiki/02_Knowledge/方法论/ | grep "关键词"
ls /Volumes/256G/gesila_wiki/02_Knowledge/项目复盘/ | grep "关键词"
```

**板块+分类判断（必做）**：
1. 先读文章内容，判断属于哪个一级板块（综合管理/研发工程/市场销售）
2. 再在对应板块内选择合适的 group_ids
3. 如果是市场简报/AI简报/财经简报 → 市场销售板块 + 12专业知识
4. 如果是管理制度/财务/人事/战略 → 综合管理板块
5. 如果是技术研发/设备/工艺 → 研发工程板块，group_ids=[12]（专业知识）或 [16]（研发工程-Blog）

## 简报类 Cron Job 的 Koala 板块归属

在 cron job prompt 里直接用 inline Python 发布 Koala 文章时，forum_id 归属如下（重要！否则发错板块）：

| 简报类型 | Koala 板块 | forum_id | group_ids |
|---------|-----------|----------|-----------|
| MLCC产业链简报 | 市场销售 | 1 | [12] |
| 半导体热点简报 | 市场销售 | 1 | [12] |
| 财经热点简报 | **综合管理** | 2 | [12] |
| AI科技热点简报 | **综合管理** | 2 | [12] |
| 原子级制造科研简报 | **研发工程** | 4 | [12] |

> ⚠️ **常见错误**：所有简报 prompt 最初都写的是 forum_id=1（市场销售），AI/财经/原子级制造板块全错。需要在 jobs.json 里逐一修正为正确的 forum_id。
>
> **jobs.json 直接编辑**：cronjob tool 的 update 接口不支持所有字段修改，forum_id 修正必须直接编辑 `~/.hermes/cron/jobs.json`（注意字段名是 `id` 而非 `job_id`），修改后保存即可，cron 下次执行时生效。

## 发布前板块+分类判断流程

> **⚠️ 必须按以下顺序判断：先判断一级板块 tab，再在同一板块内选择二级分类 group_ids。**

### 第一步：判断一级板块（tab）

根据内容主题，按以下规则判断发到哪个 tab：

| 判断关键词（标题+内容） | 一级板块 |
|----------------------|---------|
| 管理、制度、行政、人事、财务、报销、流程、规范、战略、运营、招聘、培训学习、复盘（非技术） | **综合管理** |
| 技术、开发、研发、代码、编程、系统、工艺、设备、调试、技术方案、技术复盘 | **研发工程** |
| 市场、销售、简报、客户、售前、行业分析、营销、广告、行业研究 | **市场销售** |

**如果仍然无法判断** → 发"综合管理"板块（最通用的内部管理内容）

### 第二步：在对应板块内选择 group_ids

**综合管理板块：**
- 管理心得/战略 → 49综合分享 或 12专业知识
- 财务/报销/成本 → 8财务相关
- 招聘/培训/人事 → 7人事相关
- 流程/制度/规范 → 44业务流程
- 管理复盘/事件分析 → 41案例复盘
- 行业研究/宏观分析 → 12专业知识

**研发工程板块：**
- 技术开发/编程/系统 → 43IT技术
- 技术技巧/方法心得 → 11经验技巧
- 工具分享 → 47工具推荐
- 技术培训/学习笔记 → 13培训学习
- 技术复盘/研发案例 → 41案例复盘

**市场销售板块：**
- 市场简报/行业分析/研究报告 → 12专业知识
- 销售案例/客户案例 → 41案例复盘
- 销售技巧/客户沟通 → 11经验技巧
- 市场心得/综合 → 49综合分享

**默认**（无法判断时）：板块选"综合管理"，分类选 `49` 综合分享

## Markdown 转 HTML 注意事项

- 标题：`#` → `<h1>`，`##` → `<h2>` 等
- 表格：转换为 `<table border="1" cellpadding="8" cellspacing="0">`
- 代码块：`<pre><code>...</code></pre>`
- 列表：用 `<ol><li>...</li></ol>` 或 `<ul><li>`
- 分割线：`<hr>`
- 图片：暂不支持直接上传，需用外部 URL

### 评论接口（POST）

```python
def post_comment(disc_uuid, content, attempt=1):
    """对文章发评论，disc_uuid 为文章UUID（不是数字id）"""
    try:
        csrf_req = urllib.request.Request(
            'https://192.168.30.198:8443/api/csrf',
            headers={'Authorization': f'Bearer {TOKEN}'})
        csrf = json.loads(
            urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read()
        )['data']

        payload = json.dumps({
            'discussion_uuid': disc_uuid,  # ← UUID字符串，不是数字id
            'content': content,
            'type': 'discussion'
        }, ensure_ascii=False).encode('utf-8')

        req = urllib.request.Request(
            f'https://192.168.30.198:8443/api/discussion/{disc_uuid}/comment',
            data=payload,
            headers={
                'Authorization': f'Bearer {TOKEN}',
                'X-CSRF-Token': csrf,
                'Content-Type': 'application/json; charset=utf-8',
            },
            method='POST')
        return json.loads(urllib.request.urlopen(req, context=ctx, timeout=10).read())

    except urllib.error.HTTPError as e:
        if e.code == 500 and attempt < 3:
            time.sleep(2)
            return post_comment(disc_uuid, content, attempt + 1)
        return {'success': False, 'error': f'HTTP {e.code}'}

# 示例
r = post_comment('V5Y2XYYxlNy37vuh', '写得很好，收藏了！')
if r.get('success'):
    print(f"评论成功，id={r['data']}")
```

**踩坑：评论接口不是 `/api/comment`，而是 `/api/discussion/{uuid}/comment`**
- 文章UUID从列表接口获取：`GET /api/discussion?forum_id=2&sort=latest&per_page=10`
- 数字id无法用于评论，必须用uuid字符串

## 执行环境：execute_code vs terminal 的选择

> **重要踩坑经验（2026-05 实测）**：
> - **Koala 博客发布**：推荐在 `terminal` 中用**写脚本到文件再执行**的方式，不要用 heredoc。execute_code 的 Python 3.14 沙箱对 heredoc 中的多行 raw string（尤其是含 `\n` 的 regex 模式如 `r'^---\n[\s\S]+?\n---\n'`）会报 `SyntaxError: EOL while scanning string literal`，即使 `<< 'PYEOF'` 也无法避免。
> - **正确执行方式**：
>   ```python
>   # 步骤1: 用 write_file 写脚本到 /tmp/
>   write_file("/tmp/publish_mlcc.py", script_content)
>   # 步骤2: 用 terminal 执行
>   terminal("python3 /tmp/publish_mlcc.py")
>   ```
> - **飞书 API 调用**：必须在 `terminal` 中执行，不能在 `execute_code` 沙箱中运行。原因：
>   1. 沙箱 Python 3.14 对多行字符串中的中文引号 `""`（curly quotes）会报 `SyntaxError`，而 execute_code 中的 markdown 字符串常含此类字符
>   2. `from pathlib import Path` 在沙箱中可能未 import，或 `Path.home()` 指向沙箱内部而非宿主 macOS
>   3. `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量在沙箱中为空（沙箱隔离），即使文件中有凭证也需显式 hardcode
> - **飞书发送推荐模式**：在 `terminal` 中用写脚本到文件的方式执行，读取环境变量 `$FEISHU_APP_ID` / `$FEISHU_APP_SECRET`，成功率最高
> - **execute_code 沙箱限制**：多行 raw string（`r'...'`，尤其是跨行的正则表达式）在沙箱中会报语法错误，必须换用 terminal + 文件执行

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| execute_code 中飞书 API 报 400/token 失败 | 沙箱环境 env vars 为空且 Python 3.14 语法问题 | 换用 terminal 工具执行飞书 API 调用 |
| 评论报404 | 接口路径写错 | 必须用 `/api/discussion/{uuid}/comment`，不是 `/api/comment` |
| 404 err:record not found | 用了数字id而非uuid | 评论接口只接受UUID字符串 |
| csrf token mismatch | CSRF Token 为空字符串 | 从响应 `data` 字段提取实际值 |
| 发布成功但列表查不到 | 可能是 group_ids 权限问题 | 确认账号有该分类权限 |
| koala_session httpOnly | 浏览器 cookie 是 httpOnly | 使用 Bearer Token 而非 cookie |
| HTTP 500 随机出现 | CSRF Token 复用或服务端波动 | 每次单独获取 CSRF + 3次重试 |
| HTTP 500 稳定复现（标题含×） | `×`(U+00D7) 导致 JSON 解析失败 | 将 `×` 替换为 `x` 后重试 |
| HTML 变乱码（CJK被删） | Python 3.14 emoji 正则 bug 误删 CJK | 不要用 emoji 过滤函数，原文直传 |
| HTTP 500 err:ratelimit | 请求过于频繁，触发服务端限速 | 批量发布时每篇间隔 ≥10 秒，且必须单篇顺序发，不能并发 |
| Koala发布后 `r.get('data').get('id')` 报 AttributeError | `r['data']` 返回的是字符串（文章UUID）而非 dict | 用 `data = r.get('data', ''); article_id = data if isinstance(data, str) else data.get('id', 'N/A')` 判断类型后再取值 |

### 踩坑：login_method 显示"等待管理员审批"

`GET /api/user/login_method` 返回 `prompt: "您的注册申请已提交，请等待管理员审批"` 不代表账号不可用——这只是注册流程状态。只要 Bearer Token 有效（可调用 `GET /api/user` 验证），账号就能正常发布文章。已有 token 直接用，无需重新登录。

---

## 飞书文件上传（需调用 feishu-text-message skill）

飞书 IM API 需要单独认证，参考 `feishu-text-message` skill 获取 `tenant_access_token`。

## Git Push 凭证问题（HTTP/SSH 混合环境）

**问题**：git push 时一直超时或 `could not read Username`——credential helper（osxkeychain / `gh auth git-credential`）在这个环境里读不到 token，导致无法认证。

**原因**：macOS 钥匙串里的 GitHub PAT 没被 git 正确调用，HTTPS Push 无法弹出认证。

**解决方案：创建 netrc 文件**

```python
import subprocess, os
token = subprocess.run(['gh', 'auth', 'token'], capture_output=True, text=True).stdout.strip()
home = os.path.expanduser('~')
with open(f'{home}/.netrc', 'w') as f:
    f.write(f"""machine github.com
login gesilajerry
password {token}
""")
os.chmod(f'{home}/.netrc', 0o600)
```

然后在 git push 时指定使用 store helper：

```bash
GIT_TERMINAL_PROMPT=0 git -c credential.helper=store push origin main
```

**验证**：`~/.netrc` 权限必须是 600。

The user has provided the following instruction alongside the skill invocation: [SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你AI科技新闻助手。请用 mmx search 命令搜索过去24小时内发生的AI科技热点新闻，生成AI今日热点简报。

## 搜索命令（每次执行一条，分开执行）
```bash
mmx search query -t 24h --limit 8 "人工智能 AI大模型 GPT"
```
```bash
mmx search query -t 24h --limit 8 "AI创业 融资 技术突破"
```
```bash
mmx search query -t 24h --limit 8 "自动驾驶 机器人 智能硬件"
```

## 内容要求
- 每条搜索结果整理为：标题 + 一句话摘要
- 最终输出4段式简报：【今日要闻】【技术进展】【行业动态】【今日关注】
- 前三段各3-5条，第4段【今日关注】综合当期热点给出一个有深度的评论或展望
- 字数控制在900字以内

## 输出格式
最后输出完整简报内容，格式如下：
```
🤖 AI晚报 | YYYYMMDD

【今日要闻】
（3-5条，每条：标题 + 摘要）

【技术进展】
（3-5条，每条：标题 + 摘要）

【行业动态】
（3-5条，每条：标题 + 摘要）

【今日关注】
（综合当期热点，给出一个有深度的评论或展望，2-3句话）
```---

完成上述简报后，执行以下动作：

**第五步：发布到Koala博客（综合管理-专业知识）**
```python
import sys, urllib.request, json, ssl, time
from pathlib import Path

TOKEN_FILE = Path.home() / ".hermes/skills/blog-publisher/token.txt"
TOKEN = TOKEN_FILE.read_text().strip()
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE

def md_to_html(md):
    import re
    html = re.sub(r'^---
[\s\S]+?
---
', '', md)
    for p, tag in [('^# (.+)$','h1'),('^## (.+)$','h2'),('^### (.+)$','h3')]:
        html = re.sub(p, f'<{tag}>\\1</{tag}>', html, flags=re.MULTILINE)
    html = re.sub(r'\*\*(.+?)\*\*', '<strong>\\1</strong>', html)
    html = re.sub(r'\*(.+?)\*', '<em>\\1</em>', html)
    html = re.sub(r'^> (.+)$', '<p><em>\\1</em></p>', html, flags=re.MULTILINE)
    return html

    title = "AI晚报 YYMMDD"
content_html = md_to_html("（上述简报完整markdown内容）")

csrf_req = urllib.request.Request('https://192.168.30.198:8443/api/csrf', headers={'Authorization': f'Bearer {TOKEN}'})
csrf = json.loads(urllib.request.urlopen(csrf_req, context=ctx, timeout=10).read())['data']
payload = json.dumps({"forum_id": 2, "title": title, "content": content_html, "group_ids": [12], "type": "blog"}, ensure_ascii=False).encode('utf-8')
req = urllib.request.Request('https://192.168.30.198:8443/api/discussion', data=payload, headers={'Authorization': f'Bearer {TOKEN}', 'X-CSRF-Token': csrf, 'Content-Type': 'application/json; charset=utf-8'}, method='POST')
r = json.loads(urllib.request.urlopen(req, context=ctx, timeout=15).read())
print('Koala:', '✅' if r.get('success') else f'❌ {r}')
```

**第六步：发送飞书**
将上述完整简报内容（不带配图命令）发送到飞书 oc_1e92781cdf15f71fad7dcd8d5d386e76

## Response

🤖 **AI晚报 | 2026年05月25日**

---

【今日要闻】

・**DeepSeek-V4-Pro永久降价75%，登顶全球性价比最高大模型**
  DeepSeek宣布V4-Pro将于5月31日结束限时折扣后实施永久降价，整体降幅达75%，成为全球性价比最高的大模型API。

・**DeepSeek-V4-Flash以3.43万亿Token登顶全球调用榜**
  5月18日至24日，DeepSeek V4-Flash首次登顶OpenRouter全球周调用量榜首，环比暴涨66%，中国AI模型已连续四周超越美国。

・**Anthropic新一轮融资估值或突破9000亿美元**
  据彭博社报道，Anthropic即将完成超300亿美元新融资，估值将超越OpenAI成为全球最高估值AI初创企业。

・**全球AI大模型周调用量连续五周上涨**
  上周全球大模型总调用量达28.9万亿Token，环比增长7.4%，其中中国AI模型周调用量达9.223万亿Token，环比增长19.89%。

・**DeepSeek约700亿元融资进入最后阶段**
  腾讯控股、IDG资本和Monolith Capital已接近确定参与DeepSeek约700亿元人民币融资，投前估值约450亿美元。

---

【技术进展】

・**GPT-5.6泄露：代号iris-alpha，多模态能力大幅升级**
  开发者在OpenAI Codex后台日志中发现尚未公布的gpt-5.6，内部代号iris-alpha，编程能力相比GPT-5.5进一步大幅提升。

・**谷歌更新Android开发AI模型排行，GPT 5.5夺冠**
  谷歌最新榜单中，GPT 5.5以约2%的优势超越GPT 5.4和Gemini 3.1 Pro，成为Android应用开发领域最强AI模型。

・**国产大模型Q1融资超1100亿元，商业化能力成核心指标**
  一季度AI领域融资超1100亿元，月之暗面、阶跃星辰等国产大模型企业拿下超300亿元融资，参数规模让位于商业化落地能力。

・**具身智能机器人赛道一周内两家公司融资数亿元**
  维他动力、鹿明机器人等具身智能企业在5天内接连斩获数亿元融资，硬件+AI融合的机器人商业化路径持续受资本追捧。

---

【行业动态】

・**特斯拉FSD监督版正式入华，名称更趋实际**
  特斯拉宣布FSD监督版可在华使用，并将系统更名为「特斯拉辅助驾驶」，仅HW4.0及以上硬件车型可适配完整版FSD功能。

・**2026智能养老服务机器人应用大赛在廊坊举办**
  5月25日至26日，国内外企业、高校、科研院所共同参与智能养老服务机器人应用大赛，机器人正加速落地养老场景。

・**2026年全球智能机器人硬件市场接近300亿美元**
  IDC预测2026年全球智能机器人硬件市场规模将接近300亿美元，中国将突破110亿美元，引领全球具身智能市场增长。

---

【今日关注】

大模型价格战进入深水区。DeepSeek以「V4-Pro永久降价75%」和「V4-Flash登顶全球调用榜」双重信号，宣告基础模型层的成本竞争已趋白热化。与此同时，Anthropic估值冲击9000亿美元、DeepSeek 700亿融资进入尾声，显示资本对头部玩家的共识正在快速凝聚。GPT-5.6代号iris-alpha泄露，则暗示多模态原生融合或成下一代模型竞争的核心战场。可以预见，2026年下半年，「性价比决胜+多模态融合」将成为AI大模型竞争的双主线。

---

✅ **已发布至 Koala博客**（综合管理-专业知识，article_id=EG2zSXhG9HCaTAMk）
✅ **已发送至飞书**（oc_1e92781cdf15f71fad7dcd8d5d386e76）


---

## xhs-vangogh-r3 (job_id: d4fde929cb2e)

# Cron Job: xhs-vangogh-r3

**Job ID:** d4fde929cb2e
**Run Time:** 2026-05-25 22:45:58
**Schedule:** 30 22 * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行 /usr/bin/python3 /Users/mac/.hermes/scripts/xhs_vangogh_gen.py 3

每天自动运行，梵高融合风图文：10张原创插画 + 专属情绪文案 + BGM + 慢速TTS，产出发飞书。

## Response

**小红书梵高融合风图文日更 · 第3轮完成**

📍 主题：**热带雨林**

| 序号 | 内容 |
|------|------|
| 1 | 雨林树冠层，阳光从缝隙洒下 |
| 2 | 藤蔓从树顶垂落，如绿色瀑布 |
| 3 | 雨林溪流，河水清澈见底 |
| 4 | 色彩斑斓的鹦鹉站在树枝上 |
| 5 | 大象在雨林边缘缓缓行走 |
| 6 | 雨林底层的蕨类植物，舒展如花 |
| 7 | 树懒抱着树干，慢悠悠地眨眼 |
| 8 | 雨林深处的野生兰花，独自绽放 |
| 9 | 猴子在树冠间跳跃，身影灵活 |
| 10 | 雨后雨林，泥土芬芳，苔藓翠绿 |

✅ 发布成功：**雨林深处，每一口呼吸都是活着的证明。**


---
